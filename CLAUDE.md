@AGENTS.md

# Gearwood: The Broken Spring — Claude 개발 가이드

## 0. 프로젝트 목적

이 문서는 `Gearwood: The Broken Spring` 개발을 재개할 때 Claude가 프로젝트의 현재 상태와 핵심 규칙을 빠르게 파악하고, 기존 기능을 깨뜨리지 않으면서 다음 작업을 이어가기 위한 개발 컨텍스트다.

- 플랫폼: React Native + Expo
- 장르: 사이드스크롤 플랫포머
- 콘셉트: 스팀펑크 × 자연정령
- 현재 구조: 단일 연속 레벨 + 체크포인트 + Gear Socket
- 핵심 기믹: Bloom Shift
- 핵심 이동: 점프 / 대시 / 그래플 / 벽점프 / 벽슬라이드

> 중요: 개발로그에 명시된 구현 범위와 불변 규칙을 우선한다. 문서에 없는 기능을 임의로 추가하거나 기존 구현을 재해석하지 않는다.

> 문서 역할 분담(중복 방지): 수치·규칙의 단일 소스는 이 문서(`CLAUDE.md`)다. `개발로그.md`는 구현 이력·버그·의사결정 기록에 집중하며, 이미 여기 있는 수치/규칙을 다시 전부 옮겨 적지 않고 해당 장을 참조한다. `docs/gearwood-*.md`는 프로젝트 초기 창작 기획 원안이며 실제 구현 범위와 다를 수 있다.

> **충돌 시 기본값 규칙**: 문서·지침 간 내용이 서로 충돌하면 **더 최근에 추가된 내용을 기본값**으로 따른다. 이 문서 안에서는 뒤 번호 섹션(예: 19장)이 앞 번호 섹션보다 최신이며, `CLAUDE.md`/`개발로그.md`는 항상 `docs/gearwood-*.md` 원안보다 최신 기준으로 우선한다.

---

## 1. 작업 시작 전 필수 절차

새 세션에서 작업을 시작하면 다음 순서로 확인한다.

1. `개발로그.md`의 **현재 구현 상태**를 먼저 읽는다.
2. **불변 게임 규칙**과 **현재 미완성 항목**을 확인한다.
3. **새로운 기능을 추가하기 전에, 기존 기능을 더 재미있게 만들 수 있는지 먼저 검토한다.** 신규 시스템 착수보다 우선순위가 높다 — 조작감 다듬기, 수치 밸런싱, 연출/피드백 보강, 기존 메커닉 간 시너지 발굴 등을 먼저 훑고, 그래도 새 기능이 맞다고 판단되면 그때 진행한다.
4. 실제 코드 구조를 확인하고 관련 파일을 찾는다.
5. 기존 테스트가 있으면 먼저 실행한다.
6. 수정 범위를 최소화한다.
7. 수정 후 관련 테스트 → 전체 회귀 테스트 순서로 검증한다.
8. 새로 발견한 버그나 중요한 결정은 `개발로그.md`에 기록한다.

### 절대 하지 말 것

- 이미 구현된 시스템을 모르고 처음부터 다시 구현하지 않는다.
- 기존 수치를 임의로 변경하지 않는다.
- 테스트되지 않은 기능을 '완료'라고 기록하지 않는다.
- 문서에 없는 기능을 기존 기획의 일부라고 가정하지 않는다.
- 범위 밖 기능을 몰래 추가하지 않는다.
- 단순 리팩터링으로 게임플레이 동작이 변하지 않도록 한다.

---

## 2. 현재 구현된 핵심 시스템

### 기본 플랫포머

- 타일 충돌 기반 이동/점프 물리
- 적 스톰프 판정
- 코인 수집
- 목표 깃발
- 5버튼 터치 컨트롤
- HUD
- 시작 / 게임오버 / 승리 화면
- 멀티터치 입력 처리

### Bloom Shift

- 시프트 노드 접촉 시 기계 ↔ 자연 상태 전환
- 상태별 발판 변화
- edge-triggered arming으로 반복 토글 방지
- 총 4개 존

### 몬스터

1. 코그마이트
2. 스포어 스프라이트
3. 프레셔 피스톤
4. 바이오 코일
5. 스팀 블로어(미니보스)

### 이동

- 대시
- 오버드라이브
- Root-Hook 그래플
- 벽점프 / 벽슬라이드
- 공중 제어력

### Gear Socket

- 스프링
- 마그넷
- 스팀부스트
- 루트훅 코그
- 미러
- 캐논 점프 시너지

### 진행

- 2구역 확장
- 5개 체크포인트
- 체크포인트 부활
- 처치 후 임시 발판

---

## 3. 불변 게임 규칙

아래 수치는 리팩터링이나 기능 추가 시 반드시 유지한다.

- Air Control = 지상 이동속도의 85%
- 벽슬라이드 낙하속도 = 최고 낙하속도의 45%
- 공중 대시는 1회
- 지상 대시는 무제한이지만 공용 쿨다운 존재
- 오버드라이브 게이지: 0~100
- 오버드라이브 획득: 스톰프 +15 / 대시 +10 / 벽점프 +10
- 콤보가 1.5초 이상 끊기면 오버드라이브 게이지 리셋
- 오버드라이브 발동 지속시간 = 8초
- 오버드라이브 발동 중 이동속도/점프력 +15%, 코인 2배
- 코그는 줍는 즉시 해당 슬롯에 자동 장착
- 체크포인트 부활 시 최근 체크포인트 유지
- 완전 재시작 시 코그 상태 초기화
- Bloom Shift는 노드 위에 계속 있어도 매 프레임 토글되지 않는다.

---

## 4. 몬스터 핵심 규칙

### 코그마이트

- 순찰 구간 존재
- 플레이어 감지 시 120px/s 직선 돌진
- 돌진 방향은 감지 순간 고정
- 자연 상태에서는 순찰속도 60%
- 자연 상태에서는 돌진 비활성화
- 처치 시 3초 임시 발판

### 스포어 스프라이트

- 부유형 디버프 몹
- 근접 시 이동속도 저하
- 대시 무적 프레임을 관통해야 처치 가능
- 기계/자연 상태에 따라 감지 반경이 반대로 작동

### 프레셔 피스톤

- 충전 → 분사 → 휴식
- 자연 상태에서 완전 무력화
- 처치 대상이 아님

### 바이오 코일

- 웅크림 → 윈드업 → 도약 → 착지
- 기계 상태: 예고 없는 즉발 도약
- 자연 상태: 크고 느린 예고형 도약
- 착지 후 무방비 상태에서만 스톰프 가능
- 처치 시 3초 임시 발판
- 해당 발판의 다음 점프에 +30% 바운스

### 스팀 블로어

- 미니보스
- 넉백 전용 스팀 거스트
- 데미지형 화염 로브
- 기계 상태: 뚜껑 닫힘 / 스톰프 무효
- 자연 상태: 약점 노출
- HP 3
- 스톰프 3회로 처치
- 처치 시 본체보다 넓은 3초 임시 발판

---

## 5. Gear Socket 규칙

| 코그 | 슬롯 | 효과 |
|---|---|---|
| 스프링 | 발 | 점프 최고높이 +18%, 벽점프 수평거리 +25% |
| 마그넷 | 몸 | 반경 내 코인 자동 흡수 |
| 스팀부스트 | 발 | 대시 종료 후 추가 관성 활공 |
| 루트훅 코그 | 몸 | 그래플 사거리 +30%, 스윙 중 점프로 조기 이탈 + 강화 점프 |
| 미러 | 머리 | 이동 궤적 기록 → 사망 시 체크포인트 대신 기록 지점으로 부활 |

루트훅 코그 + 스프링 장착 시 캐논 점프 시너지가 존재한다.

범위 밖:
- 포토신 코그
- 오버그로스 블록
- 오버차지 임팩트
- 스팀 가디언

---

## 6. 테스트 원칙

우선순위는 다음과 같다.

1. 순수 로직 시뮬레이션
2. 실제 렌더링/런타임 테스트
3. 멀티터치 테스트
4. 봇 플레이스루
5. 전체 회귀 테스트

특히 물리 리팩터링은 변경 전후 결과가 동일한지 확인한다.

현재 개발로그 기준으로 실기기 Expo Go 테스트는 아직 수행되지 않았다.

---

## 7. 버그 수정 원칙

버그 수정 시 반드시 다음 형식으로 사고한다.

**재현 → 원인 → 최소 수정 → 관련 테스트 → 회귀 테스트**

기존에 해결된 대표적인 문제:

- 멀티터치 입력 상태 꼬임
- 스톰프 판정이 같은 프레임에서 덮어써지는 문제
- 스톰프 직후 생성된 발판의 충돌 방향 오판
- 체크포인트 없이 긴 레벨을 처음부터 다시 시작하는 문제

같은 문제가 다시 발생할 가능성이 있는 코드를 수정할 경우 개발로그에 기록한다.

---

## 8. 작업 결과 보고 형식

작업이 끝나면 다음을 짧게 보고한다.

### 변경
- 무엇을 수정했는가

### 이유
- 어떤 문제/요구사항 때문인가

### 검증
- 어떤 테스트를 실행했는가
- 성공/실패 여부

### 영향
- 기존 시스템에 영향을 주는가

### 남은 문제
- 아직 확인하지 못한 것이 있는가

---

## 9. 개발 우선순위

기본 우선순위:

### P0
- 실행 불가
- 크래시
- 핵심 물리/충돌 버그
- 기존 기능 회귀
- 실제 플레이를 방해하는 치명적 문제

### P1
- 레벨 진행성
- 조작감
- 몬스터/보스 공정성
- 체크포인트
- Bloom Shift 플레이 경험
- **기존 기능을 더 재미있게 다듬는 작업** (수치 밸런싱, 피드백/연출 보강, 메커닉 간 시너지) — 새 기능 착수보다 먼저 검토한다.

### P2
- 미구현 기획 요소
- 추가 시너지
- 연출
- 사운드
- 콘텐츠 확장

현재 알려진 미완성 항목은 `개발로그.md`를 기준으로 한다.

---

## 18. 자율 개발 원칙 (요약)

Claude는 요구된 기능만 구현하는 수동적 도구가 아니라 완성도를 높이는 개발 파트너로 행동한다. 단, 게임 방향성·핵심 규칙은 임의로 바꾸지 않는다.

- **착수 전 자문**: 기존 시스템과 충돌하는가? / 기존 수치·조작감을 바꿔야 하는가? / 플레이어가 존재 이유를 이해할 수 있는가? / 기존 기능 재사용이 가능한가? / 새 상태·변수가 정말 필요한가? / 다른 시스템에 버그를 유발할 수 있는가? / 테스트 가능한 형태인가? — 불필요한 복잡화를 피한다.
- **완료 = 기술적 작동 + 플레이 경험**: 조작감(입력 반응성, 점프/대시의 무게감, 벽점프 방향 안정성, 그래플 통제감)과 피드백(애니메이션·화면 흔들림·파티클·사운드·UI·수치·색상·적 상태 변화)을 함께 확인한다. 단, 화려함 자체가 목적인 효과는 추가하지 않는다.
- **사망 원인은 이해 가능해야 한다**: 공격 판정 비가시성, 예고 부족, Bloom Shift 급변, 예상과 다른 충돌, 입력 씹힘 등을 최우선 개선한다. "정상 동작"이라는 이유로 불공정하게 느껴지는 문제를 무시하지 않는다.
- **난이도 곡선**: 새 시스템은 학습 → 반복 → 응용 → 조합 → 숙련 순으로 도입한다(예: Bloom Shift = 안전지대 시연 → 발판 변화 → 이동 결합 → 적 결합 → 이동기 결합 → 시간압박/복합 패턴).
- **시너지 점검**: 최소 다음 조합을 확인한다 — Bloom Shift×몬스터/플랫폼/대시/벽점프/Root-Hook, Gear×이동/체크포인트/오버드라이브, 몬스터×임시 발판, 스톰프×생성 오브젝트. 신규 기능 단독 테스트만으로 완료 처리하지 않는다.
- **엣지 케이스**: 동시 다중 입력·다중 충돌, 공중/대시 중 상태 변경, 그래플 중 사망, 벽점프 직후 대시, 처치+발판 생성 동시 프레임, 체크포인트/코그 획득 직후 사망, 시프트 노드 위 계속 정지, 레벨 경계, 빠른 연속 입력, 리셋 직후 입력, 게임오버/승리 직전 입력.
- **성능(RN/Expo)**: 매 프레임 객체 생성, 불필요한 전체 레벨 재계산, 불필요한 React state 업데이트, 미정리 리스너/타이머/애니메이션, 과도한 렌더링, 몬스터 수 비례 계산 폭증을 피한다. 단, 성능을 이유로 게임플레이 로직을 복잡하게 만들지 않는다.
- **요청 밖 개선점 발견 시**: 발견 / 영향 / 제안 / 위험 / 우선순위(P0~P2) 형식으로 기록한다. 바로 구현하지 않아도 되며, 기존 작업과 충돌하지 않는 작은 수정이면 작업 범위에 포함할 수 있다.
- **"완료"의 기준**: 구현 + 정상 동작 확인 + 주요 엣지 케이스 확인 + 회귀 없음 + 플레이 경험상 문제 없음 + 문서 업데이트. 미검증 기능은 «구현 완료 / 검증 미완료»로 표기한다.
- **과도한 리팩터링 금지**: 필요성 설명 → 영향 범위 확인 → 변경 전 테스트 → 최소 범위 수정 → 변경 후 동일 테스트 순으로 진행하고, 기능 구현과 리팩터링을 한 번에 섞지 않는다.
- **전체를 본다**: 물리/렌더링/입력/레벨 데이터/몬스터 AI/UI/체크포인트/Gear/테스트/게임 상태 관리 중 영향받는 범위를 함께 확인한다.
- **세션 종료 전 자문**: 기존 기능을 깨지 않았는가 / 미검증 부분은 무엇인가 / 새 버그 가능성 / 플레이어 위화감 / 수치 무단 변경 여부 / 다음 개발자가 알아야 할 결정 / 개발로그에 남길 내용 — 답한 뒤 `개발로그.md`에 기록한다.
- **최우선 원칙**: 목표는 "기능이 많은 게임"이 아니라 조작이 재미있고 Bloom Shift와 이동 시스템이 서로 연결되며 플레이어가 다시 도전하고 싶어지는 플랫포머다. 새 기능보다 기존 완성도를 높이는 쪽이 더 가치 있다면 그쪽을 우선한다.

---

## 19. 비주얼/UI 리디자인 지침

> 진행 중인 Presentation Layer 작업 스펙. 이 문서에서 가장 최근에 추가된 섹션이므로, 다른 장·`docs/gearwood-*.md`와 충돌하면 **이 섹션이 기본값**이다.

**배경**: 핵심 시스템은 구현됐지만 시각적으로 프로토타입 수준이다 — 원형 버튼 위주 UI, 낮은 시각 밀도, 도형 위주 배경, 오브젝트 간 스타일 불일치, 약한 스팀펑크×자연 대비, HUD와 월드가 분리된 디자인 언어, 좁아 보이는 모바일 플레이 영역, 밋밋한 플랫폼/오브젝트, 약한 Bloom Shift 연출.

**최우선 원칙 — 게임플레이 불변**: 플레이어 물리·점프·대시·벽점프·벽슬라이드·Root-Hook·Bloom Shift 로직·몬스터 AI/충돌판정·Gear Socket·오버드라이브·체크포인트·레벨 진행·기존 밸런스 수치는 변경하지 않는다. 이번 작업은 Presentation Layer 개선이며, 로직 수정이 필요해 보이면 임의로 고치지 말고 18장의 "요청 밖 개선점 발견 시" 형식으로 먼저 기록한다.

**비주얼 방향**: Whimsical Steampunk × Nature Spirit × Premium Indie Platformer — 귀엽지만 유치하지 않게, 따뜻하지만 저렴하지 않게, 단순하지만 디테일 있게, 밝지만 난잡하지 않게, 기계×자연 대비는 명확하게. 기존 오브젝트에 장식만 덧붙이는 수준으로 끝내지 말고, 버튼/HUD/체력·코인·오버드라이브 UI/Bloom Shift 표시/플랫폼/배경/캐릭터/몬스터/코인/체크포인트/그래플 포인트/기어 아이콘/이펙트/시작·게임오버·승리 화면을 디자인 시스템 관점에서 재구성한다.

**아트 스타일 통일**: 일관된 외곽선(너무 얇거나 새까맣지 않게), 완전한 기하학 도형은 지양 — 흠집/나무결/볼트/기어/잎/덩굴/이음새/그림자로 손으로 만든 세계처럼 보이게 하되 과도한 디테일은 피한다.

**기계×자연 시각 대비** (가장 중요한 특징): 기계 = 황동/구리/기어/파이프/피스톤/증기/금속 플랫폼/볼트/기계적 움직임. 자연 = 잎/덩굴/꽃/나무/이끼/포자/자연광/유기적 형태. 전환 시 "색만 바뀜"이 아니라 "세계의 상태가 바뀜"으로 느껴져야 한다.

**Bloom Shift 연출** (시그니처 시스템, 단순 색상 전환 금지): 전환 순간 화면 효과·파티클·빛의 파동·기계부품 움직임·덩굴 성장·증기 소멸·먼지/꽃가루·플랫폼 형태 변화·주변 오브젝트 상태 변화를 조합해 짧고 명확하게 "세계가 바뀌었다"는 인상을 전달한다.

**배경(5레이어 패럴랙스)**: Sky → Far Background(먼 산/거대 기계 구조물/숲) → Mid Background(나무·덩굴·공장 구조물·증기 파이프) → Gameplay Background(근접 환경) → Foreground(플레이어 주변 작은 식물·기계부품·먼지). 이동 시 깊이감이 느껴지도록 parallax를 적용한다.

**플랫폼**: 기계 = 금속판/기어/볼트/파이프/마모된 표면, 자연 = 나무/덩굴/뿌리/잎/이끼. 충돌 영역은 기존 로직 그대로 — **그래픽과 실제 충돌 박스를 분리**한다.

**Sprout**: 필요 애니메이션 = Idle/Walk/Run/Jump/Fall/Land/Dash/Wall Slide/Wall Jump/Grapple/Damage/Death/Victory. 기존 절차적 애니메이션은 유지하고 부족한 부분만 보강 — 착지(작은 Squash & Stretch), 대시(잔상+속도선+파티클), 벽점프(먼지/잎/기계 파편), 그래플(Root-Hook이 살아있는 식물처럼 느껴지는 효과).

**몬스터 실루엣 차별화**: 코그마이트=기계적 순찰 로봇, 스포어 스프라이트=부유 생명체, 프레셔 피스톤=몬스터보다 거대한 환경 장치처럼, 바이오 코일=기계 스프링+생명체 결합, 스팀 블로어=미니보스답게 확실히 크고 위협적으로(등장만으로 "다른 싸움이 시작된다"는 느낌).

**UI**: 현재의 원형 버튼 중심 UI를 그대로 두지 않는다 — 금속 프레임/황동 테두리/나무·가죽 질감/작은 기어/유리관/덩굴 장식 등 세계관과 연결하되, UI가 게임 화면을 가리지 않게 한다.

**모바일 컨트롤**: 이동(◀▶을 하나의 컨트롤 모듈로), 점프(가장 명확하게 구분), 대시(속도감 표현), 그래플(Root-Hook 연상 아이콘). 입력 시 크기/밝기 변화·작은 파티클·눌림 애니메이션으로 피드백을 준다.

**HUD**: 코인(기어/황금 코인 아이콘+숫자), Bloom Shift(예: MECHANICAL ↕ NATURE로 상태 표시 + 전환 애니메이션), 체력(단순 하트 대신 세계관에 맞는 디자인 고려), Overdrive(가장 눈에 띄는 게이지 — 차오르는 과정 자체가 재미있게, 100% 도달 시 강한 시각적 피드백).

**화면 비율/레이아웃**: 게임 영역이 화면 중앙에 작게 들어가고 주변에 빈 공간이 큰 문제를 실제 렌더링 영역/aspect ratio 처리 방식부터 조사해서 해결한다(단순 강제 확대로 UI가 잘리는 방식 금지). 목표: 화면 효율적 사용, 게임플레이 영역 확보, 컨트롤 UI가 화면을 가리지 않음, 노치/상태바/안전 영역 대응, 가로 화면에서도 안정적 레이아웃.

**카메라**: 플레이어가 항상 중앙에 고정되지 않도록, 진행 방향 시야 확보, 점프/대시/보스 등장/Bloom Shift 시 절제된 카메라 반응(흔들림은 과하지 않게).

**이펙트** (중요한 순간에 집중, 모든 행동에 넣지 않음): 플레이어(점프/착지/대시/벽점프/그래플/피격/사망), 적(발견/공격 예고/피격/처치), 환경(Bloom Shift/체크포인트/기어 획득), 보스(등장/공격/약점 노출/피격/처치).

**사운드 부재 시 시각 피드백 강화**: 대시(약한 효과→잔상→속도선→짧은 확대), 스톰프(충돌→작은 파편→적 Squash→플레이어 Bounce), Bloom Shift(노드 활성화→파동→환경 변화→파티클).

**컬러 시스템**: Mechanical(황동/구리/어두운 금속), Nature(잎/나무/자연광), Player(배경과 구분되는 포인트 컬러), Danger(즉시 인식되는 위험색), Collectible(코인/Gear가 눈에 띄는 색). 색은 서로 충분한 대비를 가져야 하며 무작정 많이 쓰지 않는다.

**픽셀아트로 무조건 변경하지 않는다**: 현재 절차적 드로잉 + AI 생성 스프라이트 혼합 방식을 유지하며, 깔끔한 2D 일러스트 + 절차적 애니메이션 + 파티클 + 일관된 아트 디렉션을 목표로 한다. 새 에셋이 필요하면 기존 에셋과 어울리는 스타일을 먼저 정의한다. **`docs/gearwood-art-production-kit.md`의 "페인터리 픽셀아트" 전제보다 이 조항이 우선한다**(충돌 시 기본값 규칙, 0장 참고).

**구현 방식**: `Game Logic → Game State → Presentation/Rendering → Effects/Animation` 분리를 유지한다. 렌더링 변경 때문에 물리 코드를 수정하는 상황은 피한다.

**작업 순서(Phase)**: 1 화면 구조(영역 크기/aspect ratio/카메라/모바일 레이아웃/HUD 위치) → 2 UI(컨트롤/HUD/메뉴/게임오버/승리 화면) → 3 World Art(플랫폼/배경/환경 오브젝트/Bloom Shift 시각 효과) → 4 Characters(Sprout/몬스터/미니보스) → 5 Effects(파티클/애니메이션/대시/스톰프/그래플/체크포인트/Gear) → 6 최종 폴리싱(색상/간격/애니메이션 속도/시각적 위계/모바일 화면 테스트/성능 테스트). 한 번에 모두 바꾸지 않는다.

**절대 금지**: 게임플레이 대규모 변경, 기능 추가로 그래픽 문제 해결, 모든 요소를 무조건 크게 만들기, 파티클 남발, 모든 UI를 반짝이게, 색상 남발, 기존 에셋과 다른 아트 스타일 혼합, 매 프레임 고비용 효과, 모바일 버튼 과소 크기, UI의 게임플레이 영역 침범.

**최종 목표**: 결과물이 "프로토타입 게임을 보고 있다"가 아니라 "정식 출시를 준비 중인 인디 플랫포머를 보고 있다"는 인상을 줘야 한다. 기능 수를 늘리는 것이 아니라 완성도를 높이는 것이 목표이며, 작업 중 발견한 개선점은 개발로그에 기록하고 18장 형식으로 우선순위를 제안한다.

---

## 20. 최신 게임 방향 — Momentum Route (사용자 승인)

> 이 섹션은 2026-08-27 사용자 피드백에 따른 최신 기준이다. 기존 Bloom Shift 관련 장과 충돌하면 이 섹션을 우선한다. 전체 기획 명세는 `GAME_DIRECTION_V2.md`를 반드시 읽는다.

### 방향 전환

- 기존 전역 기계↔자연 상태 전환은 플레이어가 쓰임과 재미를 이해하기 어렵다는 사용자 피드백으로 **더 이상 확장하지 않는다**.
- 핵심 경험은 `이동 액션 연결 → FLOW 축적 → 명확한 Route Gate 선택 → 짧은 지름길 질주 → 보상`이다.
- 자연과 기계는 더 이상 전 레벨을 칠하는 모드가 아니라, 각각 안전한 `Vine Route`와 빠르고 보상이 큰 `Gear Route`의 성격이다.

### 역할

- **Codex**: 게임 기획·디자인 디렉터·UI/아트 구현·플레이 경험 검수. 규칙/구간/보상/UX의 최종 기획 기준을 제시한다.
- **Claude Code**: 메인 시스템 구현·레벨 데이터·충돌·상태·테스트·회귀 검증. Codex가 승인한 명세를 작은 단위로 구현한다.

### Claude의 첫 작업

- `GAME_DIRECTION_V2.md`의 **Claude의 첫 구현 지시: Route Gate Foundation**을 따른다.
- Area 1에 로컬 `Vine Route`와 `Gear Route`를 하나씩만 구현한다. 게이트는 약 6초 동안 해당 구간만 열고, 실패해도 안전한 기본 경로로 돌아와야 한다.
- 기존 이동 수치, 보스, Gear Socket은 Codex 승인 없이 변경하지 않는다.
- 구현 전·후 `AI_COLLAB.md`와 `개발로그.md`를 갱신하고, 모호한 결정은 REVIEW REQUESTS로 Codex에게 넘긴다.
