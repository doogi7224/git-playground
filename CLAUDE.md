# Gearwood — 현재 개발 기준

> 이 문서는 현재 구현과 다음 작업의 단일 기준이다. `개발로그.md`는 과거 이력, `docs/gearwood-*.md`는 초기 기획 아카이브이며 이 문서보다 우선하지 않는다.

## 1. 게임 방향

**한 방향으로 달리고, 뛰고, 대시하고, 그래플을 연결하는 밝은 숲 모험 액션 플랫포머.**

- Route Gate, 분기 길, 시간제 문, 환경 상태 선택, Bloom Shift는 사용하지 않는다.
- 적 처치 뒤 임시 발판/시체 플랫폼을 만들지 않는다.
- 높은 코인 줄은 숙련 보상이고, 실패해도 아래의 안전한 동선으로 이어진다.
- 자연과 고장 난 유물의 조합은 게임 상태가 아니라 배경·발판·적·아이템의 아트 방향이다.
- 재미의 중심은 “상태를 고르는 것”이 아니라 흐름을 끊지 않는 이동과 전투다.

## 2. 기술 구조

- React Native + Expo + TypeScript.
- 전용 게임 엔진/Matter.js가 아닌 자체 게임 루프와 가벼운 AABB 사각형 충돌을 사용한다.
- 물리·상태: `src/game/*` / 프레젠테이션: `src/components/*`, `src/screens/*` / 아트: `assets/*`.

## 3. 현재 핵심 플레이

- 이동, 점프, 벽점프/벽슬라이드, 대시, Root-Hook 그래플, 스톰프, 체크포인트, 보스전, FLOW/Overdrive.
- 기본 적: Brambleling(순찰/예고 후 돌진). 내부 타입명 `Enemy`는 유지한다.
- 수집품: Sunseed 코인.
- 현재 아트 방향: 밝은 산악 숲, 손그림 페인터리 질감, 초록 후드·붉은 스카프 탐험가.

## 4. 다음 기능: 유물 활과 적 다양화

### 유물 활

- 초반에 한 번 획득하면 영구 보유하며, 첫 획득 시 화살 6발을 받는다.
- 공격 입력으로 전방에 화살 한 발을 발사한다. 발사마다 1발을 소비하며, 최대 보유량은 12발이다.
- 화살은 탐험 보상 캐시의 `arrowBundle` 보상으로 4발씩 회복한다. 화살 0발일 때 공격 입력은 아무 일도 하지 않으며 HUD/버튼도 잠김 상태를 분명히 표시한다.
- 화살은 직선 이동, 적 1회 타격 또는 벽/화면 밖에서 제거한다.
- 짧은 쿨다운만 두며 탄약 인벤토리는 만들지 않는다.
- 활 처치와 스톰프 처치는 공존한다. 스톰프의 바운스·FLOW 보상은 보존한다.

### 적

1. Cogmite: 좌우 순찰/벽·절벽 반전, 예고 후 짧은 돌진.
2. Acorn Hopper: 타이머 기반의 짧은 도약. 추적 AI·경로 탐색 없음.
3. Root Turret: 제자리에서 일정 주기로 직선 씨앗 탄환 발사.
4. Chestnut Roller: 평소에는 느리게 걷다가, 예고 뒤 껍질 공으로 빠르게 구른다. 구르는 동안은 스톰프·화살에 무적이고, 정해진 시간이 지나면 비틀거리며 다시 걷는다. 연속 구르기에는 반드시 쿨다운을 둔다. 껍질을 차는 상호작용은 이번 범위에서 제외한다.

모든 적·투사체는 AABB와 타이머 기반으로 구현한다. 무거운 물리 엔진이나 복잡한 AI를 도입하지 않는다.

## 5. 디자인 기준

- Codex가 기획·아트 디렉션·UI 디자인과 프레젠테이션 구현을 맡는다.
- Claude Code가 물리, 상태, 레벨 데이터, 입력 연결, 회귀 테스트를 맡는다.
- 캐릭터·배경·적·아이템·HUD·컨트롤은 하나의 밝은 페인터리 숲 세계관으로 통일한다.
- 싸구려 AI 느낌의 평면 도형, 무작위 글로우, 이질적인 아이콘·화풍을 남기지 않는다.
- 디자인은 충돌 박스·물리 수치와 분리한다. 시각 크기만으로 게임 규칙을 바꾸지 않는다.

## 5.1 탐험 보상 오브젝트 (다음 구현)

- 이동만 반복하지 않도록, “발견 → 무엇이 나올지 기대 → 적절한 액션으로 개봉 → 즉시 이득” 루프를 넣는다.
- `Root Cache`: 대시로만 부술 수 있는 금 간 고목 상자.
- `Relic Pod`: 활을 얻은 뒤 화살로만 열 수 있는 봉인 꼬투리.
- 두 오브젝트는 겉모습만 보고 보상 종류를 알 수 없지만, **빈 보상은 없다.** 레벨 데이터에 고정한 `sunseedBurst`/`arrowBundle`/`lifeBloom`/`flowSpark` 중 하나가 반드시 나온다.
- 처음 구현에서는 인벤토리·랜덤 테이블·상점·껍질 차기 같은 확장 시스템을 만들지 않는다. 보상은 즉시 점수·화살·체력(최대치 제한)·Overdrive 게이지에 반영하고, 짧은 획득 연출로 결과를 보여준다.

## 6. 협업 규칙

- 먼저 `AI_COLLAB.md`의 ACTIVE/HANDOFF를 확인한다.
- 같은 파일을 동시에 수정하지 않는다. 새 파일/에셋은 소유권을 선언한 뒤 작업한다.
- `src/game/*`는 Claude 소유다. Codex는 사용자 명시 승인 또는 Claude의 요청 없이 수정하지 않는다.
- `src/components/*`, `src/screens/*`, `src/theme.ts`, `assets/*`는 Codex 소유다.
- 큰 구조 변경, 저장 형식 변경, 유료 서비스 도입, 핵심 규칙 삭제는 사용자 승인이 필요하다.

## 7. 완료와 검증

기능은 구현만으로 완료가 아니다.

1. `npx tsc --noEmit`
2. 변경 기능의 실제 게임 화면 확인
3. 시작 → 이동/핵심 행동 → 체크포인트/결과의 회귀 확인
4. 콘솔 오류·깨진 이미지·입력/화면비 문제 확인
5. `개발로그.md`와 `AI_COLLAB.md` 갱신

S0/S1 문제는 해결 또는 사용자 승인 없이 완료 처리하지 않는다.
