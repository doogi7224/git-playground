# Gearwood: The Broken Spring — 개발 로그

**기어우드: 파괴된 태엽** — React Native(Expo) 기반 사이드스크롤 플랫포머. 초기 "Pixel Hopper" 프로토타입을 스팀펑크 × 자연정령 콘셉트로 완전히 재구성하고, GDD/몬스터 기획서에 문서화된 시스템들을 순차적으로 구현했습니다.

- **PR**: https://github.com/doogi7224/git-playground/pull/1
- **플레이 가능한 빌드**: https://claude.ai/code/artifact/079b62fb-6b3b-46c3-857f-5fe468085cc7
- **브랜치**: `claude/game-app-development-thc8pw`

---

## 1. 핵심 플랫포머 기반

- 타일 충돌 기반 점프/이동 물리, 적 스톰프(밟기) 판정, 코인 수집, 목표 깃발, 5버튼 터치 컨트롤(◀ ▶ ▲ 대시 그래플), HUD, 시작/게임오버/승리 화면.
- **터치 컨트롤 재작성**: `nativeEvent.touches`를 직접 훑는 단일 최상위 responder로 교체. 실제 멀티터치 시뮬레이션(CDP `Input.dispatchTouchEvent`)으로 발견한 버그 3건 수정 — 버튼을 떼도 이동이 멈추지 않던 문제, 동시 입력 중 하나가 씹히던 문제, 방향키를 누른 채로는 점프가 한 번만 먹히던 문제.
- **스톰프 판정 수정**: 프레임 시작 시점의 위치·속도를 한 번만 캡처해서 판정하도록 변경 — 같은 프레임에 두 번째 적과 겹칠 때 판정이 덮어써지는 문제 제거.
- AI 생성 스프라이트(플레이어 "Sprout", 코그마이트)에 절차적 애니메이션(착지 스쿼시, 걷기 웨이브, 대시 잔상 등) 적용. 나머지 몬스터/오브젝트는 전부 절차적 드로잉.

## 2. 블룸 시프트 (Bloom Shift)

GDD의 시그니처 기믹: 시프트 노드를 밟으면 주변 지형이 기계↔자연 상태로 실시간 전환.
- 같은 좌표에 상태별로 다른 발판(회전 톱니 ↔ 휘어지는 덩굴)을 배치해 이동 리듬 자체가 바뀌도록 구현.
- 노드에 서 있는 동안 계속 토글되지 않도록 edge-triggered arming 처리.
- 레벨 전역에 4개 존(초반 튜토리얼 존, 중반 존, 후반 존, 2구역 존) 배치.

## 3. 몬스터 5종 (기획서 기준 전원 구현)

| 몬스터 | 구현 내용 |
|---|---|
| **코그마이트** (7체) | 지정 구간 순찰 + **플레이어 감지 시 120px/s 직선 돌진**(감지 순간 방향 고정, 매 프레임 재조준 안 함) + **블룸 시프트 자연 상태에서 순찰속도 60%로 감소 & 돌진 완전 비활성화**(온순화). 돌진 중 순찰 경계에 닿으면 돌진 종료 후 방향 전환(구덩이로 유인하는 원안은 적 낙하 물리가 필요해 범위 밖으로 스코프 다운). |
| **스포어 스프라이트** | 부유 디버프형 몹. 근접 시 이동속도 저하, 대시의 무적 프레임을 관통해야만 처치 가능(일반 접촉으로는 처치 불가). 기계/자연 상태에 따라 감지 반경이 반대로 작동(자연이 오히려 불리한 함정형). |
| **프레셔 피스톤** | 충전→분사→휴식 사이클의 환경 장애물. 자연 상태로 전환하면 완전히 무력화(처치 개념 자체가 없음). |
| **바이오 코일** | 웅크림→(윈드업)→도약→착지 패턴. 기계 상태는 예고 없는 즉발 도약, 자연 상태는 크고 느린 예고형 도약. 착지 후 무방비 상태에서만 스톰프로 처치 가능. |
| **스팀 블로어** (미니보스) | 독립적으로 순환하는 2가지 공격(넉백 전용 스팀 거스트 + 데미지형 화염 로브). 기계 상태에선 뚜껑이 닫혀 스톰프 무효, 자연 상태에서만 약점 노출(HP 3, 스톰프 3회로 처치). |

**처치 후 상호작용** (몬스터 기획서 반영): 코그마이트·바이오 코일·스팀 블로어 모두 처치 시 **3초짜리 임시 발판**을 남김. 바이오 코일의 발판은 추가로 **다음 점프에 +30% 바운스**를 부여(스프링 테마). 스팀 블로어의 잔해는 본체보다 넓은 발판.

> 이 기능을 만들다가 실제 충돌 버그를 하나 발견해 고쳤습니다: 스톰프로 튕겨 오르는 순간 그 자리에 발판이 새로 생기면, 수직 충돌 판정이 `vy` 부호만으로 "위에서 착지"와 "아래서 천장에 부딪힘"을 구분하다가 플레이어를 발판 반대쪽으로 잘못 스냅시키는 경우가 있었음 — 이동 전 위치를 함께 확인하도록 고쳐서 해결.

## 4. 이동 메커닉

- **대시**: 고정 속도 수평 돌진 + 짧은 무적 프레임. 공중에서 1회(착지 또는 벽 접촉 시 재충전), 지상은 무제한이지만 공용 쿨다운으로 연타 무적을 방지.
- **오버드라이브 게이지**: 스톰프(+15)·대시(+10)·벽점프(+10)로 채워지는 0~100 콤보 게이지. 1.5초 이상 콤보가 끊기면 리셋. 가득 차면 8초간 이동속도·점프력 +15%, 코인 2배.
- **루트훅(Root-Hook) 그래플 스윙**: 지정된 루트 포인트에만 반응하는 진자 물리 그래플. 스윙 정점에서 릴리즈하면 관성이 그대로 점프에 가산. 앵커-플레이어를 잇는 로프 라인 비주얼 렌더링 추가.
- **벽점프/벽슬라이드**: 공중에서 발판 옆면·구덩이 벽에 붙으면 낙하속도가 최고 낙하속도의 45%로 제한(GDD 수치). 그 상태에서 점프하면 반대 방향으로 밀려나며 위로 튀어오르고, 공중 대시 재충전 + 오버드라이브 게이지 적립까지 연동. 새 레벨 오브젝트 없이 기존 지형(구덩이 벽)을 그대로 재사용.
- **공중 제어력(Air Control)**: 공중에서의 좌우 이동속도를 지상의 85%로 제한(GDD 수치). 착지·벽 접촉 시 즉시 100%로 복귀.

## 5. 기어 소켓 (Gear Socket) 코그 시스템

스테이지 클리어형 장비창이 없는 단일 연속 레벨 구조에 맞춰, **줍는 즉시 해당 슬롯에 자동 장착**(같은 슬롯의 기존 코그는 교체)되는 형태로 각색. 체크포인트 부활에는 유지되고 완전 재시작 시에만 초기화.

| 코그 | 슬롯 | 효과 |
|---|---|---|
| 스프링 | 발 | 점프 최고높이 +18%, 벽점프 수평거리 +25% |
| 마그넷 | 몸 | 반경 내 코인 자동 흡수 |
| 스팀부스트 | 발 | 대시 종료 후 추가 관성 활공 |
| 루트훅 코그 | 몸 | 그래플 사거리 +30%, 스윙 중 점프로 조기 이탈 + 강화 점프 |
| 미러 | 머리 | 이동 궤적 기록 → 사망 시 체크포인트 대신 그 지점으로 부활 |

**캐논 점프 시너지**(루트훅 코그 + 스프링): 스윙 중 조기 점프 이탈 시 더 강력한 점프 부여.

*범위 밖*: 포토신 코그(오버그로스 블록 필요, 미구현 오브젝트라 불가)와 그에 의존하는 시너지 2종(오버차지 임팩트, 스팀 가디언).

## 6. 레벨 & 진행 시스템

- **2구역 확장**: `worldWidth` 3300 → 4700(플레이 시간 약 2배). 적 2체, 4번째 블룸 시프트 존, 3번째 스포어 스프라이트, 2번째 프레셔 피스톤 추가.
- **체크포인트**: 레벨이 길어지면서 사망 시 항상 절대 시작점으로 돌아가던 문제를 발견 → 레벨 전역에 5개 체크포인트(점등/소등 마커 포함) 추가, 가장 최근 지점에서 부활.

## 7. 검증 방법론

- **순수 로직 시뮬레이션**(Node/tsx로 `physics.ts`를 직접 호출)을 1차 검증 수단으로 사용 — React/RN/브라우저 변수를 완전히 배제해 가장 신뢰도가 높음. 기능별 전용 테스트 스크립트를 계속 축적하며 매 변경마다 전체 재실행.
- **라이브 Metro 개발 서버 + Playwright**를 2차 검증으로 사용 — 실제 렌더링, 런타임 에러 유무, 멀티터치 컨트롤 확인.
- **봇 플레이스루 감사**: 레벨 전체를 몇 가지 다른 수준의 "봇"으로 자동 플레이시켜 완주 가능성/공정성을 검증. 여러 차례의 "버그처럼 보이는" 사망을 진단 로깅으로 근본 원인 추적한 결과, 실제 레벨 결함은 하나도 없었고 전부 봇 AI의 한계(다리 발판을 모르는 순진한 낙사 예측, 반응 판정 임계값이 2px 부족, 이미 공중에 있을 때 새 위협에 대응 못 함 등)로 판명 — 유일하게 실제로 유효했던 발견은 "체크포인트 없이 절대 시작점으로 부활"이었고 이는 위 체크포인트 시스템으로 해결.
- 코드 변경 전/후 물리 동작이 동일한지 확인해야 하는 리팩터(`applyHit()` 중복 제거 등)는 전체 회귀 스위트를 앞뒤로 돌려 바이트 단위로 결과가 같은지 비교.

## 8. 남은 과제 (알고 있는 범위 밖 항목)

- **포토신 코그** 및 그에 의존하는 시너지 2종 — 오버그로스 블록이라는, 이 프로토타입에 존재하지 않는 오브젝트가 필요.
- **코그마이트의 구덩이 유인 전략** — 기획서 원안은 돌진 중 낭떠러지로 떨어뜨릴 수 있어야 하는데, 적에게 낙하 물리 자체가 없어 순찰 경계 안으로 스코프 다운.
- **루트훅 펌프**가 고정 임펄스라 진폭을 정교하게 쌓는 형태는 아님.
- **스팀 블로어/피스톤/바이오 코일**의 투사체는 실제로 이동하는 오브젝트가 아니라 타이밍 기반 고정 히트박스 존. 화상 지속 데미지는 미구현(목숨 기반 데미지 모델과 틱 데미지가 구조적으로 잘 안 맞아 계획에서도 제외).
- **실기기(Expo Go) 테스트** 미실시 — 이 환경에서는 Expo 웹 export + Playwright로만 검증 가능.

## 9. 커밋 이력 (이번 세션, 시간순)

1. Bloom Shift 4존 확장
2. 스포어 스프라이트 몬스터
3. 프레셔 피스톤
4. 바이오 코일
5. 스팀 블로어 미니보스
6. 대시 메커닉
7. 오버드라이브 게이지
8. 루트훅 그래플 스윙
9. 2구역 레벨 확장
10. 체크포인트 시스템 + README/시작화면 갱신
11. `applyHit()` 중복 제거 리팩터
12. 기어 소켓 코그 시스템 (5종 + 캐논 점프)
13. 벽점프/벽슬라이드
14. 공중 제어력 + 루트훅 로프 비주얼
15. 처치 후 임시 발판 (+ 충돌 버그 수정)
16. 코그마이트 감지 돌진 + 블룸 시프트 순화
