# Gearwood: The Broken Spring — 개발 로그

## 1. 프로젝트 개요

**기어우드: 파괴된 태엽**

React Native(Expo) 기반 사이드스크롤 플랫포머.
초기 Pixel Hopper 프로토타입을 스팀펑크 × 자연정령 콘셉트로 재구성했다.

- PR: https://github.com/doogi7224/git-playground/pull/1
- 플레이 가능한 빌드: https://claude.ai/code/artifact/079b62fb-6b3b-46c3-857f-5fe468085cc7
- 브랜치: `claude/game-app-development-thc8pw`

---

# 2. 현재 상태 요약

## 구현 완료

- [x] 기본 플랫포머
- [x] 멀티터치 입력
- [x] Bloom Shift 4존
- [x] 몬스터 5종
- [x] 대시
- [x] 오버드라이브
- [x] Root-Hook 그래플
- [x] 벽점프/벽슬라이드
- [x] 공중 제어력
- [x] Gear Socket 코그 5종
- [x] 캐논 점프 시너지
- [x] 2구역 확장
- [x] 체크포인트 5개
- [x] 처치 후 임시 발판
- [x] 관련 충돌 버그 수정

## 미완성 / 범위 밖

- [ ] 포토신 코그
- [ ] 오버그로스 블록
- [ ] 오버차지 임팩트
- [ ] 스팀 가디언
- [ ] 코그마이트 구덩이 유인 전략
- [ ] 정교한 Root-Hook 펌프 진폭
- [ ] 실제 이동 투사체
- [ ] 화상 지속 데미지
- [ ] 실기기 Expo Go 테스트

---

# 3. 핵심 게임플레이 루프

기본적인 플레이 흐름:

이동
→ 코인/몬스터/지형을 통과
→ 스톰프·대시·벽점프로 오버드라이브 축적
→ Bloom Shift로 기계/자연 상태 전환
→ 변화한 지형과 몬스터 특성에 대응
→ 코그 획득 및 이동 능력 강화
→ 체크포인트 확보
→ 미니보스
→ 다음 구역 진행

이 구조를 변경할 경우 기존 시스템 간의 역할이 겹치지 않는지 확인한다.

---

# 4. 기본 플랫포머

기능 목록은 `CLAUDE.md` 2장(현재 구현된 핵심 시스템) 참고. 아래는 규칙표에 없는 구현 이력만 기록한다.

### 멀티터치

`nativeEvent.touches`를 직접 훑는 단일 최상위 responder로 교체.

실제 멀티터치 시뮬레이션(CDP `Input.dispatchTouchEvent`)을 통해 발견한 문제:
- 버튼을 떼도 이동이 멈추지 않음
- 동시 입력 중 하나가 씹힘
- 방향키를 누른 채 점프하면 점프가 한 번만 입력됨

수정 완료.

### 스톰프

프레임 시작 시점의 위치와 속도를 한 번 캡처해서 판정.
같은 프레임에 두 번째 적과 겹칠 때 판정이 덮어써지는 문제 해결.

---

# 5. Bloom Shift

GDD의 핵심 기믹. 전환 규칙과 불변 조건(edge-triggered arming, 노드 위 정지 시 매 프레임 토글 금지 등)은 `CLAUDE.md` 2장/3장 참고.

---

# 6. 몬스터

5종의 상세 행동 규칙(속도, 처치 조건, 상태별 변화 등)은 `CLAUDE.md` 4장(몬스터 핵심 규칙) 참고. 아래는 규칙표에 없는 배치/스코프 결정만 기록한다.

- 코그마이트: 레벨에 7체 배치.
- 코그마이트 구덩이 유인 전략: 적 낙하 물리가 없어 범위 밖으로 스코프 다운.

---

# 7. 이동 메커닉

수치(공중 대시 1회, 벽슬라이드 낙하속도 45%, 오버드라이브 게이지 등)는 `CLAUDE.md` 3장(불변 게임 규칙) 참고. 아래는 수치표에 없는 구현 방식만 기록한다.

## Root-Hook

- 지정 루트 포인트에만 반응(자유 그래플 아님)
- 진자 물리 기반 스윙
- 정점에서 릴리즈 시 관성이 그대로 점프에 가산
- 앵커와 플레이어를 잇는 로프를 렌더링

## 쿨다운 설계

- 지상 대시는 무제한이지만 공용 쿨다운으로 연속 무적 프레임 남용 방지
- 공중 대시는 착지 또는 벽 접촉 시 재충전

---

# 8. Gear Socket

코그별 슬롯/효과 표는 `CLAUDE.md` 5장(Gear Socket 규칙) 참고. 아래는 표에 없는 설계 배경만 기록한다.

단일 연속 레벨 구조(스테이지 클리어형 장비창 없음)이므로 줍는 즉시 해당 슬롯에 자동 장착하는 방식을 택했다. 같은 슬롯의 기존 코그는 교체된다.

---

# 9. 레벨 & 진행

- `worldWidth`: 3300 → 4700
- 플레이 시간 약 2배
- 2구역 확장
- 적 2체 추가
- 4번째 Bloom Shift 존
- 3번째 스포어 스프라이트
- 2번째 프레셔 피스톤
- 체크포인트 총 5개

긴 레벨에서 사망 시 시작점으로 돌아가는 문제를 해결하기 위해 최근 체크포인트 부활 시스템을 추가했다.

---

# 10. 처치 후 상호작용

몬스터별 임시 발판 보너스(코그마이트/바이오 코일/스팀 블로어)는 `CLAUDE.md` 4장 참고.

### 해결된 충돌 버그

스톰프로 튕겨 오르는 순간 발판이 생성될 경우,
수직 속도 `vy`만 사용하면 착지/천장 충돌을 잘못 구분할 수 있었다.

이동 전 위치를 함께 확인하도록 수정하여 해결.

---

# 11. 검증 방법론

## 1차: 순수 로직 시뮬레이션

Node/tsx로 `physics.ts` 직접 호출.

React/RN/브라우저 변수를 배제해 물리 로직을 우선 검증.

## 2차: 라이브 Metro + Playwright

- 실제 렌더링
- 런타임 오류
- 멀티터치 컨트롤

## 3차: 봇 플레이스루

여러 수준의 봇으로 전체 레벨을 자동 플레이.

발견했던 문제 중 대부분은 실제 레벨 결함이 아니라 봇 한계였다.

예:
- 발판 구조를 모르는 낙사 예측
- 2px 부족한 반응 임계값
- 공중에서 새 위협에 대응하지 못함

실제 유효한 레벨 문제:
- 체크포인트 부재
→ 5개 체크포인트로 해결

## 회귀 검증

물리 리팩터링 시 변경 전/후 결과가 동일한지 비교한다.

`applyHit()` 중복 제거 등이 대표 사례.

---

# 12. 실기기 테스트 상태

현재 개발로그 기준:

**Expo Go 실기기 테스트 미실시**

현재는 Expo 웹 export + Playwright 중심으로 검증했다.

따라서 실제 터치감/성능/화면 비율/기기별 입력 문제는 아직 검증되지 않은 상태다.

---

# 13. 알려진 제한사항

1. 포토신 코그 미구현
2. 오버그로스 블록 미구현
3. 관련 시너지 미구현
4. 코그마이트 낙하 유인 전략 미구현
5. Root-Hook 펌프 진폭 정교화 미구현
6. 투사체는 실제 이동 오브젝트가 아닌 고정 히트박스
7. 화상 지속 데미지 미구현
8. 실기기 테스트 미실시

이 항목들은 현재 상태에서 임의로 '버그'로 판단하지 않는다.

---

# 14. 다음 개발 우선순위

## P0 — 반드시 먼저 확인

- [ ] 실행 불가/크래시
- [ ] 핵심 물리/충돌 버그
- [ ] 기존 기능 회귀
- [ ] 실제 플레이를 방해하는 치명적 문제
- [ ] Expo Go 실기기 테스트

## P1 — 완성도

- [ ] 레벨 진행성 검증
- [ ] 조작감 검증
- [ ] 몬스터/미니보스 공정성
- [ ] 체크포인트 간 거리
- [ ] Bloom Shift의 이해 가능성
- [ ] 코그 획득이 실제 플레이 방식에 미치는 영향

## P2 — 확장

- [ ] 포토신 코그
- [ ] 오버그로스 블록
- [ ] 오버차지 임팩트
- [ ] 스팀 가디언
- [ ] 추가 연출/사운드
- [ ] 추가 콘텐츠

---

# 15. 플레이 테스트 기준

기능 구현 여부와 별도로 다음을 확인한다.

- 첫 30초 안에 기본 조작을 이해할 수 있는가?
- Bloom Shift가 등장했을 때 목적을 이해할 수 있는가?
- 죽었을 때 플레이어가 원인을 납득할 수 있는가?
- 대시/벽점프/그래플이 각각 사용할 이유가 있는가?
- 코그 획득 후 플레이 방식이 달라지는가?
- 미니보스가 일반 몬스터와 다른 대응을 요구하는가?
- 체크포인트 사이의 진행 부담이 과도하지 않은가?

---

# 16. 커밋 이력

이번 세션에서 구현된 순서:

1. Bloom Shift 4존 확장
2. 스포어 스프라이트 몬스터
3. 프레셔 피스톤
4. 바이오 코일
5. 스팀 블로어 미니보스
6. 대시 메커닉
7. 오버드라이브 게이지
8. Root-Hook 그래플 스윙
9. 2구역 레벨 확장
10. 체크포인트 시스템 + README/시작화면 갱신
11. `applyHit()` 중복 제거 리팩터
12. Gear Socket 코그 시스템
13. 벽점프/벽슬라이드
14. 공중 제어력 + Root-Hook 로프 비주얼
15. 처치 후 임시 발판 + 충돌 버그 수정
16. 코그마이트 감지 돌진 + Bloom Shift 순화

---

# 17. 세션별 추가 기록

새로운 개발 세션마다 아래 형식으로 추가한다.

## 2026-08-26 — CLAUDE.md 개발 가이드 및 개발로그 정식화

### 목표
- 다음 세션이 매번 컨텍스트를 처음부터 재구성하지 않도록, 프로젝트 전용 Claude 가이드와 이어 쓸 개발로그를 정식 문서로 저장한다.

### 변경
- `CLAUDE.md`에 프로젝트 목적, 작업 시작 전 필수 절차, 구현 현황, 불변 규칙, 몬스터/기어 소켓 규칙, 테스트·버그 수정 원칙, 보고 형식, 개발 우선순위(P0/P1/P2)를 정리해서 채워 넣음(기존에는 `@AGENTS.md` 참조 한 줄뿐이었음 — 해당 참조는 그대로 유지).
- 필수 절차와 P1 우선순위에 신규 규칙 추가: **새로운 기능을 추가하기 전에 기존 기능을 더 재미있게 만들 수 있는지 먼저 검토한다.**
- `개발로그.md`를 신설해 이번 세션까지의 구현 현황·검증 방법론·알려진 제한사항·커밋 이력을 기록.

### 버그
- 없음(문서 작업만 진행).

### 해결
- 해당 없음.

### 테스트
- 해당 없음(코드 변경 없이 문서만 추가).

### 결과
- `CLAUDE.md`와 `개발로그.md`가 서로 참조하는 구조가 실제로 성립하도록 완성. 커밋 및 푸시 완료.

### 다음 작업
- 신규 규칙에 따라, 다음 세션은 새 기능 착수 전 기존 기능(조작감·수치 밸런싱·연출) 개선 여지를 먼저 검토할 것.

### 결정 사항
- 개발 우선순위상 "기존 기능을 더 재미있게 다듬는 작업"을 새 기능 착수보다 먼저 검토하기로 확정.

## 2026-08-26 (2) — 비주얼/UI 전면 리디자인 작업 지침 등록

### 목표
- 다음 세션(들)이 비주얼/UI 리디자인 작업을 시작할 때 참고할 상세 스펙을 `CLAUDE.md`에 정식으로 등록해둔다. 아직 실제 코드 변경은 하지 않은 단계다.

### 변경
- `CLAUDE.md`에 "19. 비주얼/UI 전면 리디자인 작업 지침" 섹션을 신설.
  - 배경/문제 정의, 최우선 원칙(게임플레이 불변 시스템 목록), 비주얼 방향(Whimsical Steampunk × Nature Spirit × Premium Indie Platformer), 아트 스타일 통일 규칙, 기계/자연 대비 규칙, Bloom Shift 연출 강화 방향, 배경 5레이어 파랄락스 구조, 플랫폼 그래픽/충돌박스 분리 원칙, Sprout 애니메이션 목록, 몬스터별 실루엣 차별화, UI·모바일 컨트롤·HUD 리디자인 방향, 화면 비율/레이아웃/카메라 개선 방향, 이펙트 우선순위, 컬러 시스템, "픽셀아트로 전면 전환하지 않는다"는 제약, Presentation Layer 분리 구현 방식, Phase 1~6 작업 순서, 금지 목록, 최종 목표를 모두 포함.

### 버그
- 없음(문서 작업만 진행, 코드 변경 없음).

### 해결
- 해당 없음.

### 테스트
- 해당 없음(코드 변경 없이 문서만 추가).

### 결과
- 리디자인 작업의 범위·원칙·순서가 `CLAUDE.md` 19번 섹션에 확정. 이후 세션은 이 섹션을 기준으로 Phase 1(화면 구조)부터 순차 진행한다.

### 다음 작업
- Phase 1 착수 전, 실제 코드에서 게임 렌더링 영역/aspect ratio 처리 방식(`src/screens/GameScreen.tsx`, `App.tsx` 등)을 먼저 조사해 "화면 가운데 작게 들어가는" 문제의 원인을 파악할 것.
- 리디자인 도중 게임플레이 로직 수정이 필요하다고 판단되는 지점이 발견되면 임의로 고치지 말고 18.8 형식으로 먼저 기록할 것.

### 결정 사항
- 이번 리디자인은 새 기능 추가가 아니라 Presentation Layer 개선으로 범위를 한정하며, Phase 1(화면 구조) → Phase 6(최종 폴리싱) 순서를 따른다.

## 2026-08-26 (3) — .md 문서 최적화 (중복 제거·역할 분리)

### 목표
- `CLAUDE.md`와 `개발로그.md`가 몬스터 규칙·Gear Socket 표·이동 수치를 사실상 그대로 중복 기술하고 있던 문제, 그리고 `docs/` 원안 기획서가 실제 구현·최신 리디자인 방향과 어긋나는 부분(픽셀아트 전제, 미구현 기어/보스 등)을 정리한다. 코드 변경 없음.

### 변경
- `개발로그.md` 4~8장, 10장에서 `CLAUDE.md`와 완전히 겹치는 수치/규칙 나열을 제거하고 해당 장을 참조하도록 교체. 각 장에 없던 고유 정보(코그마이트 7체 배치, 구덩이 유인 전략 스코프다운 결정, Root-Hook 진자 물리 구현 방식, 스톰프-발판 충돌 버그 수정 내역 등)는 그대로 유지.
- `CLAUDE.md` 0장에 "문서 역할 분담" 안내 추가: 수치·규칙의 단일 소스는 `CLAUDE.md`, 구현 이력·의사결정은 `개발로그.md`, `docs/gearwood-*.md`는 초기 창작 기획 원안이며 충돌 시 앞의 두 문서가 우선한다는 원칙을 명문화.
- `docs/gearwood-design-doc.md`, `docs/gearwood-art-production-kit.md`, `docs/gearwood-monster-design.md` 상단에 "현재 구현과의 차이 안내" 블록 추가.
  - 기획서: 실제 구현 범위(5종 코그, 캐논 점프 시너지만, 단일 연속 레벨, 스팀 블로어 미니보스)가 원안(6종 코그, 3종 시너지, 6개 월드, TerraCog 보스)보다 좁다는 점을 명시.
  - 아트 프로덕션 키트: 원안의 "페인터리 픽셀아트" 전제가 실제 구현(절차적 드로잉 + AI 스프라이트 혼합) 및 진행 중인 리디자인 지침(`CLAUDE.md` 19.19, 픽셀아트 강제 전환 금지)과 다르다는 점을 명시하고, 픽셀아트 여부는 19장을 따르도록 안내.
  - 몬스터 디자인 문서: 수치·판정의 단일 소스가 `CLAUDE.md` 4장임을 명시하고, 이 문서는 비주얼/서사 보충 자료로 역할을 한정.

### 버그
- 없음(문서 작업만 진행, 코드 변경 없음).

### 해결
- `CLAUDE.md`와 `개발로그.md`의 수치 중복으로 인한 향후 드리프트(한쪽만 수정되어 값이 어긋날) 위험 제거.
- `docs/` 원안 기획서의 픽셀아트 전제와 신규 리디자인 지침(19.19)의 정면 충돌을 명시적으로 해소.

### 테스트
- 해당 없음(코드 변경 없이 문서만 정리).

### 영향
- 게임플레이/코드에 영향 없음. 향후 세션이 규칙을 찾을 때 `CLAUDE.md`를 먼저 참고하도록 유도되어 컨텍스트 낭비가 줄어든다.

### 남은 문제
- `docs/gearwood-monster-design.md`의 "골든 코그 파츠 드롭" 등 일부 서술이 실제 구현에 존재하는지는 이번 작업에서 검증하지 않았다. 실제 코드(`src/game/physics.ts`, `src/game/level.ts`)와 대조가 필요하면 별도로 확인할 것.

### 결정 사항
- 문서 중복을 원천적으로 막기 위해 "수치·규칙은 `CLAUDE.md`에만 쓰고 `개발로그.md`·`docs/`는 그 장을 참조한다"는 원칙을 `CLAUDE.md` 0장에 고정한다.

## 2026-08-26 (4) — 토큰 사용량 절감 + 충돌 시 기본값 규칙 도입

### 목표
- `CLAUDE.md`가 매 세션 그대로 로드되는 문서인데 811줄까지 불어나 토큰 사용량 부담이 크다는 피드백을 받아, 규칙 내용은 그대로 두고 표현만 압축한다. 동시에 "문서/지침이 서로 충돌하면 어느 쪽을 따를지"를 명시적으로 정한다.

### 변경
- `CLAUDE.md` 18장(자율 개발 원칙, 13개 하위 섹션·약 270줄)과 19장(비주얼/UI 리디자인 지침, 23개 하위 섹션·약 270줄)을 각각 하위 헤더 없는 압축 불릿 목록으로 재작성. 규칙·수치·목록 항목은 하나도 빠짐없이 유지하고, 반복되는 설명 문장과 예시 서술만 제거. `CLAUDE.md` 전체가 811줄 → 341줄로 줄었다(약 58% 감소).
- `CLAUDE.md` 0장에 "충돌 시 기본값 규칙" 추가: 문서·지침이 서로 충돌하면 **더 최근에 추가된 내용을 기본값**으로 따른다(문서 내에서는 뒤 번호 섹션이 최신, `CLAUDE.md`/`개발로그.md`는 항상 `docs/gearwood-*.md` 원안보다 우선).
- `docs/gearwood-art-production-kit.md`의 "현재 구현과의 차이 안내"를 "대체됨(충돌 시 기본값 규칙 적용)"으로 더 단정적으로 수정 — "픽셀아트 여부는 `CLAUDE.md` 19장이 이 문서를 대체한다"고 명시.

### 버그
- 없음(문서 작업만 진행, 코드 변경 없음).

### 해결
- `CLAUDE.md`의 세션당 고정 로딩 비용(토큰)을 절반 이하로 줄임.
- "여러 문서에 안내가 있는데 뭘 따라야 하나"라는 모호함을 "최근 추가분이 기본값"이라는 한 문장 규칙으로 해소.

### 테스트
- 해당 없음(코드 변경 없이 문서만 정리). 18장/19장 압축본에 원본 대비 누락된 규칙 항목이 없는지 항목 단위로 대조 확인.

### 영향
- 게임플레이/코드에 영향 없음.

### 남은 문제
- 이전 세션 기록(위 2026-08-26 (2)/(3) 항목)의 "18.8", "19.19" 같은 하위 섹션 번호 언급은 당시 문서 구조를 그대로 남긴 과거 기록이며, 현재 `CLAUDE.md`는 18장/19장을 하위 번호 없는 압축 목록으로 재구성했다. 과거 로그 문구는 역사적 사실 기록이므로 수정하지 않았다.

### 결정 사항
- 앞으로 `CLAUDE.md`에 새 장을 추가할 때도 하위 섹션을 잘게 쪼개 서술형으로 늘어놓기보다, 처음부터 압축 불릿 형태를 기본값으로 삼는다.

## 2026-08-26 (5) — 리디자인 Phase 1: 화면 구조/aspect ratio 수정

### 목표
- 사용자가 야간 자율 진행을 요청. `CLAUDE.md` 19장 지침에 따라 Phase 1(화면 구조)부터 순서대로 진행. 19장이 지목한 "게임 영역이 화면 가운데 작게 들어가는 문제"의 실제 원인을 코드에서 확인하고 수정.

### 원인
- `src/screens/GameScreen.tsx`에서 게임 스테이지를 `{ width: windowWidth, height: VIEWPORT_HEIGHT(=220px 고정) }`로 렌더링하고, 이를 감싸는 `outer` 뷰가 `flex:1, alignItems:center, justifyContent:center`로 화면 정중앙에 배치하고 있었다. `VIEWPORT_HEIGHT`는 `src/game/level.ts`의 `GROUND_Y = VIEWPORT_HEIGHT - 40`을 비롯해 모든 발판/적 Y좌표가 이 값을 기준으로 작성된 물리 좌표계이므로, 이 상수 자체를 키우는 것은 레벨 데이터를 다시 배치하는 것과 같아 금지된 변경(불변 게임 규칙 위반)이다. 실제 화면(보통 세로 700~900dp)에서는 220px 높이의 스테이지가 화면 중앙의 작은 띠로 보이고 위아래로 거대한 여백이 남는 구조였다.

### 변경
- `GameScreen.tsx`에 Presentation-only 스케일링 도입: `VIEWPORT_HEIGHT` 논리 좌표계는 그대로 두고, HUD/컨트롤을 위해 위/아래 여백(`HUD_RESERVED_TOP=96`, `CONTROLS_RESERVED_BOTTOM=130`, 이 파일에만 있는 지역 상수, `game/constants.ts`에는 추가하지 않음)을 제외한 실제 사용 가능 높이에 맞춰 `stageScale = availableStageHeight / VIEWPORT_HEIGHT`를 계산(0.6~3배로 클램프).
- 카메라에 넘기는 `viewportWidth`를 `windowWidth`가 아니라 `windowWidth / stageScale`로 바꿔, 화면 비율에 맞게 가로로 보이는 월드 폭이 조정되도록 함(`computeCameraX`, `physics.ts` 자체는 수정하지 않음 — 넘기는 인자 값만 다름).
- 스테이지를 `stageClip`(HUD~컨트롤 사이 실제 화면 영역, `overflow:hidden`)로 감싸고, 그 안의 `stage`는 원래 논리 크기(`viewportWidth × VIEWPORT_HEIGHT`)로 배치한 뒤 `transform:[{scale: stageScale}]` + `transformOrigin:'top left'`로 좌상단 기준 확대해 `stageClip`을 정확히 채우도록 함. 월드 좌표·충돌 로직은 전혀 건드리지 않았다(그래픽과 좌표계 분리, `CLAUDE.md` 19장 원칙).

### 검증
- `npx tsc --noEmit`: 통과(`transformOrigin` 스타일 프로퍼티가 현재 RN 버전 타입에 존재함을 확인).
- `npx expo start --web`로 실제 부팅 후 Playwright(`playwright-core`, `--no-save`로 임시 설치 — package.json 변경 없음)로 스크린샷 검증:
  - 세로 390×844(모바일 표준 비율): 게임 세계가 화면 대부분을 채우고, Sprout·발판·구름·태양·HUD·컨트롤이 모두 올바른 비율로 렌더링됨. 수정 전에는 세로 220px 띠만 중앙에 떠 있었을 상황.
  - 가로 844×390(랜드스케이프): 더 넓은 가로 시야로 발판·코인·적·깃발이 자연스럽게 렌더링, HUD/컨트롤이 게임 영역을 가리지 않음.
  - 두 경우 모두 브라우저 콘솔에 `pageerror` 없음.
- 실제 코그마이트/스톰프/대시 등 물리 동작 자체는 이번 변경으로 건드리지 않았으므로 별도 재검증하지 않음(좌표계·물리 로직 무변경).

### 영향
- 게임플레이 로직/수치 영향 없음(물리·레벨 데이터 무변경). 시각적으로만 스테이지가 화면을 채우도록 바뀜.
- 기존에 `windowWidth`를 그대로 카메라에 넘기던 방식과 비교해, 이제는 기기 화면 비율에 따라 한 번에 보이는 월드 폭이 달라진다(세로로 좁고 긴 화면일수록 가로 시야가 좁아짐) — 스테이지가 화면을 꽉 채우기 위한 의도된 트레이드오프.

### 남은 문제
- 노치/상태바 세이프 에어리어는 `react-native-safe-area-context` 같은 별도 패키지 없이 고정 여백(`HUD_RESERVED_TOP`/`CONTROLS_RESERVED_BOTTOM`)으로만 근사했다. 실기기 테스트가 아직 없는 상태이므로 실제 노치 기기에서 HUD가 상태바에 가려질 가능성은 남아 있음 — 필요하면 이후 세션에서 해당 패키지 도입을 검토할 것(새 네이티브 의존성 추가이므로 임의로 넣지 않고 별도 판단 필요).
- `stageScale` 클램프(0.6~3)의 상한/하한은 임의로 정한 값이며, 실기기에서 특이한 화면 비율(예: 매우 넓은 태블릿)로 테스트되지 않았다.

### 결정 사항
- Phase 1은 "화면 구조" 단독으로 완료 처리(«구현 완료, 웹 브라우저 검증 완료 / 실기기 미검증»). 카메라 시야 상수(`HUD_RESERVED_TOP`, `CONTROLS_RESERVED_BOTTOM`, 스케일 클램프)는 `GameScreen.tsx` 로컬 상수로 유지해 `game/constants.ts`(게임플레이 수치 전용)와 분리한다.

## 2026-08-26 (6) — 리디자인 Phase 2: HUD·모바일 컨트롤 리디자인

### 목표
- Phase 1에 이어 `CLAUDE.md` 19장의 Phase 2(UI)를 진행. 입력 판정 로직은 전혀 건드리지 않고 순수 스타일/장식 레이어만 교체.

### 변경
- `src/components/Hud.tsx` 전면 재작성(로직 없음, 표현만):
  - 코인 뱃지: 단색 원 → 하이라이트가 있는 코인 아이콘.
  - Bloom Shift 뱃지: 상태가 바뀔 때마다 `Animated.Value` 하나로 구동되는 짧은 스케일+글로우 펄스 추가(리렌더 없이 native driver로만 애니메이션).
  - 체력: 하트 텍스트(`♥♥♥`) → `STARTING_LIVES`만큼 항상 표시되는 다이아몬드형 "생명 핵" 슬롯(잃은 목숨은 빈 슬롯으로 남아 잔여/소모를 함께 보여줌).
  - 오버드라이브 게이지: 양쪽에 황동 캡을 단 "유리관" 트랙 + 은은한 시머 스윕 애니메이션 + 100% 도달 시 1회성 스케일 펀치·화이트 플래시.
  - 장착 코그 표시(`cogRow`)는 기존 그대로 유지.
- `src/components/Controls.tsx`: `ButtonFace`에 역할별 색상(`move`=중성 스틸, `grapple`=이끼그린, `dash`=엠버, `jump`=기존 황동)과 볼트 리벳 4점, 유리 하이라이트, 눌림 글로우를 추가. 눌림 글로우는 기존 터치 판정이 이미 구동하던 `scale` Animated.Value를 보간해서 만들어 새 `Animated.Value`나 React state를 추가하지 않았다(`CLAUDE.md` 18장 성능 원칙 — 불필요한 state 업데이트 금지).
- 터치 히트박스 계산(`measure()`가 참조하는 ref, `nativeEvent.touches` 기반 판정)은 전혀 수정하지 않음 — 장식 요소는 전부 `pointerEvents="none"`이거나 히트박스 밖의 절대 위치 자식 뷰라 입력 판정에 관여하지 않는다.

### 검증
- `npx tsc --noEmit`: 통과(`StyleSheet.absoluteFillObject`가 이 RN 버전 타입에 없어 `absoluteFill`로 수정 후 통과).
- Expo 웹 + Playwright 스크린샷(390×844):
  - 정적 스크린샷으로 새 HUD/컨트롤 디자인 확인(생명 핵 3슬롯, 코인 아이콘, 기계/자연 뱃지, 황동 캡 게이지, 색상별 컨트롤 버튼).
  - 90프레임(~1.5초) 대기 후 재스크린샷 — 크래시 없음, Sprout idle 애니메이션 정상 유지.
  - CDP `Input.dispatchTouchEvent`로 오른쪽 이동 버튼을 1.2초간 실제로 눌러 시뮬레이션: 플레이어가 우측으로 이동, 카메라 패닝, Bloom Shift 노드를 통과해 HUD 뱃지가 "기계"→"자연"으로 실시간 전환되는 것까지 확인 — 입력 판정·물리·Bloom Shift 로직이 리디자인 이후에도 그대로 동작함을 확인.
  - 콘솔 `pageerror` 없음.

### 영향
- 게임플레이 로직 영향 없음(표현 레이어만 교체). 생명 핵 UI는 `STARTING_LIVES`(3)를 새로 import해서 참조하지만 그 값 자체를 변경하지 않았으므로 불변 수치 위반 아님.

### 남은 문제
- 시작 화면(`StartScreen.tsx`)과 게임오버/승리 오버레이 카드(`GameScreen.tsx`의 `overlay`/`card`)는 아직 Phase 2 대상인데 이번 커밋에는 포함하지 못했다 — 다음 작업으로 이어간다.
- 실제 터치스크린 기기에서의 버튼 크기감·눌림 피드백 체감은 여전히 미검증(Expo Go 실기기 테스트 전체가 미실시 상태).

### 결정 사항
- 장식 요소를 추가할 때 새 `Animated.Value`를 만들기보다, 이미 입력 판정이 구동하는 기존 값(예: 버튼의 `scale`)을 보간해 재사용하는 것을 기본 패턴으로 삼는다 — 애니메이션 수를 늘리지 않으면서 피드백을 더할 수 있다.

## 2026-08-26 (7) — 리디자인 Phase 2 마무리: 시작 화면·게임오버/승리 카드

### 목표
- Phase 2(UI) 나머지 대상이던 시작 화면과 결과(게임오버/승리) 오버레이 카드에 Controls/Hud와 동일한 "황동 프레임 + 코너 리벳" 디자인 언어를 적용해 화면 간 디자인 언어 통일(`CLAUDE.md` 19장 — "HUD와 게임 월드의 디자인 언어가 분리되어 있음" 문제 해결).

### 변경
- `src/screens/StartScreen.tsx`: 제목/부제/설명/버튼/조작법 힌트를 감싸는 반투명 황동 테두리 패널(`brassPanel`)을 추가하고 네 모서리에 리벳을 배치. 게임 시작 버튼에도 Controls 버튼과 동일한 유리 하이라이트(`startGlassHighlight`)를 추가.
- `src/screens/GameScreen.tsx`: 게임오버/승리 오버레이 카드에 3px 테두리 + 코너 리벳 4개를 추가하고, 테두리 색을 결과에 따라 분기(승리=이끼그린 `palette.moss`, 게임오버=엠버 `palette.uiDanger`)해 결과를 색으로도 즉시 인지할 수 있게 함.

### 검증
- `npx tsc --noEmit`: 통과.
- Expo 웹 + Playwright:
  - 새로고침 후 시작 화면 스크린샷 — 황동 프레임 패널과 리벳, 버튼 하이라이트 정상 렌더링.
  - CDP로 오른쪽 이동을 계속 눌러 실제로 코그마이트에게 부딪혀 목숨을 모두 소진시킨 뒤 "게임 오버" 오버레이가 뜨는 것을 확인 — 엠버색 테두리와 리벳이 정상 적용되고, 다시 시작/메인 메뉴 버튼도 정상 클릭 가능한 상태로 렌더링됨. 배경에 있던 코그마이트가 실제 사망 원인으로 화면에 그대로 보여 "왜 죽었는지" 인지 가능(`CLAUDE.md` 18장 원칙과도 부합).
  - 콘솔 에러 없음.

### 영향
- 게임플레이 로직 영향 없음(스타일/장식만 추가, 버튼 onPress 핸들러는 그대로).

### 남은 문제
- Phase 2(UI)는 이것으로 사실상 마무리. 다음은 `CLAUDE.md` 19장 Phase 3(World Art: 플랫폼/배경/Bloom Shift 시각 효과)로 넘어갈 차례.
- 승리(win) 화면 쪽은 실제 클리어까지 플레이해서 초록 테두리를 스크린샷으로 확인하지는 못했다(레벨이 길어 자동 진행 테스트 시간이 오래 걸림) — 스타일 코드 자체는 게임오버와 동일한 조건부 로직이라 위험도는 낮다고 판단해 별도 실측 없이 진행.

### 결정 사항
- Phase 2를 여기서 마무리하고 Phase 3(World Art)로 진행한다.

## 2026-08-26 (8) — 리디자인 Phase 3 시작: Bloom Shift를 세계 전체의 변화로 확장

### 목표
- `CLAUDE.md` 19장 Phase 3(World Art) 착수. 그 중에서도 "Bloom Shift가 핵심 기믹인데 시각적으로 강하게 느껴지지 않는다"는 문제와 "배경이 단순한 도형 위주"라는 문제를 함께 해결. `PlatformView.tsx`는 확인해보니 이미 리벳/나무결/이끼 잎 디테일이 상당히 들어가 있어 이번 라운드에서는 손대지 않고 그대로 재사용.

### 사전 확인
- `gameState.bloomState`는 레벨 전역에 단 하나만 존재하는 값(존 4개의 시프트 노드 중 아무거나 밟아도 레벨 전체가 한 번에 전환)임을 `types.ts`/`physics.ts`에서 확인. 배경도 이 전역 상태에 반응시키는 것이 자연스럽다고 판단.

### 변경
- `src/components/Background.tsx`: `bloomState` prop 추가.
  - 하늘에 기계(호박색)/자연(이끼그린) 틴트 오버레이 두 장을 겹쳐두고, `bloomState`가 바뀔 때마다 `Animated.Value` 하나(`wildAnim`)를 0↔1로 650ms 크로스페이드 — 두 오버레이의 불투명도가 서로 반대로 움직인다(`useNativeDriver: true`이므로 opacity만 사용, backgroundColor 보간은 쓰지 않음).
  - 원경 장식도 기계 상태의 톱니 실루엣(`GearSilhouette`, 기존의 점선 원을 8개 이빨이 있는 실제 기어 모양으로 교체)과 자연 상태의 덩굴 다발(`VineCluster`, 신규)을 같은 슬롯에 겹쳐 그리고 동일한 `wildAnim` 값으로 서로 반대 방향 크로스페이드.
- `src/screens/GameScreen.tsx`: `<Background>`에 `bloomState` prop 전달 추가. 그리고 `bloomFlash`라는 별도의 `Animated.Value`로 상태가 바뀌는 그 순간(전 프레임 대비 변화 감지, `prevBloomStateRef`)에 스테이지 전체를 덮는 550ms 색상 플래시(새 상태의 팔레트 색, 최대 불투명도 0.45)를 1회 재생 — Background의 650ms 크로스페이드보다 짧고 강하게 튀어서 "지금 이 순간 바뀌었다"는 즉각적 신호 역할을 하고, Background의 크로스페이드는 그 뒤에 남는 "세계가 계속 그 상태다"라는 배경 무드를 맡는 방식으로 역할을 나눴다.

### 검증
- `npx tsc --noEmit`: 통과.
- Expo 웹 + Playwright: CDP로 오른쪽 이동을 누른 채 0.3초 간격으로 연속 스크린샷 10장을 찍어 시프트 노드를 넘는 구간을 포착.
  - HUD 뱃지가 "기계"→"자연"→"기계"로 바뀌는 프레임들에서 하늘 틴트, 발판(나무↔덩굴), 원경 장식(기어 실루엣↔덩굴 다발)이 모두 함께 바뀌는 것을 확인.
  - 콘솔 `pageerror` 없음.

### 영향
- 게임플레이 로직 영향 없음(순수 표현 레이어, `bloomState`는 읽기만 하고 물리/충돌 판정에는 관여하지 않음).
- `Background`가 이제 `bloomState`를 필수 prop으로 받으므로, 이 컴포넌트를 다른 곳에서 재사용할 경우(현재는 `GameScreen.tsx` 한 곳에서만 사용) 호출부를 함께 갱신해야 한다.

### 남은 문제
- Phase 3 나머지 항목(플랫폼 추가 디테일은 기존 구현으로 충분하다고 판단해 보류, 다만 "손으로 만든 느낌"을 더 살릴 여지는 있음)과 Phase 4(캐릭터)·Phase 5(파티클 이펙트)는 아직 손대지 않았다.
- Bloom Shift 전환 시 파티클(먼지/꽃가루, 빛의 파동 링 등)은 이번에 추가하지 않았다 — 19장 원문은 이를 Phase 3(World Art)에 포함하지만, 개별 파티클 시스템 구축은 Phase 5(Effects)와 성격이 겹쳐 다음 작업에서 이어서 판단할 것.

### 결정 사항
- Bloom Shift 시각 효과를 "순간적 플래시(GameScreen, 550ms)"와 "지속적 배경 무드 전환(Background, 650ms 크로스페이드)" 두 레이어로 나누어 역할을 분리한다 — 이후 파티클을 추가할 때도 이 두 레이어 중 어디에 넣을지 판단 기준으로 삼는다.

## 2026-08-26 (9) — 리디자인 Phase 4: 캐릭터(Sprout) 이동 이펙트

### 목표
- `CLAUDE.md` 19.9(플레이어 캐릭터) 중 아직 없던 항목만 보강: 대시 잔상/속도선, 벽점프 파편, Root-Hook 로프의 "살아있는 식물" 느낌. `PlayerView.tsx`를 확인해보니 착지 Squash&Stretch·공중 스트레치·달리기 bob/lean은 이미 이전 세션에 구현되어 있어 그대로 유지.

### 변경
- `src/components/PlayerView.tsx`:
  - `player.dashTimer > 0`인 동안 최근 위치 3개를 `useRef` 배열에 누적해 반투명 잔상(고스트) 이미지로 렌더링하고, 진행 반대쪽에 흰색 속도선 3줄을 추가. 트레일 배열은 렌더 도중 직접 갱신하는데, 이 컴포넌트는 어차피 부모의 게임 루프에 의해 매 틱 리렌더되므로(기존 `bobY`가 `Date.now()`를 렌더 중 직접 읽는 것과 동일한 패턴) 별도 state/렌더를 추가하지 않았다.
  - `player.touchingWall`이 0이 아니었다가 0이 되면서 동시에 `vy<0`(위로 튕겨나감)인 프레임을 "벽점프 순간"으로 판정해, 기존 착지 스쿼시와 같은 `useEffect`에 이어붙여 벽 반대쪽에 작은 이끼색 파편 3개가 튀는 320ms 펀치를 추가.
- `src/components/RopeView.tsx`: 단색 직선 로프를 이끼 그라디언트 + 길이에 비례해 배치되는 작은 잎 돌기들로 교체해 "살아있는 덩굴"처럼 보이도록 함. 로프 길이·각도 계산 로직 자체는 그대로.

### 검증
- `npx tsc --noEmit`: 통과.
- Expo 웹 + Playwright:
  - 대시: 실제 버튼 좌표를 `boundingBox()`로 정확히 읽어 대시 버튼을 짧게 눌렀다가 뗀 뒤 60ms 후 스크린샷 — 플레이어 옆에 흰 속도선 3줄이 렌더링되는 것을 확인. CDP 멀티터치(이동+대시 동시 홀드) 에뮬레이션은 이 환경에서 신뢰도가 낮아(터치 다중화가 브라우저 자체 제스처와 섞이는 것으로 추정) 단일 터치로 검증 방식을 바꿈.
  - 대시 잔상(고스트 이미지)과 벽점프 파편, Root-Hook 로프 잎 장식은 코드 검토와 타입체크로만 확인했고 스크린샷으로 개별 포착하지는 못했다 — «구현 완료 / 검증 미완료»로 기록.
  - 콘솔 에러 없음.

### 영향
- 게임플레이 로직 영향 없음(모두 기존 `player.dashTimer`/`touchingWall`/`grappling`/`grappleAnchorX·Y` 필드를 읽기만 함, 물리 코드 미수정).

### 남은 문제
- 벽점프 파편·대시 잔상·로프 잎 장식의 실제 화면 캡처 검증이 남아있다(다음 세션에서 정확한 시나리오로 재시도하거나 실기기 테스트 때 육안 확인).
- Phase 5(Effects: 스톰프/체크포인트/기어 획득 파티클)는 아직 시작하지 못했다.

### 결정 사항
- 멀티터치 CDP 에뮬레이션이 불안정한 상황에서는 단일 입력으로 쪼개서 검증하고, 개별 시나리오를 스크린샷으로 못 잡은 경우 "구현 완료/검증 미완료"로 정직하게 남긴다(`CLAUDE.md` 18.9 원칙).

## 2026-08-26 (10) — 리디자인 Phase 5 착수: 체크포인트 활성화 이펙트 + 전체 회귀 스모크 테스트

### 목표
- 사용자가 자정 넘어 자율 진행을 요청("최대한 개발 진행해줘"). Phase 5(Effects) 중 가장 명확한 공백이던 체크포인트를 먼저 채우고, 이번 세션에서 쌓인 변경사항(Phase 1~5 일부) 전체를 한 번에 오래 플레이시켜 회귀가 없는지 마지막으로 점검.

### 사전 확인
- 코드베이스 전체에 "particle" 관련 컴포넌트가 전혀 없음을 확인(`grep -rli particle src/components` 결과 없음). 스톰프/코인·기어 획득 순간의 파티클 버스트는 아직 전혀 없다는 뜻 — 코인/코그는 `collected` 즉시 `return null`이라 사라지는 순간 자체에 애니메이션을 걸려면 로컬 state로 언마운트를 지연시키는 추가 구조가 필요해 이번엔 보류(체크포인트는 `reached`가 바뀌어도 계속 렌더링되는 컴포넌트라 상대적으로 안전하게 먼저 처리).

### 변경
- `src/components/CheckpointView.tsx`: `reached`가 false→true로 바뀌는 프레임을 감지해(기존 착지/벽점프와 동일한 prevRef 패턴) 700ms짜리 확장 링 + 위로 튀는 스파크 3개를 1회 재생. 깃발/발판처럼 이미 값이 있는 지점 도달을 축하하는 효과가 전혀 없었던 공백을 메움.

### 검증
- `npx tsc --noEmit`: 통과.
- Expo 웹 + Playwright로 체크포인트 통과 지점까지 자동 이동을 시도했으나, 이번 봇은 장애물을 넘는 판단이 없어 첫 코그마이트에게 반복해서 부딪혀 체크포인트 도달 전에 게임오버됨 — 체크포인트 이펙트 자체는 스크린샷으로 직접 포착하지 못했다. 코드는 기존에 이미 3차례 검증된 동일 패턴(useRef 기반 prev-diff + Animated.timing)이라 위험도는 낮다고 판단, «구현 완료 / 검증 미완료»로 기록.
- **전체 회귀 스모크 테스트**: 오른쪽 이동을 계속 누른 채 0.6초 간격으로 점프를 21회 반복 입력(총 약 14초 실플레이, 대시는 사용하지 않음)했을 때 브라우저 콘솔에 `pageerror`가 한 번도 발생하지 않음을 확인. 최종적으로 코그마이트에게 부딪혀 게임오버 화면(엠버 테두리+리벳)이 정상 출력되는 것도 재확인. 이번 세션에서 만든 모든 변경(화면 스케일링, HUD/컨트롤/시작화면/오버레이 리디자인, 배경 Bloom Shift 반응, 대시·벽점프·로프 이펙트, 체크포인트 이펙트)이 함께 활성화된 상태에서 크래시 없이 동작함을 확인.

### 영향
- 게임플레이 로직 영향 없음(`reached` prop을 읽기만 함).

### 남은 문제
- 코인/기어 코그 획득 순간의 파티클 버스트는 아직 없음(구현하려면 `collected` 전환 시 잠깐 더 렌더링을 유지하는 로컬 state가 필요해 이번 세션 범위에서 보류) — 다음 세션 후보.
- 스톰프 임팩트 파티클(19.17 "스톰프: 충돌→작은 파편→적 Squash→플레이어 Bounce")도 아직 없음. 이건 `physics.ts`에 이벤트를 추가하지 않고 순수 표현 레이어에서 구현하기 까다로운 편(스톰프가 일어난 정확한 위치/시점을 프레젠테이션 레이어가 알아야 함) — 다음 세션에서 `gameState`에 읽기 전용 이벤트 로그(예: 최근 스톰프 좌표+타임스탬프 배열)를 추가하는 것이 필요한지 먼저 검토할 것(이 경우 `physics.ts` 수정이 필요하므로 임의로 진행하지 않고 우선 기록만 남김 — `CLAUDE.md` 19.1 원칙).
- 실기기 Expo Go 테스트는 이번 세션에서도 진행하지 못했다(계속 알려진 제한사항).

### 결정 사항
- 이번 자율 세션은 여기서 마무리한다. Phase 1(화면 구조) 완료, Phase 2(UI) 완료, Phase 3(World Art) 중 Bloom Shift 세계 반응 완료(플랫폼 디테일은 기존 구현 재사용), Phase 4(캐릭터) 중 대시/벽점프/로프 완료(몬스터는 기존 구현이 이미 충분하다고 판단해 보류), Phase 5(Effects) 중 체크포인트만 완료. 다음 세션은 Phase 5 나머지(코인/기어/스톰프 파티클)부터 이어가되, 스톰프 이펙트는 `physics.ts`에 이벤트 로그를 추가할지 먼저 사용자와 상의 후 진행할 것.

### 세션 종료 전 자동 점검 (`CLAUDE.md` 18.12)
- 기존 기능을 깨뜨리지 않았는가? → 이번 세션 내내 물리/충돌/레벨 데이터 코드는 한 줄도 수정하지 않았고(모두 `GameScreen.tsx`/컴포넌트의 표현 레이어), 매 단계마다 `tsc` + 실제 플레이 스모크 테스트로 확인했다.
- 테스트하지 않은 부분은? → 대시 잔상, 벽점프 파편, 로프 잎 장식, 체크포인트 이펙트, 승리(win) 화면의 초록 테두리 — 전부 스크린샷으로 직접 포착하지 못했다(위 각 세션 기록에 개별적으로 명시).
- 새 버그를 만들 가능성? → 스타일/애니메이션 전용 변경이라 낮다고 판단하지만, 실기기 터치감·성능은 미검증.
- 플레이어가 이상하게 느낄 부분? → 화면 스케일 변경으로 기기별로 한 번에 보이는 가로 시야가 달라진다(의도된 트레이드오프, 위 Phase 1 기록 참고).
- 기존 수치를 무단 변경하지 않았는가? → `game/constants.ts`, `game/level.ts`, `game/physics.ts` 미수정. 새 로컬 UI 상수(`GameScreen.tsx`의 `HUD_RESERVED_TOP` 등)만 추가.
- 다음 개발자가 알아야 할 결정? → 위 "결정 사항" 및 각 세션의 "남은 문제" 참고.
- 개발로그에 기록해야 할 내용? → 모두 기록함(이 항목 포함 세션 (5)~(10)).

## 2026-08-26 (11) — 리디자인 Phase 5 계속: 읽기 전용 이펙트 로그 + 스톰프/피격/획득 파티클

### 목표
- 지난 세션에서 "물어보고 진행" 조건으로 보류했던 스톰프/피격/코인·기어 획득 파티클을 구현. 사용자가 "진행해"로 계속 자율 진행을 재확인해 착수.

### 설계
- `physics.ts`를 수정해야 하는 사안이라 신중하게 범위를 최소화: **기존 판정/수치는 전혀 바꾸지 않고, 새 필드만 추가**하는 방식으로 한정했다.
- `CorpsePlatform`이 이미 쓰던 "생성 시점에 `timeLeft` 부여 → 매 프레임 `dt`만큼 감소 → 0 이하면 필터링" 패턴을 그대로 재사용해 `EffectEvent`(종류 `impact`/`hit`/`pickup`/`gearPickup`, 좌표, `timeLeft`)를 설계. `GameState.effects` 배열과, id 채번용 `effectSeq` 카운터를 추가(둘 다 `physics.ts`가 쓰기만 하고 읽지는 않음 — 렌더링 전용 편도 데이터라 게임플레이에 영향 줄 수 없음이 구조적으로 보장됨).

### 변경
- `src/game/types.ts`: `EffectKind`, `EffectEvent` 타입과 `GameState.effects`/`effectSeq` 필드 추가.
- `src/game/constants.ts`: `EFFECT_DURATION = 0.5`(연출 전용 수치, 밸런스 수치 아님을 주석에 명시) 추가.
- `src/game/physics.ts`: `createInitialState`에 초기값 추가, 매 프레임 `decayedEffects`를 corpsePlatforms와 동일하게 감쇠·필터링, `pushEffect(kind, x, y)` 헬퍼 추가 후 다음 지점에 호출만 삽입(판정 로직 자체는 한 글자도 안 바꿈):
  - 코그마이트/바이오코일(착지 상태)/스팀 블로어 스톰프 성공(또는 기계 상태라 튕겨나가는 경우 포함) → `impact`
  - 스포어 스프라이트 대시 관통 처치 → `impact`
  - 5곳의 `applyHit()` 호출 직전(코그마이트 충돌, 바이오코일 충돌, 스팀 블로어 화염포자, 프레셔 피스톤, 낙사) → `hit`
  - 코인 획득 → `pickup`, 기어 코그 획득 → `gearPickup`
- `src/components/EffectsView.tsx` 신규: `gameState.effects` 배열을 매핑해 종류별 색상(파편 색 + 링 색)의 380ms 파편 버스트 + 확장 링을 1회 재생하는 순수 표현 컴포넌트. `EFFECT_DURATION`(0.5초)이 애니메이션 재생시간(0.38초)보다 넉넉히 길어서, 실제로 화면에서 사라지는 건 애니메이션이 다 끝난 뒤 물리 쪽에서 배열을 정리하는 타이밍과 맞아떨어진다.
- `src/screens/GameScreen.tsx`: `EffectsView`를 world 컨테이너 안(PlayerView 다음)에 렌더링, `effects={gameState.effects}` 전달.
- 참고: 이 이펙트 시스템 덕분에 지난 세션에 "구조가 복잡해서 보류"했던 코인/기어 획득 버스트 문제도 자연스럽게 해결됨 — `CoinView`/`CogPickupView` 자체의 언마운트 지연 로직 없이도, 별도의 `EffectsView`가 수집 "순간"의 좌표에 독립적으로 버스트를 그려주기 때문.

### 검증
- `npx tsc --noEmit`: 통과.
- Expo 웹(재시작) + Playwright:
  - 이동+점프 반복 재생(약 8초)에서 코인 2개를 실제로 주워 점수가 0→20으로 정상 증가하는 것을 확인.
  - 같은 세션 동안 `pageerror` 0건.
  - 버스트 애니메이션 자체(파편이 퍼지는 380ms 순간)를 정확한 타이밍에 스크린샷으로 포착하는 데는 실패했다 — 점수 변화를 감지해 그 프레임에 캡처를 시도했으나 DOM 텍스트 노드 탐색 휴리스틱이 react-native-web의 실제 렌더 구조와 맞지 않아 이벤트 자체를 못 잡음. 코드 경로는 이미 세 번 이상 검증된 동일 패턴(useRef prev-diff 또는 mount 시 1회 재생 Animated.timing)이라 위험도는 낮다고 판단.
  - «구현 완료 / 스크린샷 캡처 미완료»로 기록.

### 영향
- 게임플레이 로직 영향 없음 — `effects`/`effectSeq`는 물리 코드가 쓰기만 하고 어디서도 읽지 않으므로 판정·점수·충돌 결과에 구조적으로 영향을 줄 수 없다.
- `GameState` 형태가 바뀌었으므로(새 필드 2개 추가) `createInitialState`를 거치지 않고 `GameState`를 직접 구성하는 다른 코드가 있다면 갱신이 필요하지만, 이 저장소엔 `createInitialState` 외의 다른 생성 지점이 없음을 확인했다.

### 남은 문제
- 파편 버스트를 자동 테스트로 정확히 캡처하는 방법은 다음 세션 숙제로 남긴다(스코어 텍스트 탐색 대신 `gameState.effects.length > 0`을 굳이 페이지에 노출하지 않고도 잡을 안정적인 방법이 필요 — 예: 캡처 프레임 수를 늘리거나 애니메이션 지속시간을 일시적으로 늘려 디버그 빌드로 확인).
- 몬스터 실루엣/비주얼 추가 개선, 실기기 Expo Go 테스트는 여전히 미착수.

### 결정 사항
- "표현 레이어 전용이라도 `physics.ts` 수정이 필요하면 먼저 기록 후 진행"이라는 지난 세션의 유보를, 순수 쓰기 전용·읽기 없음 구조로 설계함으로써 게임플레이에 영향 줄 수 없음을 구조적으로 보장하는 조건 하에 이번엔 직접 구현까지 진행했다. 앞으로도 physics.ts에 프레젠테이션 전용 데이터를 추가할 땐 "물리 코드가 그 필드를 절대 읽지 않는다"를 원칙으로 삼는다.

## 2026-08-26 (12) — 이펙트 로그를 순수 로직 시뮬레이션으로 확정 검증

### 목표
- 지난 세션에 "스크린샷 캡처 실패"로 미결 처리했던 스톰프/피격/획득 파티클을, `개발로그.md` 11장이 원래 1순위로 명시한 검증 방식("순수 로직 시뮬레이션 — Node/tsx로 physics.ts 직접 호출")으로 확정 검증.

### 과정
- 스크린샷 폴링 → 점수 변화 감지 → Playwright 비디오 녹화 후 ffmpeg로 프레임 추출 + Python/Pillow로 프레임 간 픽셀 차이 랭킹까지 시도했지만, 380~420ms짜리 작은 파편 이펙트는 20~30fps 저해상도 webm 압축에 묻혀 스크린샷/영상으로는 끝내 명확히 포착하지 못함.
- 방향을 바꿔 `EffectsView.tsx`의 파편 크기(6px→8px)·색상 대비를 키우고, 애니메이션 시작 시점에 밝은 화이트/골드 "플래시" 팝을 추가해 실제 플레이 시 눈에 더 잘 띄도록 개선(육안 가독성 자체의 개선이므로 스크린샷 검증 성패와 무관하게 값어치 있는 변경).
- 근본적으로 렌더링 결과물을 화면 캡처로 검증하려는 접근 자체가 이런 초단명 이펙트엔 안 맞는다고 판단, 프로젝트가 원래 채택한 1순위 방법으로 전환: `createLevel()` + `createInitialState()`로 실제 레벨을 불러온 뒤, 플레이어를 실제 적/코인/코그 위치로 순간 이동시켜 `stepGame()`을 1프레임 호출하고 `effects` 배열을 직접 assert하는 tsx 스크립트를 임시로 작성해 실행(레포에는 남기지 않음, 기존 세션들의 검증 스크립트 관례와 동일).

### 검증 결과 (11개 항목 전부 통과)
1. 낙하 스톰프 → `impact` 이펙트 생성, 좌표가 적 중심에 정확히 일치.
2. 스톰프 조건 미충족 측면 충돌 → `hit` 이펙트 생성 + 생명 1 감소(기존 판정 그대로 유지되는지도 함께 확인).
3. 코인 획득 → `pickup` 이펙트 생성 + 점수 증가.
4. 코그 획득 → `gearPickup` 이펙트 생성.
5. 이벤트 발생 직후엔 `effects.length > 0`, 이후 1초(60프레임)를 더 진행하면 `EFFECT_DURATION`(0.5초)이 지나 `effects.length === 0`으로 정리됨 — 메모리에 계속 쌓이지 않음을 확인.
6. 같은 런에서 여러 이펙트가 생성돼도 `effectSeq` 기반 id가 서로 겹치지 않음.

### 영향
- 게임플레이 로직 영향 없음(검증만 진행, 코드 변경은 `EffectsView.tsx`의 시각적 대비 개선뿐).

### 남은 문제
- 위 시뮬레이션은 "물리 쪽이 이펙트를 정확히 만들고 정확히 정리하는가"를 확정적으로 증명하지만, "실제 화면에서 육안으로 봤을 때 이펙트가 자연스럽고 만족스러운가"는 여전히 실제 플레이(이상적으로는 실기기)로 판단해야 하는 부분이다. 다음에 실기기 테스트를 하게 되면 이 항목을 함께 확인할 것.

### 결정 사항
- 앞으로 애니메이션 지속시간이 500ms 미만인 짧은 이펙트를 검증할 땐, 스크린샷/영상 캡처를 먼저 시도하지 말고 곧바로 "순수 로직 시뮬레이션으로 상태 변화를 assert"하는 방식부터 사용한다 — 이번 세션에서 스크린샷 계열 시도에 들인 시간 대비, 로직 시뮬레이션이 훨씬 빠르고 확정적이었다.

## 2026-08-26 (13) — 몬스터 비주얼 보완: 프레셔 피스톤·스포어 스프라이트

### 목표
- `CLAUDE.md` 19.10(몬스터 실루엣 차별화) 중 상대적으로 손이 덜 간 두 몬스터를 보완: 프레셔 피스톤(현재는 작은 파이프처럼만 보임 → "거대한 환경 장치"로), 스포어 스프라이트(단순한 점 → "부유하는 생명체"답게).

### 변경
- `src/components/PressurePistonView.tsx`: 파이프 하단에 실제 충돌 판정 폭(`piston.width`)보다 넓은(130%) 장착 브라켓 + 볼트 2개를 추가해 "몬스터가 아니라 바닥에 고정된 설비"처럼 보이도록 함. `piston.x/y/width/height`(충돌 사각형)는 그대로.
- `src/components/SporeSpriteView.tsx`: 본체 바깥에 펄스와 함께 옅어졌다 짙어지는 글로우 헤일로(170% 크기)를 추가하고, 떠다니는 포자 점(mote)을 2개→3개로 늘림.

### 검증
- `npx tsc --noEmit`: 통과.
- Expo 웹 + Playwright로 실제 레벨의 피스톤(x=650)·스포어(x=1150) 위치까지 자동 이동을 시도했으나, 이번 봇도 장애물 근처에서 진행이 막혀(오늘 밤 다른 세션에서도 반복 관찰된 동일한 봇 한계 — 똑똑한 경로 탐색이 없어 장애물을 만나면 제자리에서 점프만 반복) 두 몬스터와 조우하는 프레임을 직접 포착하지 못했다.
- 두 변경 모두 이미 여러 차례 화면으로 검증된 패턴(절대 위치 자식 뷰 추가, `Animated.Value` 보간)의 단순 반복이라 위험도는 낮다고 판단해 «구현 완료 / 스크린샷 미포착»으로 기록.

### 영향
- 게임플레이 로직 영향 없음(두 컴포넌트 모두 판정에 쓰이는 `piston`/`sprite` 필드를 읽기만 하고 그대로 렌더링 스타일만 확장).

### 남은 문제
- 이번 세션 전반에 걸쳐 자동 플레이 봇이 장애물 앞에서 자주 막히는 한계가 반복 관찰됨 — 다음에 시각적 검증이 많이 필요한 작업을 할 땐, 매번 처음부터 걷게 하기보다 (a) 레벨 특정 지점으로 순간 이동하는 디버그 전용 시작 지점을 임시로 넣거나 (b) 이번에 했던 순수 로직 시뮬레이션 방식을 렌더링 스냅샷에도 응용하는 방법을 검토할 가치가 있다.
- 몬스터 비주얼 폴리싱은 여기서 일단 마무리. 코그마이트/바이오 코일/스팀 블로어는 기존 구현이 이미 충분하다고 판단해 그대로 둔다.

### 결정 사항
- Phase 4(캐릭터/몬스터)를 여기서 마무리 처리한다. 다음 세션 우선순위는 실기기 Expo Go 테스트, 또는 Phase 6(최종 폴리싱: 색상·간격·애니메이션 속도·성능 점검)로 넘어가는 것을 제안.

## 2026-08-26 (14) — 스톰프/피격 파티클 최종 스크린샷 확인 + 세션 마무리

### 목표
- 세션 (11)/(12)에서 순수 로직으로만 검증하고 스크린샷 포착엔 실패했던 이펙트 버스트를, 이동+점프+대시 조합 회귀 테스트 도중 우연히 잡은 게임오버 프레임에서 최종 확인.

### 검증
- 이동+점프/대시 25사이클 조합 회귀 테스트(콘솔 에러 0건) 도중 코그마이트에게 부딪혀 사망하는 순간의 스크린샷을 찍었는데, 게임오버 오버레이 뒤로 코그마이트 주변에 `hit` 이펙트의 붉은 파편(`palette.uiDanger`) 3개가 선명하게 남아있는 것을 확인(해당 영역을 3배 확대해 육안으로 재확인). 오버레이가 반투명해서 배경이 살짝 비치는 덕에 우연히 포착됨.
- 이로써 스톰프/피격/획득 파티클은 **순수 로직 시뮬레이션(11개 항목 통과) + 실제 화면 스크린샷** 양쪽으로 모두 확인 완료. «구현 완료 / 검증 완료»로 상향.
- 시작 화면 → 게임 화면 → 게임오버 화면 전체 흐름을 한 번에 다시 스크린샷으로 확인, 이번 세션에서 만든 모든 변경(화면 스케일링, HUD/컨트롤/시작화면/오버레이, 배경 Bloom Shift 반응, 대시/벽점프/로프, 체크포인트, 이펙트 로그, 몬스터 비주얼)이 함께 정상 동작함을 재확인.

### 영향
- 게임플레이 로직 영향 없음(검증만 진행, 코드 변경 없음).

### 남은 문제 (다음 세션 인계)
- 실기기 Expo Go 테스트 — 계속 미실시.
- 승리(win) 화면의 초록 테두리 — 아직 스크린샷으로 직접 확인 못함(레벨을 끝까지 클리어해야 해서 자동 테스트 시간이 오래 걸림).
- Phase 6(최종 폴리싱: 색상 시스템 전체 재검토, 간격, 애니메이션 속도, 성능 프로파일링)은 아직 별도로 착수하지 않음.
- 자동 플레이 봇의 장애물 회피 능력 부재는 여러 세션에 걸쳐 반복 관찰된 한계 — 다음에 시각 검증이 많이 필요하면 디버그용 순간이동 스폰이나 순수 로직 스냅샷 방식을 우선 고려할 것.

### 결정 사항
- 오늘 밤 자율 진행 세션(2026-08-26 (5)~(14))을 여기서 마무리한다. Phase 1(화면 구조) 완료, Phase 2(UI) 완료, Phase 3(World Art) 중 Bloom Shift 세계 반응 완료, Phase 4(캐릭터/몬스터) 완료, Phase 5(Effects) 중 체크포인트·스톰프·피격·획득 파티클 완료. 남은 것은 위 "다음 세션 인계" 참고.

## 2026-08-27 (15) — AI 일러스트 에셋 통합(배경/플랫폼/몬스터 4종) + 투명도 버그 수정

### 목표
- 사용자가 업로드한 `CLAUDE_VISUAL_QUALITY_OVERHAUL.md`와 참고 이미지("이정도 퀄리티로 못해줘?")에 따라, 기존 절차적 도형(구름/산/장식/플랫폼/몬스터 4종)을 Sprout·코그마이트와 동일한 화풍의 AI 일러스트로 교체.
- 대상: `Background.tsx`(구름 2종, 산 2종, 기계/자연 장식 1종씩), `PlatformView.tsx`(기계/자연 플랭크), `SporeSpriteView.tsx`/`BioCoilView.tsx`/`PressurePistonView.tsx`/`SteamBlowerView.tsx`(몬스터 4종). 코그마이트와 플레이어는 이전 세션에 이미 일러스트가 적용되어 있었음.

### 변경
1. **에셋 생성**: `nano_banana_pro` 모델로 12장 생성(구름 2, 산 2, 장식 2, 플랭크 2, 몬스터 4), sprout/cogmite 이미지를 스타일 레퍼런스로 사용. 각 몬스터/플랭크/장식은 한 상태(기계 또는 자연)만 일러스트로 그리고, 반대 상태는 반투명 틴트 오버레이로 표현(생성 크레딧 절약 목적, 의도된 트레이드오프).
2. **컴포넌트 통합**: 6개 파일을 다시 작성해 절차적 `View` 대신 `<Image>`를 렌더링하되, 각 컴포넌트의 상태 기반 애니메이션(펄스/글로우, phase별 stretch, jet/gust/ember, HP pips, Bloom Shift cross-fade)은 전부 그대로 유지. `PlatformView.tsx`는 플랭크 이미지가 가로로 반복되는 무늬(리벳/덩굴 짜임)라는 점에 착안해, 한 장을 늘려 붙이는 대신 고정 비율 타일을 폭만큼 배열하는 방식(`PlankTiles`)을 새로 만듦 — 기존 `dirtSeam`/`rivet`/`leafBump` 패턴과 동일한 "반복 배열" 컨벤션.
3. **충돌 판정과 그래픽 분리 유지**: 모든 뷰에서 `platform.x/y/width/height`, `piston.x/y/width/height` 등 실제 충돌 사각형은 전혀 건드리지 않고, 그래픽만 `VISUAL_SCALE`(1.4~1.8배)로 살짝 크게 그림 — 기존 세션에서 확립한 컨벤션 그대로.

### 발견한 버그와 수정 (중요)
- **`npx tsc` 통과 후 실제 Expo 웹 빌드로 화면을 스크린샷했더니, 12장 중 10장(구름 2, 산 2, 자연 장식 1, 자연 플랭크 1, 몬스터 4종 전부)이 투명 배경이 아니라 "투명을 나타내는 회색/흰색 체커보드 무늬"를 실제 RGB 픽셀로 그대로 그린 채 alpha=255(완전 불투명)로 저장되어 있었다.** 즉 이전 세션에 "Read 도구 미리보기가 실제로는 투명한 파일을 흰/검정으로 잘못 보여주는 것뿐"이라고 결론 내렸던 것은 `decor_mech.png`/`plank_mech.png` 단 2장에 대해서만 맞는 얘기였고, 나머지 10장은 실제로 불투명한 파일이었다(PIL로 코너/그리드 픽셀의 alpha 채널을 직접 검사해 확인 — `min_a=255, max_a=255`).
  - 원인 추정: 이미지 생성 모델이 "투명 배경"을 요청받았을 때, 실제 알파 채널 대신 PNG 뷰어의 체커보드 표시 관습을 그림 내용 자체로 그려버리는 실패 모드(생성 시 `remove_background` 후처리를 거치지 않은 10장에서만 발생, 후처리를 거친 2장은 정상).
  - **재현 → 원인 → 최소 수정**: 10장을 `media_upload`→`remove_background`→`jobs_wait`로 재처리해 실제 알파 채널을 만들고, 결과를 원본 해상도 그대로(리사이즈 없이, 기존 파일과 동일 크기) 덮어씀. 처리 후 PIL로 전 파일의 alpha 분포를 재검사(투명 비율 42~85%, 불투명 비율 12~54% — 정상적인 실루엣 컷아웃 패턴)해 확인, Expo 웹 재빌드 + Playwright 스크린샷으로 체커보드가 완전히 사라졌음을 육안 확인.
  - 크레딧 비용: `remove_background` 10회(배경 제거는 이미지 생성보다 저렴 — 정확한 크레딧 차감분은 `balance` 조회로 세션 시작 62.8 crd 확인, 별도 상세 차감 로그는 추적 안 함).
- 이번 수정 중 아주 미세한 잔여 아티팩트 하나를 발견: `mountain_1.png`의 안개 낀 가장자리(원래 의도적으로 반투명하게 그려진 부분)에 4배 확대해야 겨우 보이는 옅은 체커 얼룩이 남아있음(프레셔 피스톤 헤드 뒤쪽). 실제 게임 해상도에서는 하늘색과 거의 구분되지 않아 육안으로 인지 불가능한 수준 — 재생성/재처리하지 않고 기록만 남김. 다음에 산 배경을 다시 만질 일이 있으면 함께 손볼 것.

### 검증
- `npx tsc --noEmit`: 통과(컴포넌트 재작성 직후, 그리고 에셋 교체 후 모두).
- Expo 웹 빌드(`expo export --platform web`) + Playwright로 시작 화면, 게임 화면(기계 상태), 자동 이동 후 자연(wild) 상태 전환 화면, 게임오버 화면까지 스크린샷 확인 — 콘솔 에러/pageerror 0건.
- 자연 상태 전환 화면에서 배경의 자연 장식(나무/바위 일러스트)이 기계 장식(기어 타워)과 정확히 크로스페이드되는 것을 확인(기존 650ms `wildAnim` 로직 그대로 재사용됨을 재확인). 자연 플랭크(덩굴 짜임)도 타일 반복이 자연스럽게 이어지는 것을 확인.

### 영향
- 게임플레이 로직 영향 없음 — 이번 세션에서 건드린 파일은 전부 `src/components/*View.tsx`, `Background.tsx`, `PlatformView.tsx`(렌더링 전용) + `assets/**`(이미지 파일)뿐이며, `physics.ts`/`level.ts`/충돌 판정/밸런스 수치는 전혀 수정하지 않음.

### 남은 문제 (다음 세션 인계)
- `mountain_1.png` 안개 가장자리의 미세 체커 잔여물(위 참고) — 육안 인지 불가 수준이라 낮은 우선순위.
- `CLAUDE_VISUAL_QUALITY_OVERHAUL.md`에 명시된 나머지 범위(HUD 아이콘 자체를 일러스트로 교체, 레벨 진행 배너, 타이포그래피/색보정 최종 폴리싱)는 아직 미착수.
- 실기기 Expo Go 테스트 — 계속 미실시.
- 이번에 발견한 "생성 모델이 투명 배경 대신 체커보드 무늬를 그대로 그리는" 실패 모드는 앞으로 투명 배경이 필요한 에셋을 생성할 때마다 재발할 수 있음 — **다음부터는 생성 직후 반드시 `remove_background` 후처리를 거치고, Read 도구 미리보기만 믿지 말고 PIL 등으로 실제 alpha 채널 분포를 코너뿐 아니라 그리드 샘플링으로 검사하는 절차를 표준화할 것.**

### 결정 사항
- 위 버그 수정은 "이미지 자체가 잘못됐다"는 근본 원인 수정이므로, 시각 자산 관련 다음 작업(HUD 아이콘 등)을 시작하기 전 이 교훈을 우선 적용한다.

## 2026-08-27 (16) — 캐릭터/몬스터/배경 2차 전면 재생성 + 배경 타일링 버그 수정

### 목표
- 사용자가 (15)의 결과물을 직접 스크린샷으로 보고 "캐릭터 디자인부터 맵까지 너무 쓰레기같다"고 직접 피드백. 참고 이미지를 다시 제시하며 "컨셉 유지 안 해도 됨, 퀄리티만 올려줘, 예시랑 똑같아도 되니까"라고 명시적으로 우선순위를 지정 — 기존 시각 컨셉(Sprout/코그마이트 등의 정확한 형태) 보존보다 참고 이미지 수준의 렌더링 퀄리티를 우선하라는 지시.

### 변경
1. **캐릭터/몬스터 6종 전면 재생성**: Sprout, 코그마이트, 스포어 스프라이트, 바이오 코일, 프레셔 피스톤, 스팀 블로어를 참고 이미지(하나만)를 스타일 레퍼런스로 삼아 전부 다시 생성. 기존 컨셉(잎사귀 머리/기어창 몸통, 6족 곤충형 등)은 정체성만 유지하고 렌더링 방식을 프로토타입 수준의 평면적 벡터에서 방향성 조명·리치 셰이딩이 있는 페인터리 스타일로 전환. 플레이어는 정면 아이콘 포즈 → 3/4 측면 러닝 포즈로 변경(사이드스크롤러에 맞게).
   - 스포어 스프라이트 1차 생성 실패 사례: 프롬프트가 참고 이미지를 너무 강하게 따라가 "참고 이미지 콜라주 전체(다른 캐릭터·UI·한글 텍스트 포함)"를 그대로 재현해버림 — "다른 캐릭터/UI/콜라주/텍스트를 포함하지 말 것"을 명시한 프롬프트로 1회 재생성해 해결.
2. **배경 2종 전면 재생성**: 기존의 "구름 2장 + 산 2장 + 장식 2장을 조합"하던 다중 레이어 방식을 버리고, 기계/자연 상태별로 하나의 완성된 풍경 일러스트(원경 산 + 중경 공장/숲이 한 그림에 다 들어있는 "히어로 샷")를 각각 1장씩 생성해 `scene_mechanical.png`/`scene_wild.png`로 교체. `Background.tsx`를 재작성해 `Cloud`/`Mountain`/`GearDecor`/`VineDecor` 컴포넌트를 전부 제거하고 이 두 장을 크로스페이드하는 구조로 단순화(650ms `wildAnim` 크로스페이드 로직 자체는 그대로 재사용).
3. **플랫폼 2종 재생성**: 리벳 박힌 금속 그레이팅(기계) / 이끼 낀 흙+뿌리+꽃(자연) 타일을 참고 이미지 수준으로 재생성. `PlatformView.tsx`는 파일 경로가 동일해 코드 변경 없이 새 그림으로 교체됨.
4. **투명 배경 처리**: 캐릭터/몬스터 6종 + 플랫폼 2종(총 8장)은 (15)에서 확립한 "생성 → 업로드 → `remove_background` → alpha 채널 그리드 샘플링으로 검증" 절차를 처음부터 적용해 재발 없이 진행. 배경 2장은 애초에 투명이 필요 없는 풀블리드 이미지라 배경 제거를 거치지 않음.
5. **`PlayerView.tsx`**: `VISUAL_SIZE`를 52→58로 소폭 상향 — 새 러닝 포즈 그림이 팔다리가 벌어진 구도라 이전보다 사각형 안에서 차지하는 비율이 낮아진 것을 보정(주석 갱신).

### 발견한 버그와 수정 (배경 타일링)
- 배경을 히어로 샷 1장으로 교체한 뒤 처음 렌더링했을 때, 화면에 **똑같은 산 그림이 나란히 2장 겹쳐 보이는** 문제 발생. 원인은 타일 폭을 이미지의 자연스러운 가로세로비(`viewportHeight × aspect ≈ 394px`)로 계산했는데, 실제 게임 뷰포트 폭(~900px)보다 훨씬 좁아서 한 화면에 타일이 2장 이상 동시에 보였기 때문(레벨 전체를 기준으로 타일 반복 "빈도"를 줄이는 것과, 한 화면 안에 타일이 "몇 장 동시에" 보이는지는 별개 문제였음).
  - **수정**: 타일 폭을 이미지의 원래 비율이 아니라 **뷰포트 폭 자체**로 설정하고 `resizeMode="cover"`로 크롭되게 함 — 한 화면엔 항상 타일 1장만(경계를 넘나들 때만 2장) 보이도록 구조 변경. 이를 위해 `GameScreen.tsx`에서 이미 계산되어 있던 `viewportWidth`를 `Background`에 새 prop으로 전달.
  - 추가로 원경 레이어의 스크롤 배율을 0.25 → 0.15로 낮추고(진짜 먼 산은 거의 안 움직인다는 사실감), 타일 컨테이너 폭도 레벨 전체 폭이 아니라 "카메라가 실제로 스크롤할 수 있는 범위(뷰포트 폭 + 레벨 폭×배율)"로 한정해 레벨 전체를 통틀어 이음매가 보이는 횟수 자체도 최소화(기존 로직은 원경 레이어 컨테이너를 레벨 전체 폭으로 잡아 필요 이상으로 많은 타일을 만들고 있었음 — 실제 보이는 범위 밖의 타일까지 반복 생성하던 낭비/버그).

### 검증
- `npx tsc --noEmit`: 통과.
- Expo 웹 빌드 + Playwright로 시작 화면 → 기계 상태 게임 화면 → 자연 상태로 자동 전환된 화면 → (봇이 코그마이트에 부딪혀 사망) 게임오버 화면까지 스크린샷 확인. 이동+점프+대시 40사이클 동안 콘솔 에러/`pageerror` 0건.
- 배경 이음매(동일 그림 중복 노출) 문제가 수정 후 스크린샷에서 사라진 것을 육안으로 확인. 자연 상태 크로스페이드, 새 플랫폼 타일링(금속 그레이팅/이끼 뿌리)도 정상 동작 확인.
- 새로 생성한 8장(캐릭터/몬스터 6 + 플랫폼 2) 전부 PIL로 alpha 채널 그리드 샘플링해 정상 투명도(12~76% 투명 비율) 확인 — (15)의 버그가 재발하지 않았음을 재확인.

### 영향
- 게임플레이 로직 영향 없음 — 변경 파일은 `Background.tsx`(렌더링), `PlayerView.tsx`(시각 크기 상수), `GameScreen.tsx`(prop 전달 한 줄 추가)뿐이며 물리·충돌·레벨 데이터는 그대로.
- `PlatformView.tsx`, `SporeSpriteView.tsx`, `BioCoilView.tsx`, `PressurePistonView.tsx`, `SteamBlowerView.tsx`, `EnemyView.tsx`는 코드 변경 없이 동일 파일 경로의 새 이미지만으로 자동 갱신됨(경로가 같으므로).

### 남은 문제 (다음 세션 인계)
- 자동 플레이 봇이 이번에도 레벨 초반에 코그마이트에게 부딪혀 일찍 죽어, 스포어 스프라이트·바이오 코일·프레셔 피스톤·스팀 블로어가 새 그림으로 실제 화면에 렌더링되는 모습은 개별 이미지 파일로만 확인했고 인게임 스크린샷으로는 확인하지 못함(각 컴포넌트의 `VISUAL_SCALE` 수치는 새 이미지의 여백 비율이 이전과 비슷해 보여 그대로 두었으나, 실기기/수동 플레이 시 크기감을 재확인할 것).
- HUD 아이콘(코인/체력/기어) 자체를 일러스트로 교체하는 작업은 아직 미착수 — `CLAUDE_VISUAL_QUALITY_OVERHAUL.md`의 남은 범위.
- 사용자가 지적한 "카메라 스케일/캐릭터가 화면에서 작아 보이는 문제"는 이번 라운드의 우선순위에서 사용자가 명시적으로 제외했으므로 손대지 않음 — 필요시 별도 요청 시 진행.
- 실기기 Expo Go 테스트 — 계속 미실시.

### 결정 사항
- 참고 이미지 하나를 모든 에셋의 공통 스타일 앵커로 사용하는 방식이 이전 세션의 "레퍼런스 없이 절차적 요소 재사용"보다 훨씬 일관된 결과를 냈다 — 다음에 추가 에셋(HUD 아이콘 등)을 만들 때도 같은 참고 이미지를 계속 재사용해 스타일 통일성을 유지할 것.

## 2026-08-27 (17) — 맵 길이 연장 (Area 3)

### 목표
- 사용자가 "포탈 타고 보스 만나는" 엔딩을 계획 중이며, 그 전 단계로 맵 길이부터 늘려달라고 요청(그림 작업 없이 가능한 부분). `src/game/level.ts`에 새 구간(Area 3)을 추가.

### 변경
- `groundSegments`에 3개 세그먼트 추가: `[4750,5300]`, `[5450,6150]`, `[6220,6800]` (기존 마지막 구간 `[4030,4600]` 뒤).
- 신규 플랫폼 p17~p24(8개), Bloom Shift 존 1개(`bloom-area3`+`shift-area3`), 그래플 포인트(`root3`), 코그마이트 2마리(`e8`,`e9`), 스포어 스프라이트 1마리(`spore4`), 프레셔 피스톤 1개(`piston3`), 체크포인트 2개(`4750`,`5600`), 코인 11개 추가.
- `worldWidth` 4700→6900, 깃발 위치 4620→6820으로 이동. 기존 콘텐츠(좌표·수치)는 전혀 건드리지 않고 전부 뒤에 추가만 함.

### 검증 (순수 로직 시뮬레이션)
- `npx tsx`로 `stepGame`을 직접 호출하는 스크립트를 만들어 검증(이전 세션에서 확립한 "짧은 상태 변화는 스크린샷보다 순수 로직 시뮬레이션이 빠르고 확정적" 방식을 레벨 전체 통과 여부 검증에도 적용).
- 1차: 몬스터 포함 상태로 레벨 처음부터 자동 이동(우측+점프 계속 입력) → 기존 몬스터 밀집 구간(스팀 블로어/바이오 코일, x≈3000)에서 사망. 이건 이 세션에서 만든 Area 3와 무관하게 원래도 그랬던 것(멍청한 봇이 몬스터를 못 피하는 건 여러 세션에 걸쳐 반복 확인된 한계) — Area 3 자체를 검증하려면 몬스터를 배제한 별도 테스트가 필요하다고 판단.
- 2차: 몬스터를 전부 제거한 레벨 사본에서 플레이어를 Area 3 시작 지점(x=4610)에 스폰해 순수 플랫포밍만 테스트 → 세 번째 웅덩이(x=6100~6250 부근)에서 반복적으로 추락 실패.
- **원인 분석**: 실패 지점 직전의 발판 `p22`(지면보다 120px 높음)에서 뛴 뒤 착지 지점을 못 찾고 그대로 죽음. 원인은 테스트 봇이 "점프 버튼을 매 프레임 누른 상태로 유지"하는 방식이라 발판에 착지하자마자 즉시 다시 튀어오르는 "연속 방방 뛰기"가 발생했고, 그 결과 예측 불가능하게 높고 먼 궤적이 만들어져 다음 발판을 통째로 지나쳐버린 것으로 보임(실제 플레이어가 매 프레임 점프를 누르고 있는 일은 드묾 — 테스트 봇 특유의 아티팩트일 가능성이 높음).
- **최소 수정**: 안전 마진을 위해 (1) 세 번째 웅덩이 폭을 150px→70px로 축소, (2) `p22` 높이를 지면+120px→+90px로 낮춤 — 두 조정 모두 레벨 내 기존에 이미 쓰이던 값 범위 안(예: 기존 웅덩이 중 가장 좁은 것도 120px, 다른 발판들도 60~110px 높이 사용) 이라 새로운 난이도 기준을 만든 게 아니라 기존 패턴에 맞춘 것.
- 수정 후 재시뮬레이션 → Area 3 전체를 통과해 깃발(x=6820)에 도달, `phase==='win'` 확인.
- `npx tsc --noEmit` 통과, Expo 웹 빌드 + Playwright로 시작 화면~게임 화면 콘솔 에러 0건 확인(신규 콘텐츠는 전부 기존 렌더링 컴포넌트를 재사용하는 데이터 추가라 별도 시각적 버그 위험은 낮다고 판단, 픽셀 단위 스크린샷 검증은 생략).

### 영향
- 기존 콘텐츠(좌표·적·코인·체크포인트·깃발 위치 등)는 전혀 수정하지 않고 전부 추가만 함 — 기존 구간 플레이 경험에는 영향 없음.
- 신규 구간은 기존 몬스터/이펙트/배경 렌더링 컴포넌트를 그대로 재사용(신규 아트 에셋 불필요, 크레딧 소모 없음).

### 남은 문제
- 이번 검증은 "몬스터 없이 플랫포밍만" + "순수 로직"으로 진행했다. 몬스터 포함 실제 난이도(코그마이트 2마리, 스포어 스프라이트, 프레셔 피스톤 배치가 공정한지)는 아직 사람이 직접 플레이해서 확인 못함.
- 사용자가 요청한 "1스테이지 클리어 후 포탈로 보스 조우" 기능은 아직 미착수(크레딧 충전 대기 중) — 포탈 트리거·스테이지 전환·보스 아레나는 이 맵 길이 연장과 별개로 추가해야 함. 지금 깃발(도착 지점)은 그대로 두었으므로, 나중에 포탈로 교체하거나 깃발 직전에 포탈을 추가하는 방식으로 이어붙이면 이번 작업을 다시 할 필요는 없음.

### 결정 사항
- 레벨 데이터처럼 "충돌/통과 가능 여부"가 중요한 변경은 스크린샷보다 `stepGame`을 직접 호출하는 순수 로직 시뮬레이션으로 검증하는 게 훨씬 빠르고 확정적이었다 — 다음에 레벨 구조를 바꿀 때도 이 방식을 우선 사용할 것. 단, 봇이 "매 프레임 점프 유지" 같은 비현실적 입력을 하면 실제 플레이보다 어렵게(혹은 다르게) 나올 수 있다는 점을 감안해서 해석할 것.

## 2026-08-27 (18) — 포탈 + 보스 조우 로직 추가 (아트 없이 로직만)

### 목표
- 사용자가 "1스테이지를 깨면 포탈을 타고 들어가서 보스를 만나는" 엔딩을 요청. 크레딧이 부족한 상황이라, 이번엔 새 아트 없이(=크레딧 소모 없이) 게임 로직/구조만 먼저 구현. 보스 캐릭터는 나중에 크레딧 충전 후 완전히 새로운 디자인으로 그릴 예정(사용자가 명시적으로 "완전히 새로운 보스 캐릭터 디자인" 선택).

### 변경
1. **`src/game/types.ts`**: `Portal`(트리거 전용, 충돌 없음), `Boss`(`BossPhase`: `idle`→`telegraph`→`attack`→`vulnerable`→반복) 타입 추가. `Level`에 `portal`/`boss` 필드, `GameState`에 `boss`/`portalActivated` 필드 추가.
2. **`src/game/constants.ts`**: `PORTAL_WIDTH/HEIGHT`, `BOSS_WIDTH/HEIGHT/HP`(90×110, HP 5 — 기존 5대 몬스터 로스터에 없는 완전 신규 콘텐츠라 참고할 기존 수치가 없음, 스팀 블로어(48×54, HP 3)보다 확실히 크게 잡음), `BOSS_IDLE/TELEGRAPH/ATTACK/VULNERABLE_DURATION`, `BOSS_ATTACK_RANGE/KNOCKBACK` 추가.
3. **`src/game/level.ts`**: 기존 레벨 끝(x=6800)에 웅덩이 없는 평평한 "보스 아레나" 구간을 이어붙임(`worldWidth` 6900→7800). 포탈(x=6830, 아레나 진입부)과 보스(x=7300, 아레나 중간)를 배치. 기존 깃발(`flag`)은 아레나 끝(x=7650)으로 옮기되 **더 이상 승리 조건이 아닌 장식용**으로 격하(아래 참고). 보스 근처에서 죽어도 처음부터 다시 걷지 않도록 체크포인트 1개(x=6830) 추가.
4. **`src/game/physics.ts`** — 보스 전투 로직:
   - `stepBoss`: `idle`(1.5s, 안전) → `telegraph`(1.0s, 공격 예고) → `attack`(0.6s, 피격 판정) → `vulnerable`(1.5s, 스톰프 가능) → 반복. 플레이어 위치와 무관하게 고정 타이머로 순환(스팀 블로어와 동일한 "패턴을 외워서 대응" 방식).
   - **보스는 살아있는 동안 충돌 플랫폼 목록에 포함되는 유일한 몬스터** — 기존 몬스터(코그마이트/스포어 스프라이트 등)는 전부 "그냥 통과되는 히트박스"였지만, 보스는 물리적으로 길을 막아야 "만나서 싸운다"가 성립하므로 이번에 처음으로 몬스터를 고체 충돌체로 취급하는 코드 경로를 추가함(`platforms` 배열에 `prev.boss.alive`일 때만 포함).
   - 스톰프 판정은 기존 "겹침(rectIntersect) 여부"가 아니라 **"이번 프레임에 이 보스 위에 착지했는가"**(`landedOnBoss` 플래그, 수직 충돌 루프 안에서 `'hp' in plat`으로 구분)로 감지 — 보스가 고체 충돌체라 착지 시 이미 겹침이 아니라 밀착 상태로 정리되기 때문에, 기존 스팀 블로어 방식(겹침 감지)을 그대로 쓸 수 없어서 다른 방식을 새로 만듦.
   - `vulnerable` 상태가 아닐 때 보스 위에 착지하면 그냥 튕겨나갈 뿐 피해 없음(스팀 블로어의 "뚜껑 닫힘" 패턴과 동일).
   - **승리 조건 변경**: 기존 "깃발(`level.flag`)에 닿으면 승리" → "보스를 처치하면(`boss.alive === false`) 승리"로 교체. 깃발은 보스가 길을 막고 있어 어차피 살아있는 동안 못 지나가므로 별도 관문 장치(사라지는 벽 등) 없이도 "보스를 이겨야만 진짜 결말"이 자연스럽게 성립함.
   - 포탈은 충돌 로직이 전혀 없고, 플레이어가 처음 지나갈 때 이펙트 로그에 1회성 `impact` 이펙트만 남김(`portalActivated` 플래그로 1회 제한).
5. **`src/components/BossView.tsx`, `PortalView.tsx`(신규)**: 아직 아트가 없어서 절차적 도형으로 구현. 보스는 페이즈별로 색이 뚜렷하게 바뀌고(대기=고유색, 공격=위험색 깜빡임, 무방비=강조색) HP 핍을 표시 — "예고 부족"을 최우선으로 피하기 위해 아트 유무와 무관하게 페이즈 구분을 먼저 시각적으로 명확히 함. 나중에 실제 그림이 나오면 `BossView`의 배경색 블록만 `<Image>`로 교체하면 되고 페이즈 반응 로직은 그대로 재사용 가능하도록 분리해둠. `theme.ts`에 `portalGlow`/`bossAccent` 계열 색상 신규 추가(기존 팔레트에 보라·짙은 자주 계열이 없어서).

### 검증 (순수 로직 시뮬레이션)
- `npx tsc --noEmit` 통과.
- `stepGame`을 직접 호출하는 포커스 테스트 4종 작성 후 실행, 전부 통과:
  1. 보스가 살아있는 동안 실제로 고체 벽처럼 플레이어를 막는지 (통과 시도 30틱 후 플레이어 x가 보스 왼쪽 경계에 정확히 정지 — `7268 = 7300 - player.width(32)`).
  2. `attack` 페이즈 중 접촉 시 실제로 생명력이 줄어드는지(3→2).
  3. `vulnerable` 페이즈 중 스톰프 5회로 HP 5→0, `alive:false`, `phase:'win'` 전환까지 확인.
  4. 포탈을 넘는 첫 프레임에만 `portalActivated`와 `impact` 이펙트가 1회 발생하는지 확인.
- Expo 웹 빌드 + Playwright로 시작 화면~기본 이동까지 콘솔 에러 0건 확인(아레나까지 실제로 도달하는 스크린샷은 이번에도 봇의 몬스터 회피 불가 한계로 생략 — 순수 로직 검증으로 대체).

### 영향
- 기존 몬스터(코그마이트~스팀 블로어)의 충돌 방식은 전혀 바뀌지 않음 — 이번에 추가한 "고체 충돌 몬스터" 경로는 보스 전용이며 `prev.boss.alive` 조건으로만 활성화됨.
- 기존 깃발 도달 승리 조건을 보스 처치로 교체한 것은 **의도된 사양 변경**(사용자가 명시적으로 요청한 "보스를 만나야 끝난다" 요구사항)이지, CLAUDE.md가 금지하는 "임의 수치 변경"이 아님 — 다음 세션 담당자가 오해하지 않도록 기록.
- Area 3 자체(플랫폼/웅덩이/코인 등, 세션 (17))는 전혀 건드리지 않음.

### 남은 문제 (다음 세션 인계)
- **보스 아트 미착수**: 크레딧 충전 대기 중. `BossView.tsx`는 실제 그림이 와도 로직 변경 없이 바로 갈아끼울 수 있는 구조로 만들어둠.
- **실제 플레이로 보스전 난이도 검증 안 됨**: HP 5, 페이즈 지속시간(1.5/1.0/0.6/1.5초) 전부 "첫 버전" 추정치이며, 사람이 직접 붙어보고 너무 쉽거나 어려우면 조정 필요. 자동 봇은 타이밍을 맞춰 스톰프할 수 없어서 검증 불가.
- 보스 공격은 현재 "넓은 범위 접촉 데미지 1종"뿐 — 나중에 여러 공격 패턴(예: 돌진, 원거리)을 추가할지는 아트 작업과 함께 논의 필요.
- 포탈은 순수 연출용 트리거라 실제 "레벨 전환" 없이 같은 연속 월드로 이어짐 — 나중에 화면 전환 연출(암전 등)을 추가하면 더 그럴듯해지겠지만, 이번엔 최소 구현 원칙에 따라 생략.

### 결정 사항
- 승리 조건을 깃발 접촉에서 보스 처치로 바꾸는 대신, "보스를 물리적 장애물로 만들어 우회 불가능하게 하기"라는 더 간단한 방법을 선택함 — 사라지는 벽이나 별도 게이트 로직을 새로 만드는 것보다 기존 충돌 시스템에 보스를 끼워 넣는 쪽이 훨씬 적은 코드로 "반드시 싸워야 함"을 보장함. 다음에 비슷한 "보스 필수 조우" 요구가 생기면 이 패턴(몬스터를 `platforms` 배열에 조건부로 포함)을 재사용할 것.

## 2026-08-27 (19) — Codex 협업 체계 도입 + 보스전 밸런스 검증 (반응형 봇 시뮬레이션)

### 목표
- 사용자가 Codex와 공동개발을 시작하기로 함. `CLAUDE_CODEX_COLLAB_PROMPT.md`를 프로젝트 루트에 배치하고 `AI_COLLAB.md` 상태판을 신설, 역할을 고정(Codex=디자인 총괄·UI 구현 책임자, Claude=기능 구현·시스템 통합 책임자).
- 첫 작업으로 **보스 캐릭터 아트 제작**을 Codex에게 배정(`BossView.tsx` placeholder 교체). Claude는 그 사이 로직 쪽에서 "보스전 난이도 미검증"(세션 (18)에서 남긴 미해결 항목) 문제를 실제로 검증하기로 함.

### 변경
- 코드 변경 없음(순수 검증/문서화 세션). `CLAUDE_CODEX_COLLAB_PROMPT.md`, `AI_COLLAB.md` 신규 추가(협업 문서).

### 검증 — 보스전 밸런스 (반응형 봇 시뮬레이션)
- 세션 (18)에서 "자동 봇은 스톰프 타이밍을 못 맞춰 검증 불가"로 남겨뒀던 부분을, **매 프레임 `boss.phase`를 직접 읽고 반응하는 스크립트 봇**(고정 타임라인이 아니라 실제 게임 상태 기반 반응형 로직)을 임시로 작성해 검증함(`stepGame`을 직접 호출하는 pure-logic 시뮬레이션, 세션 관례 그대로).
- 봇 전략: `telegraph`/`attack` 중에는 보스 공격 판정 범위(`BOSS_ATTACK_RANGE=100px` 양쪽) 밖으로 후퇴(플레이어가 보스 좌/우 어느 쪽에 있는지 판별해 방향 결정), `vulnerable` 중에는 보스 쪽으로 달려가 접촉 시 점프.
- **결과**: 90초 시뮬레이션 캡 안에서 **약 23초 만에, 생명 손실 0(3→3 유지)으로 보스 HP 5를 전부 스톰프해 승리(`phase==='win'`)**. 사이클(4.6초)당 평균 1회 스톰프 성공, 5사이클 만에 처치 — 설계 의도("패턴을 외워서 대응")대로 작동함을 확인.
- 부가 확인: `telegraph` 지속시간 1.0초 동안 공격 판정 경계(보스 가장자리에서 100px)까지 후퇴하는 데 필요한 이동 시간은 약 0.45초(`MOVE_SPEED=220px/s` 기준)로, 실제 필요 시간의 2배 이상 여유가 있어 "공격 예고 부족" 문제(CLAUDE.md 18장 최우선 개선 대상)는 없다고 판단.
- 최초 시도한 단순 봇(좌측 후퇴만 가정)은 스톰프 바운스로 플레이어가 보스 우측으로 튕겨 나간 뒤에도 "왼쪽으로 후퇴"를 계속 시도해 오히려 공격 범위로 되돌아가 생명을 잃는 결과가 나왔었음 — 이는 **봇 스크립트 자체의 결함**(좌/우 어느 쪽에 있는지 고려 안 함)이었고, 좌우 판별 로직을 추가하자 곧바로 무피해 클리어로 해결됨. 실제 사람 플레이어라면 자신이 보스의 어느 쪽에 있는지 당연히 인지하므로 게임 자체의 결함이 아니라고 판단.
- `npx tsc --noEmit` 통과(변경 없음이지만 재확인).

### 영향
- 코드/수치 변경 없음. 세션 (18)에서 "첫 버전 추정치"로 남겨뒀던 HP 5 / 페이즈 지속시간(1.5/1.0/0.6/1.5초) 값이 최소 하나의 합리적 플레이 전략으로 무리 없이 클리어 가능함을 확인했으므로, **현재로선 수치 변경 불필요**로 결론.
- 다만 이건 어디까지나 "완벽한 정보를 가진 스크립트 봇" 기준 검증이며, 실제 사람이 터치 컨트롤로 반응 시간·입력 지연을 감안했을 때도 동일하게 느껴지는지는 여전히 실기기/실플레이로 확인 필요(아래 남은 문제 참고). 이번 검증은 "이론적으로 클리어 가능한 설계인가"에 대한 답이지, "사람이 하기에 재미있고 공정한가"에 대한 최종 답은 아님.

### 남은 문제 (다음 세션 인계)
- 실제 사람 플레이(터치 컨트롤, 반응 지연 포함) 기준 체감 난이도는 여전히 미검증 — 이번 봇 검증으로 "이론적 클리어 가능성"은 확인됐지만 사람이 느끼는 재미/공정성은 별개 문제.
- 보스 아트는 Codex에게 배정(진행 중, `AI_COLLAB.md` CODEX ACTIVE 참고).

### 결정 사항
- 협업 체계: Codex=디자인/UI, Claude=로직/시스템으로 역할을 고정하고, 공용 파일(`GameScreen.tsx`, `types.ts`, 크기 관련 상수)은 즉시 분리하지 않고 필요 시점에 조율하기로 함(과도한 사전 리팩터링 방지 원칙과 일치).
- 타이밍이 중요한 몬스터/보스 로직의 난이도 검증에는 "고정 스크립트 봇" 대신 "매 프레임 실제 게임 상태를 읽고 반응하는 봇"을 쓰는 게 훨씬 신뢰도 높은 결과를 준다는 걸 확인 — 다음에 비슷한 반응형 전투 로직(예: 향후 보스 공격 패턴 추가)을 검증할 때도 이 방식을 우선 사용할 것.

### 검증 — 화면 비율/레이아웃 (CLAUDE.md 19장 Phase 1 재확인)
- CLAUDE.md 19장이 명시한 "게임 영역이 화면 중앙에 작게 들어가고 주변에 빈 공간이 큰 문제"가 실제로 남아있는지 `npx expo export -p web`로 빌드 후 정적 서버(`http-server`)에 올리고 Playwright로 6가지 화면 크기(좁은 세로폰 360×740/390×844, 태블릿 세로 810×1080, 가로폰 844×390, 짧은 가로 700×320, 데스크톱 1440×900)에서 스크린샷 확인.
- **결과**: 좁은 세로폰·가로 화면들은 스테이지가 컨트롤/HUD 사이 공간을 꽉 채움. 그러나 **태블릿 세로(810×1080)에서 스테이지 아래 큰 민트색 빈 공간 확인**(가용 높이 854px 중 실제 사용 660px, 약 194px/22% 낭비). 1440×900 데스크톱에서도 같은 원인으로 소폭(~2%) 낭비.
- **원인**: `src/screens/GameScreen.tsx`의 `MAX_STAGE_SCALE=3` 클램프가 세로로 크거나 큰 화면에서 필요한 스케일(예: 810×1080 기준 `854/220≈3.88`)보다 낮은 상한을 걸고 있음 — 클램프만 없으면 코드는 이미 가용 높이를 정확히 채우도록 스케일을 계산하는 로직이라, 값 하나만의 문제로 보임.
- **처리**: `GameScreen.tsx`는 AI_COLLAB.md 기준 Codex 소유(레이아웃/스타일)라 직접 수정하지 않고, 재현 방법·원인·제안 수정안을 `AI_COLLAB.md`의 REVIEW REQUESTS/ISSUES에 [S2]로 기록해 Codex에게 넘김.

## 2026-08-27 (20) — 보스 Verdigris Rootwarden 아트 제작 및 적용

### 목표
- `BossView.tsx`의 단색 사각형 placeholder를 기존 몬스터 화풍과 통일된 신규 미니보스 일러스트로 교체한다.
- 보스 로직·충돌 크기·HP·페이즈 타이밍·HP 핍은 변경하지 않고, 4개 페이즈를 그림만으로도 즉시 구분 가능하게 만든다.

### 변경
1. **신규 보스 디자인**: 황동·녹청 갑주, 거대한 뿌리 팔, 단안형 황금 눈, 유리 화로 속 수액 코어, 기어 왕관과 살아 있는 가지를 결합한 `Verdigris Rootwarden`을 제작. 스팀 블로어의 확대판이 아닌 넓고 위협적인 독립 실루엣으로 설계함.
2. **4페이즈 전용 아트** (`assets/sprites/boss_rootwarden_*.png`):
   - `idle`: 안정된 황금 코어, 직립 대기 자세.
   - `telegraph`: 몸을 웅크리고 발톱을 모으며 눈·코어가 적색으로 과열, 증기와 경고 불꽃 발생.
   - `attack`: 양팔을 크게 펼친 타격 자세, 백열 코어와 고압 증기 분출.
   - `vulnerable`: 어깨와 팔이 처지고 흉부 해치가 열려 민트색 자연 코어와 작은 꽃이 노출.
3. **`src/components/BossView.tsx`**: 기존 페이즈 반응 로직과 telegraph 점멸, HP 핍 마크업을 유지하고 단색 `Animated.View`를 페이즈별 `Animated.Image`로 교체. `resizeMode="contain"`으로 기존 90×110 논리 크기 안에 렌더링하며 `BOSS_WIDTH`, `BOSS_HEIGHT`, `BOSS_HP`는 변경하지 않음.
4. 최초 생성본의 체크무늬 투명 배경이 실제 픽셀로 포함된 문제를 웹 렌더링에서 발견하고, 네 장 모두 실제 알파 투명 PNG로 배경 추출 후 교체함.

### 에셋 출처·라이선스
- 제작 방식: Codex 내장 OpenAI 이미지 생성 도구를 사용한 자체 생성 아트. 외부 CC0/유료 에셋은 사용하지 않음.
- 스타일 참고: 저장소에 이미 포함된 `cogmite.png`, `bio_coil.png`, `steam_blower.png`만 내부 스타일 레퍼런스로 사용. 기존 캐릭터의 실루엣이나 구성은 복제하지 않음.
- 생성 스펙 요약: premium hand-painted 2D game sprite, whimsical steampunk × nature spirit, antique brass/copper/verdigris/moss palette, transparent cutout, readable at 90×110, phase-specific pose/core/steam changes, no text·UI·watermark·scenery.

### 검증
- `npx tsc --noEmit` 통과(에러 0).
- Expo 웹 실제 게임 화면에서 보스 컴포넌트, 페이즈 이미지, HP 5 핍이 렌더링되는 것을 확인.
- 렌더링 중 발견한 불투명 체크무늬 배경을 수정한 뒤 에셋을 실제 투명 배경 버전으로 교체.

### 영향
- Presentation Layer만 변경. `src/game/*`, 보스 물리·페이즈·HP·충돌·승리 조건은 변경 없음.
- 남은 검증: 사람이 보스 아레나까지 정상 플레이해 전투 난이도와 각 페이즈의 90×110 실전 가독성을 확인해야 함. 실기기 Expo Go 검증은 여전히 미수행.

### Claude 기술 검수 (통합/회귀 관점)
- `npx tsc --noEmit` 재확인 통과.
- `npx expo export -p web`로 재빌드 후 정적 서버에 올려 Playwright로 시작 화면 진입까지 콘솔 에러/리소스 로드 실패 0건 확인(신규 `require()` 경로 4개가 전부 정상 해석됨).
- PIL로 4장 전부 모서리 알파=0 / 중앙 알파≈253 확인 — Codex 로그의 "체크무늬 → 실제 투명 배경 교체" 조치가 실제로 반영됐음을 재확인.
- **신규 발견(S2)**: 보스 스프라이트 4장이 장당 2.3~2.4MB(총 ~9.4MB)로 기존 몬스터 스프라이트(100KB~397KB)의 6~24배. 원본 해상도(1150~1368px)가 실제 표시 크기(논리 90×110px, 최대 스케일 적용해도 ~440px대)에 비해 과도하게 큼 — 이 세션 초반 배경 이미지 렉 버그(1600×893px PNG를 훨씬 작게 표시)와 동일한 패턴. 에셋 파일은 Codex 소유라 직접 리사이즈하지 않고 `AI_COLLAB.md` REVIEW REQUESTS/ISSUES에 재현·원인·제안(다운스케일+최적화)을 기록해 Codex에게 요청함.
- 보스가 실제로 화면에 표시된 스크린샷은 레벨 x=7300 지점까지 자동 이동해야 하는데, 기존 자동 봇은 초반 몬스터 밀집 구간을 못 피해 도달 불가(세션 전반에 걸쳐 반복 확인된 한계) — 이번에도 시각적 스크린샷 확인은 생략하고 기술적 검증(타입체크/리소스 로드/알파 채널)으로 대체함. 최종 시각 확인은 사람 실플레이 또는 실기기 테스트 때 필요.

## 2026-08-27 (21) — 태블릿 세로 화면 스테이지 빈 공간 수정

### 목표
- Claude REVIEW REQUEST의 810×1080 태블릿 세로 화면 하단 빈 공간 문제를 Presentation Layer 안에서 해결한다.
- 게임 좌표·물리·입력·HUD/컨트롤 예약 높이는 유지한다.

### 변경
- `src/screens/GameScreen.tsx`의 `MAX_STAGE_SCALE`을 `3`에서 `4.5`로 완화했다. 810×1080 화면에 필요한 배율(약 3.88)이 기존 상한에 막히던 문제만 제거하는 최소 수정이다.
- 작은 가로 화면의 축소 하한(`MIN_STAGE_SCALE=0.6`)과 `VIEWPORT_HEIGHT`, 게임 로직, 카메라 계산은 변경하지 않았다.

### 검증
- `npx tsc --noEmit` 통과.

## 2026-08-28 (51) — 보상 캐시·탄약 HUD 프레젠테이션 연동 (Codex)

### 변경
- `TreasureCacheView`로 대시 전용 고목 상자와 화살 전용 봉인 꼬투리를 실제 레벨에 렌더링했다.
- `LootRevealView`는 캐시 개봉 후 Sunseeds/Arrow/Life/FLOW의 구체적인 결과를 짧게 떠오르게 표시한다.
- HUD에 `ARROWS 0/5` 형식의 탄약 표기를 추가하고, 탄약 0발일 때 ARROW 버튼을 잠김 외형으로 처리했다.
- Claude가 구현한 상태·수치·보상 결과만 소비했으며 게임 규칙 파일은 변경하지 않았다.

### 검증
- `npx tsc --noEmit` 통과.
- Expo 웹에서 시작→플레이 화면 로드와 기존 HUD/컨트롤 렌더링을 확인했다.
- Expo 웹 실제 렌더링 스크린샷 검증:
  - 810×1080 태블릿 세로: 기존 약 194px 민트색 빈 공간 제거, 스테이지가 HUD와 컨트롤 사이를 채움.
  - 390×844 세로폰: 기존 레이아웃 유지, 겹침·잘림 없음.
  - 844×390 가로폰: HUD·스테이지·컨트롤 분리 유지.
  - 1440×900 데스크톱: 스테이지 높이 충족, UI 침범 없음.
- 브라우저 콘솔 오류 0건.

### 영향
- Presentation Layer 상수 1개만 변경했으며 게임플레이 동작과 밸런스에는 영향 없음.
- 다음 UI 작업은 CLAUDE.md 19장 Phase 2의 HUD·컨트롤·메뉴 디자인 시스템 리빌드.

## 2026-08-27 (22) — 보스 시스템 엣지 케이스 점검 (CLAUDE.md 18장 체크리스트)

### 목표
- CLAUDE.md 18장이 요구하는 "신규 기능 단독 테스트만으로 완료 처리하지 않는다" 원칙에 따라, 포탈+보스 시스템(세션 (18))이 다른 시스템과 만나는 지점 중 아직 안 짚어본 조합을 코드 리딩 + pure-logic 시뮬레이션으로 점검.

### 점검 항목과 결과
1. **Root-Hook 그래플 × 보스(고체 충돌체)**: `physics.ts`에서 그래플 스윙 중(`grappledThisFrame`)에는 모든 플랫폼 충돌 처리 블록 자체를 건너뛰는 기존 구조(보스 도입 이전부터 있던 동작, 스윙 중엔 어떤 플랫폼과도 충돌하지 않음)를 확인. 보스 근처 유일한 그래플 포인트(`root3`, x=6110)는 보스(x=7300)와 1190px 떨어져 있고 그래플 사거리(`ROOTHOOK_RANGE=90`, 코그 장착 시 117)로는 전혀 닿지 않음 — 현재 레벨 배치상 그래플이 보스와 겹칠 방법이 없어 실질적 위험 없음(코드 확인만, 새 버그 아님).
2. **보스 공격 넉백 × 아레나 바깥 낙사 웅덩이**: 아레나(`[6800,7700]`)는 웅덩이 없는 평평한 방으로 설계됐고, `worldWidth=7800`이라 아레나 끝(7700) 이후 100px은 지면이 없음. 공격 판정 범위(`BOSS_ATTACK_RANGE=100`, 보스 우측 끝 기준 최대 x=7480) + 넉백(`BOSS_ATTACK_KNOCKBACK=420px/s`, 마찰 900px/s² 기준 이동거리 ≈98px)을 더해도 최대 도달 지점은 약 x=7578로, 낙사 구간(7700~)까지 122px 여유가 있어 넉백만으로 아레나 밖으로 떨어지는 경우는 없음(계산 확인, 새 버그 아님).
3. **보스전 도중 사망(추락사) → 체크포인트 부활 시 보스 진행도 유지 여부** (pure-logic 시뮬레이션으로 실측): 보스 HP를 3으로 설정하고 `vulnerable` 페이즈 중 플레이어를 강제로 낙사시킨 뒤 재시뮬레이션 → **사망/부활 후에도 `boss.hp`, `boss.phase`, `boss.timer`가 전혀 초기화되지 않고 그대로 유지됨**, 부활 지점은 아레나 입구 체크포인트(x=6830)로 정상 복귀. `applyHit()`가 플레이어 상태만 되돌리고 `boss` 객체는 건드리지 않는 기존 구조(불변 객체 아키텍처) 덕분에 별도 코드 없이 자연스럽게 성립.

### 결론
- 위 세 항목 모두 버그 없음 — 다만 3번은 명시적으로 검증되지 않았던 동작이라 실측으로 확인함.
- **판단**: "보스전 도중 죽어도 처음부터 다시 싸우지 않고 그 자리에서 이어감"은 CLAUDE.md 3장의 "체크포인트 부활 시 최근 체크포인트 유지" 원칙과 일치하는 player-friendly 동작으로 판단, 코드 수정 없이 현행 유지.
- 코드/수치 변경 없음(순수 검증). `npx tsc --noEmit` 통과(변경 없음, 재확인).

### 남은 문제
- 위 검증은 여전히 "완벽한 정보를 가진 로직 시뮬레이션" 기준이며, 사람이 실제로 보스전 도중 죽었다가 이어서 싸우는 경험이 직관적으로 이해되는지(예: "왜 보스 체력이 그대로지?"에 대한 시각적 피드백 여부)는 실플레이로만 확인 가능 — 현재 `BossView.tsx`의 HP 핍 UI가 이미 이 상태를 보여주므로 큰 문제는 아닐 것으로 예상되나 미확정.

## 2026-08-27 (23) — Rootwarden 보스 스프라이트 용량 최적화

### 목표
- Claude 기술 검수에서 발견된 보스 PNG 4장의 과도한 해상도·용량을 실제 표시 크기에 맞게 줄여 모바일 디코딩·메모리·렌더링 부담을 낮춘다.
- 캐릭터 디자인, 4페이즈 구분, 투명 배경을 그대로 유지한다.

### 변경
- `assets/sprites/boss_rootwarden_*.png` 4장을 LANCZOS 방식으로 긴 변 640px까지 다운스케일하고 PNG `optimize=True`, `compress_level=9`로 저장했다.
- 코드와 게임 로직은 변경하지 않았다.

### 결과
- attack: 1150×1368, 2263.7KB → 538×640, 577.8KB
- idle: 1150×1368, 2316.0KB → 538×640, 603.3KB
- telegraph: 1199×1312, 2321.5KB → 585×640, 649.9KB
- vulnerable: 1159×1356, 2365.8KB → 547×640, 612.2KB
- 전체 약 9.1MB → 약 2.4MB로 약 73% 감소.

### 검증
- 네 파일 모두 RGBA, 알파 범위 0~255 유지.
- 최적화본을 원본 크기로 재확대해 가시 영역을 비교한 평균 RGBA 차이: 5.34~7.58/255로 실제 표시 크기에서 식별하기 어려운 수준.
- Expo 웹 로드 및 브라우저 콘솔 오류 0건.
- `npx tsc --noEmit` 통과.

### 영향
- 이미지 디코딩·메모리·전송 부담 감소. `BossView.tsx`, 페이즈 로직, HP 핍, 충돌 크기에는 영향 없음.

## 2026-08-27 (24) — Phase 2 HUD·모바일 컨트롤 리빌드

### 목표
- 기존의 서로 분리된 둥근 HUD 배지와 원형 버튼 모음을 하나의 일관된 스팀펑크 계기판 언어로 정리한다.
- 입력·멀티터치 동작은 건드리지 않고 정보 위계와 조작 인지성을 개선한다.

### 변경
- `src/theme.ts`: HUD·컨트롤·메뉴에서 공유할 `uiPlate`, `uiPlateDeep`, `uiPlateEdge`, `uiPlateHighlight`, `uiTextMuted` 표면 토큰 추가.
- `Hud.tsx`: GEAR/LIFE/OVERDRIVE 영문 캡션을 추가하고 배지를 둥근 유리 캡슐에서 모서리가 절제된 금속 계기판으로 변경. 오버드라이브 게이지를 독립된 어두운 프레임 안에 배치해 밝은 배경에서도 대비 확보.
- `Controls.tsx`: 좌우 이동 버튼을 `MOVEMENT` 금속 하우징과 브리지로 연결해 하나의 컨트롤 모듈로 통합. 각 액션에 MOVE/HOOK/DASH/JUMP 보조 레이블을 추가해 기호만으로 의미를 추측하지 않도록 개선.
- 기존 responder, 멀티터치 좌표 측정, 콜백, 버튼 히트 영역은 변경하지 않음.

### 검증
- `npx tsc --noEmit` 통과.
- Expo 웹 390×844 세로 화면: HUD·게이지·5개 버튼 잘림/겹침 없음.
- Expo 웹 844×390 가로 화면: 좁은 스테이지와 컨트롤 밴드 분리 유지, 좌우 모듈과 액션 패드 정상.
- 브라우저 콘솔 오류 0건.

### 영향
- Presentation Layer만 변경. 게임 상태, 입력 판정, 멀티터치 처리, 물리·밸런스에는 영향 없음.
- 다음 단계는 시작 화면 및 게임오버·승리 오버레이의 이모지/프로토타입 표현 제거와 동일 디자인 시스템 적용.

## 2026-08-27 (25) — 비주얼 리부트 Vertical Slice 1: 새 Sprout과 프레임 애니메이션

### 목표
- 사용자가 제시한 밝고 프리미엄인 페인터리 2D 플랫폼 게임 레퍼런스에 맞춰, 기존 정적·이질적 주인공을 새 마스코트형 주인공으로 교체한다.
- 물리·입력·충돌을 바꾸지 않은 상태에서 실제 프레임 단위 이동 표현을 시작한다.

### 변경
1. `assets/sprites/sprout_v2/`에 새 Sprout 8프레임 PNG를 추가했다. 대기 2, 주행 4, 점프 1, 낙하 1 프레임이며 모두 실제 알파 투명 RGBA다.
2. 새 디자인은 큰 머리·짧은 몸의 친근한 식물+태엽 모험가로, 잎 모자·황동 고글·붉은 스카프를 핵심 실루엣으로 사용했다. 섬뜩한 생체기계 질감, 촉수·근육·고어 표현은 배제했다.
3. `PlayerView.tsx`는 기존 `vx`, `vy`, `onGround`, `dashTimer`를 읽어 대기/주행/상승/낙하 프레임을 전환한다. 기존 착지 스쿼시, 대시 잔상, 벽점프 파편, 깜빡임은 유지했다. `src/game/*`와 입력 로직은 변경하지 않았다.
4. 시작 화면은 새 idle 프레임을 사용하도록 교체했고, 게임오버·승리 오버레이를 이모지 기반 임시 표현 대신 상태 씰·설명·점수 패널·공통 버튼 시스템으로 정리했다.

### 에셋 출처·라이선스
- 제작 방식: Codex 내장 OpenAI 이미지 생성 도구로 자체 생성. 사용자가 제공한 AFTER 이미지는 스타일·완성도 레퍼런스로만 사용했고, 캐릭터·UI·구성을 복제하지 않았다. 외부 CC0/유료 에셋은 사용하지 않았다.
- 생성 프롬프트 핵심: premium hand-painted chibi platformer mascot, bright forest × restrained steampunk, transparent 4×2 eight-frame character sheet, no text/watermark/scenery, no grotesque organic styling.
- 생성 도구가 체크무늬을 실제 배경으로 반환해, 동일 프레임 배치와 캐릭터 픽셀을 보존한 채 체크무늬과 연결된 배경 영역만 기계적으로 분리하여 RGBA PNG로 저장했다.

### 검증
- `npx tsc --noEmit` 통과.
- 웹 실제 시작/플레이 화면에서 새 Sprout이 58px 논리 표시 크기에서도 렌더링되는 것을 확인.
- 8개 자산 모두 RGBA, 알파 범위 0~255 확인.
- 브라우저 콘솔 error 0. 기존 React Native web의 shadow/textShadow/pointerEvents deprecation warning과 native-driver web fallback warning은 이번 변경에서 새로 발생한 오류가 아니다.

### 다음 단계
- 첫 스테이지의 배경·플랫폼·Cogmite/Spore Sprite를 새 밝은 숲 화풍으로 교체해야 전체가 하나의 게임처럼 읽힌다.
- 몬스터도 귀엽고 단순한 마스코트 방향으로 만들고, 상태별 프레임 애니메이션을 추가한다.

## 2026-08-27 (26) — 비주얼 리부트 Vertical Slice 2a: 숲 배경 리빌드

### 변경
- `scene_wild_v2.png`: 푸른 산맥·겹겹의 침엽수·호수·이끼 유적·반딧불이로 구성한 밝은 페인터리 숲 계곡을 추가.
- `scene_mechanical_v2.png`: 같은 구도와 빛을 유지한 채, 이끼 낀 유적에 절제된 황동 기어·청록 증기만 더한 기계 상태를 추가.
- `Background.tsx`가 기존 배경 대신 새 자산 쌍을 교차 페이드하도록 교체. 기존 parallax·Bloom Shift 로직은 유지.
- 두 이미지는 1672×941 생성 원본에서 1151×648로 다운스케일·PNG 최적화해 약 2.47/2.25MB에서 약 1.17/1.12MB로 줄였다.

### 검증
- 웹 실제 플레이 화면에서 기계 상태 렌더링 확인, 콘솔 error 0.
- 새 배경 위에서 Sprout의 작은 실루엣이 선명하게 읽히는 것을 확인.

### 다음 단계
- 구형 Cogmite와 금속 플랫폼은 새 숲과 화풍이 충돌하므로, 먼저 Cogmite를 귀여운 태엽 딱정벌레 마스코트로 교체한다.

## 2026-08-27 (27) — 비주얼 리부트 Vertical Slice 2b: Cogmite 마스코트와 프레임 애니메이션

### 변경
- `assets/sprites/cogmite_v2/`에 대기 2, 순찰 4, 경고 1, 무해한 처치 상태 1의 8프레임 Cogmite PNG를 추가했다.
- 새 Cogmite는 이끼빛 등껍질·황동 테두리·태엽 키·큰 눈·짧은 다리의 둥근 장난감 딱정벌레로 설계했다. 공격적이지만 공포·고어·날카로운 생체 표현은 없다.
- `EnemyView.tsx`가 기존 `x`, `vx`, `chargeDir`, `bloomState`를 읽어 순찰 4프레임과 돌진 경고 프레임을 전환한다. 기존 걷기 흔들림·방향 전환·기계/자연 상태 표시는 보존했다.
- 생성 시트의 불투명 스튜디오 배경은 프레임의 색상·배치·포즈를 보존한 채 부드러운 배경 추정치와의 차이만 이용해 기계적으로 분리했으며, 최종 파일은 모두 RGBA 알파 0~255이다.

### 검증
- `npx tsc --noEmit` 통과.
- 웹 실제 플레이 화면에서 새 Cogmite가 새 숲·Sprout과 같은 화풍으로 렌더링되고, 콘솔 error 0 확인.

### 다음 단계
- Spore Sprite를 둥근 포자 요정 마스코트으로 교체하고, 금속 플랫폼 표면도 풀·돌·흙 기반으로 재빌드한다.

## 2026-08-27 (28) — 게임 방향 전환: Momentum Route

### 문제
- 사용자 피드백: 전역 자연/기계 상태 전환이 “어디에 쓰는지 모르겠고 재미도 없다.” 현재 Bloom Shift는 시각 변화와 일부 발판/적 상태만 바꾸지만, 이동의 손맛·명확한 선택·보상으로 충분히 연결되지 않는다.

### 결정
- Codex가 게임 기획·아트·UX를 주도하고 Claude Code가 게임 시스템 구현·테스트를 주로 담당한다.
- 전역 Bloom Shift 확장을 중단하고, 이를 **Momentum Route**로 단계 전환한다. 핵심은 `이동 액션 연결 → FLOW 축적 → Vine/Gear Route 선택 → 짧은 지름길 질주 → 보상`이다.
- 자연은 안전하고 공중제어가 쉬운 `Vine Route`, 기계는 빠르지만 타이밍을 요구하는 `Gear Route`로 역할을 명확히 한다. 어느 길도 막힘·강제 낙사로 이어지면 안 된다.

### Claude 인계
- 상세 기획, 첫 Area 1 설계, 로직 범위, 금지 항목, 완료 조건은 새 `GAME_DIRECTION_V2.md`에 기록했다.
- 첫 구현은 Area 1의 로컬 Route Gate 두 개로 제한한다. 전 레벨/전 몬스터를 한 번에 고치지 않는다.

### Codex 후속
- Claude의 Foundation이 들어오면 Route Gate 아트, 6초 시간감각, 열린 지름길 표시, HUD 피드백, 실제 플레이 경험을 검수하고 조정한다.

## 2026-08-27 (23) — Route Gate Foundation 구현 (Claude)

### 목표
- `GAME_DIRECTION_V2.md`의 "Claude의 첫 구현 지시"를 따라 Area 1에 `Vine Route`/`Gear Route` 게이트를 각 1개씩 로컬로 구현. 전역 Bloom Shift는 건드리지 않고(끄거나 삭제하지 않음) 완전히 별개의 추가 시스템으로 병행 구현(격리).

### 변경
1. **`src/game/types.ts`**: `RouteGateKind`, `RouteGate`(Boss와 같은 패턴 — 정적 정의와 런타임 상태를 한 타입에 결합: `active`/`timer`/`armed`) 추가. `Platform`에 `activeGate?: string` 추가(기존 `visibleIn`과 동일한 조건부 존재 방식, Bloom과 별도 축). `Level`/`GameState`에 `routeGates: RouteGate[]` 추가.
2. **`src/game/constants.ts`**: `ROUTE_GATE_WIDTH=30`, `ROUTE_GATE_HEIGHT=70`, `ROUTE_GATE_DURATION=6`(기획 문서 명시값) 추가.
3. **`src/game/level.ts`**: `vineGate1`/`gearGate1` 두 게이트와 각각의 보상 발판(Vine: 낮고 완만한 3연속 발판, Gear: 더 높고 간격이 넓어 대시 없이도 되지만 계속 움직여야 하는 2단 발판), 보상 코인 7개(항상 존재하지만 해당 게이트가 열렸을 때만 발판을 통해 도달 가능 — Coin 타입 자체는 손대지 않고 도달 가능성만으로 게이팅) 추가.
   - **배치 재검토**: 처음엔 x=560~700(기존 Bloom 튜토리얼 존 x=470 바로 다음)에 배치했으나, pure-logic 시뮬레이션으로 실제 낙사/피격이 발생해 조사한 결과 **그 구간에 이미 `piston1`(프레셔 피스톤, x=650, 기계 상태에서 상시 위험)과 `e1`(코그마이트 순찰, x=400~620)이 존재**한다는 걸 뒤늦게 발견 — 기존 컨텐츠를 사전에 충분히 확인하지 않은 실수. 레벨 전체에서 완전히 비어있는 구간을 다시 찾아 **스폰(x=40)과 첫 Shift Node(x=220) 사이(x=85~215)**로 전면 재배치. 이 구간은 기존에 아무 콘텐츠가 없어 새 게이트의 "학습" 단계 취지(첫 위협 없이 안전하게 매커니즘을 보여줌)에도 더 부합함.
4. **`src/game/physics.ts`**: `stepRouteGates()` — Bloom Shift 노드와 동일한 "이전 프레임 위치 기준 판정 + edge-triggered arming" 패턴(게이트 접촉 시 열림+6초 타이머 시작, 벗어나면 재무장). `activePlatforms()`에 `activeGateIds` 파라미터 추가해 `activeGate` 조건도 함께 필터링. `createInitialState`/`stepGame` 반환값에 `routeGates` 연결.
5. **`src/components/RouteGateView.tsx`**(신규): 최소 placeholder — Vine=이끼색, Gear=황동색 사각 마커, 활성/비활성 시 투명도만 다름. Codex가 실제 "덩굴 문/황동 아치" 비주얼로 교체할 자리(로직 없음, 순수 프레젠테이션).
6. **`src/screens/GameScreen.tsx`**(공용 파일): 플랫폼 렌더 필터에 `activeGate` 조건 추가, `RouteGateView` 렌더 라인 추가. 기능상 반드시 필요한 최소 변경만 수행 — Codex의 레이아웃/스타일 변경분(HUD/Controls 리빌드 등)은 건드리지 않음.

### 검증 (pure-logic 시뮬레이션 + tsc + Playwright)
- `npx tsc --noEmit` 통과.
- `stepGame` 직접 호출 포커스 테스트 7종(적 제거한 레벨 사본으로 게이트 메커니즘만 격리 검증):
  1. 게이트 접촉 시 `active:true`+타이머 시작 확인.
  2. Vine Route 보상 코인 전부(7개 중 6개 한 런에서, 나머지는 다른 런에서) 실제로 발판을 통해 도달·수집 가능함을 확인.
  3. Gear Route도 동일 확인 — 두 런의 합집합으로 신규 코인 7개 전부 도달 가능함을 확인.
  4. 게이트를 건드리지 않고 그 상공에서 낙하해도 정상적으로 바닥에 착지(안전한 실패) 확인.
  5. 게이트 보상 발판 위에 서 있는 도중 6초 타이머가 만료돼도 데미지 없이 바닥으로 살짝 떨어질 뿐임을 확인(소프트락/낙사 없음).
  6. 게이트에서 벗어났다가 다시 들어가면 재발동(재무장) 확인.
  7. 기존 시스템 회귀 — 지상 대시(vx=DASH_SPEED 확인), 보스 스톰프 가능 여부(세션 (18)/(19) 시나리오 재사용) 모두 정상.
- 최초 배치(x=560~700) 검증 중 `piston1`/`e1`과의 의도치 않은 충돌로 인한 사망을 실제로 잡아냈고, 재배치 후 동일 테스트 전부 통과로 원인이 배치 문제였음을 확인.
- `npx expo export -p web` 정적 빌드 + Playwright로 실제 게임 화면 로드, 이동, 게이트 통과까지 콘솔 에러 0건. 스크린샷으로 두 게이트(이끼색/황동색 마커)가 스폰 직후 나란히 서 있는 것을 육안으로도 확인(최초 placeholder는 배경과 색 대비가 약해 거의 안 보였음 — opacity/그림자를 보강해 가독성 확보, 여전히 임시 도형이라 최종 비주얼은 Codex 담당).

### 영향
- 기존 Bloom Shift(`bloomState`/`shiftNodes`/`visibleIn` 발판)는 코드 한 줄도 건드리지 않음 — 완전히 별개의 병행 시스템으로 추가(요청된 "격리" 방식).
- 기존 이동 수치, 보스, Gear Socket 수치는 변경 없음(금지 사항 준수).
- Area 2/3, 기존 몬스터/코인/체크포인트 배치는 전혀 건드리지 않음.

### 남은 문제 (Codex에게 인계)
- Route Gate 시각 언어(덩굴 문/황동 아치, 화살표, 6초 카운트다운을 숫자 없이 표현 등)는 전혀 없음 — 현재 placeholder 사각형뿐.
- 실제 화면 기준 보상·발판·카메라 시선·피드백 검수는 Codex 몫(기획 문서에 이미 명시).
- Vine/Gear Route가 실제로 "재밌는지"는 사람이 플레이해봐야 판단 가능 — 자동 검증은 "메커니즘이 의도대로 작동하는가"까지만 확인함.
- 게이트 위치가 원래 계획(x=560)에서 스폰 근처(x=90~165)로 바뀌었으므로, 기획 문서의 "학습(0~20초)/선택(20~45초)/숙련(45~75초)/결산(75~90초)" 타임라인 중 "학습" 단계가 사실상 게임 시작 직후로 앞당겨짐 — 이후 단계(선택/숙련/결산에 해당하는 추가 게이트)를 배치할 때 이 점을 고려할 것.

### 결정 사항
- 신규 콘텐츠를 배치하기 전에 반드시 해당 x 범위의 기존 몬스터/피스톤/코일 등 위험 요소를 먼저 확인할 것 — 이번에 이미 촘촘하게 채워진 Area 1에 사전 확인 없이 배치했다가 실제 시뮬레이션에서야 피스톤/몬스터와의 충돌을 발견했다. 다음에 레벨에 새 콘텐츠를 추가할 때는 좌표를 정하기 전에 `level.ts`의 해당 x 구간 전체(플랫폼/적/피스톤/코일/블로어/그래플 포인트)를 먼저 grep으로 확인하는 절차를 습관화한다.

## 2026-08-27 (29) — Route Gate 시각 언어 및 보상 경로 프레젠테이션 (Codex)

### 변경
- Vine Route를 이끼색 덩굴 문·잎·연두 코어·`EASY` 화살표로, Gear Route를 황동 아치·기어·전진 셰브런·`FAST` 화살표로 교체했다.
- 활성 중에는 숫자 타이머 대신 6칸 빛 막대가 하나씩 비워진다.
- Route 보상 발판은 덩굴/황동 표면에 맞춰 상단 발광과 진행 화살표를 표시한다. 충돌·활성 조건은 변경하지 않았다.

### 검증
- `npx tsc --noEmit` 통과.
- Expo 웹 시작 구간에서 두 게이트, 보상 코인 아치, 새 Sprout/Cogmite가 함께 렌더링되는 것을 확인. 브라우저 콘솔 error 0.

### 남은 검증
- 자동 테스트는 작동·안전성만 확인했으므로, 사람이 `EASY`/`FAST`를 보고 길을 선택하는지와 6초가 충분한지는 실제 플레이로 확인해야 한다.

## 2026-08-27 (30) — 화면 스케일·월드 표면 리빌드 및 Route Gate 폐기 (Codex)

### 사용자 피드백
- 화면 전체 오브젝트가 레퍼런스보다 너무 크고, 발판·바닥·코인 디자인이 세계와 어울리지 않는다.
- Route Gate처럼 구간을 나누는 기획도 폐기 요청.

### 변경
- Sprout 시각 크기 58→44, Cogmite 56→42로 축소했다. 물리/충돌 크기는 변경하지 않았다.
- 코인을 큰 평면 원에서 작은 씨앗 보석 수집품으로 교체했다. 기존 수집 hitbox와 애니메이션 lifecycle은 유지한다.
- 발판·바닥을 금속 패널/리벳에서 풀·이끼·흙·뿌리·돌 표면으로 전면 교체했다. 충돌 직사각형은 그대로다.
- `GameScreen.tsx`에서 Route Gate 시각 렌더링을 즉시 제거했다. 남은 상태·물리·레벨 제거는 Claude에게 P1로 인계했다.

### 검증
- `npx tsc --noEmit` 통과.
- Expo 웹 실제 화면에서 축소된 Sprout/Cogmite, 작은 보석 코인, 새 바닥/발판이 렌더링되고 콘솔 error 0 확인.

## 2026-08-27 (31) — 처치 후 발판 생성 폐기 결정

### 사용자 피드백
- 몬스터를 처치했을 때 남는 임시 발판은 용도가 불명확하고 게임 흐름에 도움이 되지 않는다.

### 결정
- `CorpsePlatform` 시스템을 전면 제거한다. 처치한 적이 지형을 만들지 않으며, 진행은 항상 미리 설계된 레벨 지형으로만 한다.
- 스톰프의 바운스, 점수, Overdrive/Flow 보상, 파편 이펙트만 남겨 처치 순간의 손맛은 유지한다.
- 구현은 Claude의 게임 로직 담당 범위이므로 `CLAUDE.md` 22장과 `AI_COLLAB.md` P1 요청으로 인계했다.

## 2026-08-27 (32) — Route Gate·처치 발판 시스템 완전 제거 (Codex)

### 변경
- 사용자 명시 승인에 따라 `RouteGate`, `RouteGateKind`, `Platform.activeGate`, `routeGates`, `stepRouteGates`, `ROUTE_GATE_*`, `RouteGateView` 및 Route Gate 발판·렌더 연결을 제거했다.
- `CorpsePlatform`, `corpsePlatforms`, `CorpsePlatformView`, 관련 상수와 처치 시 임시 지형 생성 로직을 제거했다.
- Bio-Coil 시체 위 추가 점프 보너스도 함께 제거했다. 적 처치는 점수·Overdrive/Flow·바운스·파편만 남긴다.

### 검증
- `npx tsc --noEmit` 통과.
- Expo 웹 실제 시작 구간에서 Gate/분기 없이 단일 동선이 렌더링되고 콘솔 error 0 확인.

## 2026-08-27 (33) — Bloom Shift 제거 및 단일 규칙화 (Codex)

### 사용자 피드백
- 자연/기계 환경이 바뀌는 이유와 활용처가 직관적이지 않고 재미에도 기여하지 않는다.

### 변경
- `BloomState`, `ShiftNode`, `Platform.visibleIn`, 전환 상태와 렌더 연결을 제거했다.
- 전환 노드, HUD 상태 배지, 전체 화면 플래시, 기계/자연 배경 교차전환을 제거하고 밝은 숲 배경 하나로 고정했다.
- 이전 전환 전용 발판은 언제나 존재하는 높은 보너스 점프 발판으로 바꿨다.
- 적과 위험요소도 한 가지 공정한 규칙으로 정리했다: Cogmite는 항상 감지 후 돌진, Bio-Coil은 항상 windup 후 도약, Steam Blower는 항상 스톰프로 피해를 받고, Pressure Piston은 언제나 같은 충전→분사 주기를 사용한다.

### 검증
- `npx tsc --noEmit` 통과.
- Expo 웹에서 시작 메뉴와 실제 게임 화면을 확인. 자연/기계 배지·전환 노드 없이 숲 배경, 영구 지형, HUD가 정상 렌더링된다.

## 2026-08-27 (34) — 페인터리 월드 아트 및 배경 여행감 리빌드 (Codex)

### 사용자 피드백
- 단일 배경을 반복하는 화면은 여행하는 느낌이 없고, 이전 바닥·발판은 고품질 배경과 어울리지 않는다.

### 변경
- 상용 인디 플랫포머 수준의 페인터리 월드 아트를 새로 제작해 적용했다.
  - 시작: 햇빛이 드는 산악 초원 계곡
  - 중반: 폭포·안개·깊은 침엽수 숲
  - 후반: 고목 뿌리와 이끼 낀 고대 유적의 황금빛 숲
- 세 장면은 카메라 진행에 따라 넓은 구간에서 부드럽게 겹쳐 전환된다. 플레이어의 상태·충돌·적 규칙·보상에는 어떠한 영향을 주지 않는다.
- 코드로 그린 갈색 블록 바닥을 손그림 이끼·꽃·흙·뿌리 지형 스트립으로 교체했다. 충돌 박스는 유지했다.
- 시작 화면도 같은 초원 계곡 일러스트를 사용해 메뉴와 실게임의 아트 언어를 통일했다.

### 검증
- `npx tsc --noEmit` 통과.
- Expo 웹에서 신규 배경, 공중 발판, 지면, Sprout, 코인, HUD가 한 화면에 정상 렌더링되는 것을 확인했다.

## 2026-08-27 (35) — Claude 기술 검수: Bloom Shift/Route Gate/처치 발판 3종 제거 회귀 검증

### 배경
- CLAUDE.md 21~23장에 따라 Codex가 사용자 승인 하에 Route Gate(세션 (23), 내가 방금 만든 것), 처치 후 임시 발판, Bloom Shift 3개 시스템을 전부 직접 제거함(`src/game/*.ts` 포함). 세 커밋 모두 `AI_COLLAB.md` REVIEW REQUESTS에 "Codex→Claude" 형태로 사전 통보 후 "명시 승인 하에" 처리했다고 투명하게 기록되어 있음. CLAUDE.md 22장이 명시적으로 요구하는 "제거 후 스톰프, Bio-Coil 처치, Steam Blower 처치, 보스전, 체크포인트 회귀 검증"을 내 쪽에서 독립적으로 수행.

### 검증 방법 및 결과 (pure-logic 시뮬레이션 12종 + tsc + Playwright)
- `npx tsc --noEmit` 통과.
- 12개 항목 전부 PASS: Cogmite 무조건 감지+돌진, Cogmite 스톰프(처치 발판 없이도 정상), Bio-Coil 무조건 windup 후 도약, Bio-Coil landed 상태에서만 스톰프 가능, Steam Blower 무조건 스톰프로 데미지, Pressure Piston 고정 주기로 데미지, Spore Sprite 대시로만 처치, 지상 대시 속도, Root-Hook 그래플 부착, 체크포인트 인덱스 갱신, **보스전 무피해 클리어(반응형 봇, 세션 (18)/(19) 시나리오 재사용)**, 스프링 코그 점프 부스트.
- 처음 2개는 테스트 스크립트 자체의 좌표/타이밍 실수(플레이어가 스팀 블로어 히트박스 밖에서 낙하 / 첫 프레임엔 `onGround`가 아직 `false`라 점프가 안 먹힘)로 FAIL이 떴다가, 스크립트를 고치니 정상 PASS로 확인됨 — 실제 게임 로직 결함 아님.
- **직접 확인이 필요했던 우려 하나**: 세션 (23)에서 내가 만든 Route Gate 보상 코인 7개(x=100~205)가 제거된 게이트 발판 없이 `makeCoins()`에 그대로 남아 있어 "도달 불가능한 코인"이 될까 걱정했으나, 실측 결과 **전부 순수 점프만으로 도달 가능**(코인 박스와 플레이어 박스의 겹침 판정 여유 덕분에 필요 상승 높이가 애초 생각보다 낮았음 — 직접 계산 대신 시뮬레이션으로 확인해 착오를 바로잡음). 오히려 이 배치가 새 기획 방향("잘 플레이하면 높은 코인 줄을 잇고, 놓치면 아래 안전한 길로 이어진다")과 우연히 정확히 들어맞아, 별도 수정 없이 그대로 유지.
- Expo 웹 빌드 + Playwright로 실제 화면 확인 — 자연/기계 배지, 전환 노드, Route Gate 마커, 처치 후 발판 전부 사라지고 밝은 숲 배경 위에 코인 아치와 몬스터만 남은 단일 동선을 육안으로도 확인, 콘솔 에러 0.

### 결론
- Codex의 3종 대규모 제거(Bloom Shift, Route Gate, 처치 후 발판)는 기술적으로 안전하게 완료됨 — 코드 리뷰 + 12종 회귀 테스트 + 실제 렌더링 확인 전부 통과. 추가 수정 불필요.
- `src/game/*.ts`(Claude 소유 파일)를 Codex가 직접 수정한 것은 평소 파일 소유권 원칙과는 다르지만, `AI_COLLAB.md`에 사전 REVIEW REQUEST로 투명하게 기록되어 있고 사용자가 직접 승인한 긴급/대규모 방향 전환이라 이번엔 그대로 받아들이고 사후 기술 검수로 대응함.

## 2026-08-28 (36) — 주인공·수집품·기본 적 아트 통일 (Codex)

### 사용자 피드백
- 배경만 개선되어서는 부족하며, 주인공·코인·적·기타 화면 요소도 같은 상용 게임 수준의 디자인 언어로 바뀌어야 한다.

### 변경
- 기존 잎사귀 고글 Sprout 대신, 초록 후드·붉은 스카프·황토색 재킷의 숲 탐험가 주인공으로 대기/달리기 시각 자산을 교체했다. 점프·대시·충돌 수치와 물리 로직은 변경하지 않았다.
- 기존 다이아 모양 코인을 손그림 황동 `Sunseed` 수집품으로 바꿨다. 수집 hitbox·부유 애니메이션은 유지했다.
- 기본 적 Cogmite를 이끼·나무·황동으로 만든 숲의 태엽 딱정벌레로 교체했다. 순찰·돌진 로직은 그대로다.
- 생성형 에셋은 Codex 내장 이미지 생성 도구로 직접 제작했고, 프로젝트에는 투명 PNG로 다듬어 저장했다. 외부 저작물은 사용하지 않았다.

### 검증
- `npx tsc --noEmit` 통과.
- Expo 웹에서 시작 화면과 실제 첫 전투 화면을 확인했다. 새 주인공·Sunseed 코인·Cogmite가 배경 및 지형과 함께 렌더링되고 브라우저 콘솔 오류가 없었다.

### 남은 디자인 작업
- 나머지 적 변형, 조작 버튼/HUD, 보스전 주변 요소도 새 아트 언어에 맞춰 순차 교체한다.

## 2026-08-28 (37) — 유물 활·신규 적 아트 패키지 준비 (Codex)

### 변경
- 유물 활 획득 아이템, 활의 나뭇잎 화살, 도약형 `Acorn Hopper`, 고정 사격형 `Root Turret`의 투명 PNG 아트를 제작했다.
- 모든 자산은 초록 후드 탐험가·Sunseed·Cogmite와 같은 밝은 페인터리 숲 모험 화풍으로 통일했다.
- 도약형은 버섯/괴물 대신 도토리 모자와 잎사귀를 쓴 숲 정령으로, 사격형은 고목·이끼·돌로 만든 유적 포탑으로 설계했다.

### 연동 대기
- Claude의 활 획득/화살/신규 적 상태·물리 구현이 들어오면 Codex가 이 자산을 프레젠테이션 컴포넌트에 연결하고 HUD 공격 버튼을 디자인한다.
- 이번 단계에서는 게임 규칙과 로직 파일을 수정하지 않았다.

## 2026-08-28 (38) — 유물 활·화살·신규 적 2종 로직 구현 (Claude)

### 목표
- 사용자 지시에 따라 유물 활+화살 시스템과 신규 적 2종(도약형=Jumper/Acorn Hopper, 고정 사격형=Turret/Root Turret)을 기존 물리 구조에 최소한으로 붙인다. 대규모 재작성 금지, AABB+타이머 기반만 사용(Matter.js 등 도입 금지), Route Gate/Bloom Shift/시체 발판 재도입 금지.

### 변경
1. **`src/game/types.ts`**: `BowPickup`(`collected`만 있는 CogPickup과 동일 패턴), `Arrow`(`id,x,y,width,height,vx,age` — `alive` 필드는 두지 않고 배열에서 바로 spliced-out시키는 방식 채택, 이유는 회전문처럼 계속 생기고 사라지는 발사체라 죽은 유령 항목을 영구 보관할 이유가 없어서), `Jumper`(`phase:'grounded'|'windup'|'airborne'`, `homeX`/`groundY`로 자기 발판을 벗어나지 않게), `Turret`, `SeedProjectile` 추가. `Player`에 `hasBow`/`arrowCooldown`, `InputState`에 `attackPressed`(기존 `jumpPressed`/`dashPressed`와 동일한 edge-trigger 소비 패턴), `Level`/`GameState`에 각각 필드 추가.
2. **`src/game/constants.ts`**: `BOW_PICKUP_SIZE`, `ARROW_*`(폭/높이/속도/쿨다운/수명), `JUMPER_*`(간격/윈드업/발사속도), `TURRET_*`(발사간격/충전텔레그래프/전역 최대 씨앗 수), `SEED_*` 전부 신규 상수로 분리(밸런스 조정 쉽게 하라는 지시 반영).
3. **`src/game/level.ts`**: 유물 활을 x=250 지상(스폰~기존 콘텐츠 사이 완전히 빈 구간)에 배치. Jumper 2개/Turret 2개는 **지상이 아니라 기존 넓은 공중 발판(p6/p9/p15/p22) 위**에 배치 — 지상 순찰 코그마이트는 전부 `GROUND_Y` 높이에서만 움직이므로, 공중 발판 위는 항상 기존 위험요소와 충돌 없는 안전지대라는 걸 이용한 선택.
   - **배치 사고와 수정**: 최초엔 유물 활을 x=560 근처에 두려다가, 이전 세션(Route Gate)에서 이미 겪은 실수를 떠올리고 미리 `npx tsx`로 해당 구간의 기존 발판/적/피스톤/코일을 전부 조회한 뒤 x=250(완전히 빈 구간)으로 확정했다 — 같은 실수를 반복하지 않음.
4. **`src/game/physics.ts`**:
   - `stepJumpers`: grounded(타이머)→windup(텔레그래프, Bio-Coil과 동일 패턴)→airborne(수직 도약, `homeX`로만 착지)→grounded. 플레이어 위치를 전혀 참조하지 않는 순수 타이머 기반.
   - `stepTurrets`+`isTurretCharging`: 발사 주기 타이머, 발사 순간에만 플레이어와의 상대 위치로 방향을 한 번 고정(코그마이트의 `chargeDir`, 바이오코일의 `facing`과 동일한 "그 순간에만 고정, 이후 추적 안 함" 패턴 재사용). 전역 `TURRET_MAX_SEEDS` 상한 체크.
   - 화살/씨앗: 이동 후 벽(`level.platforms`)/월드 밖/수명 초과 시 배열에서 제거. `consumedArrowIds` Set으로 한 프레임에 화살 하나가 여러 대상을 못 맞히게 하고, 맞은 화살은 그 프레임 끝에 배열에서 제거.
   - 기존 코그마이트/바이오코일/스팀 블로어/보스의 `.map()` 처리 블록 각각에 "화살에 맞았는가" 분기를 스톰프 분기 바로 앞에 추가 — 화살은 기존 스톰프와 동일한 점수·Overdrive 보상을 준다. **의도적 예외 2건**: (a) 스포어 스프라이트는 기존 "대시로만 처치 가능" 규칙을 그대로 보존하기 위해 화살 면역으로 남김(CLAUDE.md에 문서화된 몬스터별 개성이라 판단), (b) 보스는 무조건 화살을 소모하되(고체라 튕겨나감) 데미지는 기존 vulnerable 페이즈일 때만 들어가게 해서 스톰프와 동일한 타이밍 제약을 유지 — 화살이 보스전 난이도를 우회하는 새로운 방법이 되지 않도록 함.
   - 씨앗은 플레이어에게 접촉 데미지를 주고(무적시간 존중), 반대로 스톰프나 화살로 파괴 가능(작은 점수+Overdrive 보상).
5. **`src/screens/GameScreen.tsx`**: `InputState`에 `attackPressed` 추가에 따른 최소 배선(입력 ref 초기값+매 프레임 소비 후 리셋)만 추가. `Controls`에 공격 버튼을 아직 연결하지 않음 — `Controls.tsx`가 `onAttack` prop을 아직 선언하지 않아서 타입 에러 없이는 붙일 수 없고, 이 UI는 Codex 소유라 `AI_COLLAB.md`에 정확한 인터페이스로 요청만 남김. 새 렌더링 컴포넌트(`BowPickupView` 등)도 만들지 않음 — 이번엔 사용자가 명시적으로 "src/components/*.tsx는 Codex가 맡는다, 필요하면 REVIEW REQUESTS에 인터페이스만 남겨라"라고 지시해서, 이전 세션들의 관례(placeholder 컴포넌트를 직접 만들던 것)와 다르게 로직만 완성하고 렌더링은 전혀 건드리지 않았다.

### 검증 (pure-logic 시뮬레이션 29종 + tsc + Playwright)
- `npx tsc --noEmit` 통과.
- 29개 항목 전부 PASS: 활 획득 전 공격 무효/획득 시 영구 hasBow 부여/획득 후 발사·연사 쿨다운, 화살의 코그마이트·바이오코일(모든 페이즈)·스팀 블로어(hp 차감) 처치, 화살 수명/벽 충돌 제거, Jumper의 무추적 도약·스톰프·화살처치, Turret의 발사 시점 방향 고정·충전 텔레그래프·전역 씨앗 상한·스톰프·화살처치, 씨앗의 플레이어 피격(무적시간 존중)·스톰프/화살 파괴, 보스는 vulnerable 아닐 때 화살 데미지 없음/vulnerable일 때만 데미지, 반응형 봇으로 보스전 전체 무피해 클리어(세션 (19) 시나리오 재사용), 스포어 스프라이트 화살 면역 유지, hasBow가 사망/부활을 넘어 유지(기어 소켓과 동일하게 완전 재시작 시에만 초기화), 기존 대시/그래플/체크포인트 회귀.
- Expo 웹 빌드 + Playwright로 실제 게임 화면 로드·이동까지 콘솔 에러 0 확인. 신규 요소(화살/활/Jumper/Turret/씨앗)는 아직 전용 렌더링 컴포넌트가 없어 화면에 보이지 않음 — 로직은 정상 동작 중이며 시각적 확인은 Codex의 컴포넌트 연동 이후 가능.

### 영향
- 기존 이동/대시/그래플/체크포인트/Gear Socket/보스전 수치·로직은 전혀 변경하지 않음(회귀 테스트로 확인).
- 신규 상수는 전부 `constants.ts`에 이름 붙여 분리해서 이후 밸런스 조정이 값 하나만 바꾸면 되게 해뒀다.

### 남은 문제 (Codex에게 인계)
- 렌더링 컴포넌트(활/화살/Jumper/Turret/씨앗)와 공격 버튼(`Controls.tsx`의 `onAttack` prop) 전부 미구현 — `AI_COLLAB.md`에 정확한 인터페이스 남겨둠.
- 실제 터치 조작으로 연사 쿨다운(0.35초)과 신규 적 밀도가 체감상 적절한지는 사람 실플레이 필요.
- Jumper/Turret은 현재 각 2개씩만 배치(레벨 전체 재배치가 아니라 최소 검증 목적) — 사용자가 원하면 추가 배치는 후속 작업으로.

### 결정 사항
- 새 콘텐츠(Jumper/Turret)를 지상이 아니라 기존 "빈 공중 발판" 위에 배치하는 패턴을 발견 — 이 레벨의 지상 차선은 이미 몬스터/피스톤으로 포화 상태이지만, 모든 기존 순찰 몬스터가 지상 높이에서만 움직이기 때문에 공중 발판은 예외 없이 안전지대다. 다음에 새 지상 적/오브젝트를 추가할 때도 이 패턴(공중 발판 우선 검토)을 먼저 고려할 것.
- 화살의 보스 상호작용은 "기존 스톰프 조건(vulnerable 페이즈)을 그대로 요구하는 대체 입력 수단"으로 설계 — 새 무기가 이미 검증된 보스 난이도를 몰래 낮추지 않도록 하는 일반 원칙으로 다음에도 적용할 것.

## 2026-08-28 (39) — 유물 활·신규 적 프레젠테이션 연동 (Codex)

### 변경
- `BowPickupView`, `ArrowView`, `JumperView`, `TurretView`, `SeedProjectileView`를 추가해 Claude가 구현한 상태를 실제 숲 세계 화면에 연결했다.
- 활은 부유·황금빛 획득 연출을, Jumper는 도약 전 납작해지는 예고와 경고 고리를, Turret은 사격 직전 주황 충전광을 표시한다.
- 공격 버튼은 활 획득 전에는 흐린 잠금 상태이고, 획득 후에는 청록색 `ARROW` 버튼으로 활성화된다. 기존 멀티터치 처리 방식은 유지했다.
- 충돌, 화살 처리, 적 AI, 수치 등 `src/game/*`는 변경하지 않았다.

### 검증
- `npx tsc --noEmit` 통과.
- Expo 웹에서 유물 활, 잠긴 ARROW 버튼, 기존 HUD/컨트롤이 한 화면에 정상 렌더링되는 것을 확인했다.

## 2026-08-28 (40) — 모바일 조작감·핵심 연출 1차 보정 (Codex)

### 사용자 피드백
- 모바일에서 일반 이동이 너무 빠르고, 게임 영역이 좁아 조작하기 어렵다.
- 주인공이 지면 위에 떠 보이며 활 사격 포즈가 없다.
- 단순 깃발과 원형 Root-Hook 표식이 세계관과 어울리지 않는다.

### 변경
- 일반 이동 속도를 220→160px/s로 낮췄다. 대시는 별도 고속 액션으로 유지했다.
- HUD/컨트롤 예약 높이와 버튼 크기를 줄여 모바일 가로 화면의 실제 게임 영역을 넓혔다.
- 주인공의 시각 발 앵커를 지면으로 보정하고, 화살 발사 직후에는 유물 활을 손에 드는 짧은 사격 포즈를 추가했다.
- 도형 깃발을 나무·덩굴·붉은 원정 배너로, 원형 그래플 표식을 빛나는 고목 뿌리 갈고리로 바꿨다. 그래플 선도 더 두꺼운 덩굴과 잎으로 보강했다.

### 검증
- `npx tsc --noEmit` 통과.
- Expo 웹에서 줄어든 컨트롤, 넓어진 스테이지, 주인공·활·새 Root-Hook 오브젝트가 함께 렌더링되는 것을 확인했다.

## 2026-08-28 (41) — 달리기 프레임 및 원정 화면 리디자인 (Codex)

### 변경
- 주인공의 두 번째 달리기 포즈를 제작해 기존 단일 정지 포즈 반복을 실제 두 프레임 달리기 사이클로 교체했다.
- 시작 화면을 `WILDROOT / 태양씨앗의 길` 원정 화면으로 정리하고, 더 이상 사용하지 않는 기계 상태 전환/태엽 세계관 문구를 제거했다.
- 게임오버·클리어 오버레이도 길 위의 재도전과 원정 완료라는 현재 게임 방향에 맞는 문구로 교체했다.

### 검증
- `npx tsc --noEmit` 통과.
- Expo 웹에서 새 시작 화면, 주인공 아트와 모든 텍스트가 정상 렌더링되는 것을 확인했다.

## 2026-08-28 (42) — 활·화살·Jumper·Turret 로직 1차 안정화 검증 (Claude)

### 배경
- `AI_COLLAB.md` CLAUDE ACTIVE에 정의된 "사용자 부재 중 1차 플레이 안정화" 4단계(이동 160/대시 480 체감차이, 활 쿨다운/화살 속도·수명, Jumper/Turret/씨앗 충돌·전역 스폰 상한, 핵심 게임플레이 자동 회귀)를 수행했다.

### 방법
- `npx tsc --noEmit`.
- 직접 작성한 pure-logic 시뮬레이션 스크립트(저장소 루트에 임시로 두고 검증 후 삭제, 커밋하지 않음) — `createInitialState`/`stepGame`/`createLevel`을 실제로 임포트해 42종 항목을 자동 검증.
- Expo 웹 서버(`expo start --web`)를 띄우고 전역 설치된 Playwright(`/opt/node22/lib/node_modules/playwright`, 크로미움 `/opt/pw-browsers/chromium-1194`)로 시작 화면 로드 → Start 클릭 → 실제 게임 화면까지 콘솔 에러 수집 및 스크린샷 확인.

### 결과
- `npx tsc --noEmit` 통과.
- **1) 이동/대시 체감**: 지상에서 이동 입력 시 정확히 `MOVE_SPEED`(160px/s)에 도달, 대시 입력 시 `DASH_SPEED`(480px/s, 이동의 3배)로 전환되고 `DASH_DURATION` 동안 반대 방향 입력을 눌러도 덮어써지지 않는 락이 유지됨을 확인.
- **2) 활 쿨다운/화살**: 활 획득 전 공격 입력이 화살을 만들지 않음, 픽업 접촉 시 `hasBow` 영구 true 전환, 발사 시 화살 `vx = facing * ARROW_SPEED(500)`, `ARROW_COOLDOWN(0.35s)` 내 연사가 막히고 경과 후 재발사됨, 화살이 `ARROW_LIFETIME(1.2s)` 또는 벽 충돌로 소멸함을 확인.
- **3) Jumper/Turret/씨앗**: Jumper의 grounded→windup(`JUMPER_WINDUP_DURATION`)→airborne→grounded 전체 사이클이 `homeX`로 정확히 복귀함, Turret이 `TURRET_FIRE_INTERVAL - TURRET_CHARGE_DURATION` 시점부터 `isTurretCharging`이 true가 되고 주기마다 `SEED_SPEED(200)`의 씨앗을 발사함, 씨앗이 이미 `TURRET_MAX_SEEDS(6)`개 존재하면 추가 발사가 억제됨(전역 상한 준수), Jumper/Turret/씨앗 모두 스톰프와 화살 양쪽으로 파괴 가능하고 씨앗이 플레이어와 접촉 시 무적시간을 존중하며 데미지를 줌을 확인.
- **4) 핵심 회귀**: Cogmite가 감지 시 `ENEMY_CHARGE_SPEED(120)`로 돌진 방향을 잠그고(패트롤 경계에 걸리면 즉시 반전), 스톰프로 처치 시 어떤 임시 발판도 생기지 않음(제거된 시스템 재도입 없음)을 확인. Bio-Coil이 사거리 내 진입 시 windup으로 전이함, Steam Blower가 가스/화염 페이즈와 무관하게 스톰프 데미지를 항상 받음, Spore Sprite가 화살 면역을 유지하면서 대시로는 여전히 즉사함(신규 화살 시스템이 기존 몬스터 개성을 깨지 않음), 체크포인트 인덱스가 통과 시 갱신됨, Root-Hook 그래플이 사거리 내에서 정상 부착됨, 보스전을 반응형 봇으로 vulnerable 페이즈에만 스톰프하도록 20000프레임까지 시뮬레이션한 결과 무피해로 클리어되고 `phase`가 `win`으로 전환됨을 확인.
- 처음 실행에서 4건 FAIL이 떴으나 전부 **테스트 스크립트 자체의 좌표/타이밍 실수**로 확인됨(실제 게임 로직 결함 아님): (a) 스폰 직후 플레이어가 아직 공중 상태(`onGround: false`)라 Air Control 배율(0.85)이 적용된 채로 지상 속도를 검사했음 → 착지를 기다리도록 수정, (b) 대시가 무적시간을 부여한 직후 같은 프레임에서 `dt`만큼 즉시 감쇠되는 것을 검사식에 반영하지 않았음 → 허용 오차 반영, (c) 전역 상한 테스트용 합성 씨앗 좌표를 `worldWidth` 밖(x=100000)에 뒀더니 씨앗의 자체 despawn 필터(`x < worldWidth`)에 걸려 즉시 사라졌음 → 월드 안쪽, 화면 밖 y좌표로 수정, (d) 코그마이트 감지 테스트 위치가 패트롤 최소 경계(`minX`) 바로 앞이라 감지→돌진이 시작되자마자 같은 프레임에 경계에 걸려 즉시 반전(`chargeDir`가 다시 0으로)됐음 → 패트롤 구간 중앙 근처로 위치를 옮겨 수정. 네 건 모두 스크립트를 고친 뒤 전부 PASS로 확인.
- Expo 웹 + Playwright: 시작 화면 로드 후 Start 클릭 → 주인공, Cogmite, 유물 활 아이템, 코인, 배경, HUD, 하단 컨트롤이 모두 정상 렌더링되고 브라우저 콘솔 에러 0건.

### 결론
- **코드 변경 없음.** 활/화살/Jumper/Turret/씨앗 로직과 기존 5종 몬스터+보스+체크포인트+그래플 회귀가 전부 설계대로 동작함을 확인했고, 밸런스 수치를 바꿀 근거(버그나 명백한 체감 이상)를 발견하지 못했다.
- `AI_COLLAB.md`의 CLAUDE ACTIVE 4단계를 완료 처리하고 비웠다. 실제 모바일 터치 조작에서의 연사 간격/밀도 체감(기존 OPEN ISSUES의 S2)은 이번 검증 범위 밖이며, 여전히 사람의 실제 플레이가 필요하다.

### 남은 문제
- S2(기존과 동일, 미해결): 실제 모바일 터치 플레이에서 활 사격 간격과 Jumper/Turret 밀도 체감 확인 필요 — 이번 세션은 로직/수치 검증만 수행했고, 촉감(feel) 평가는 Codex/사용자의 실플레이 몫이다.

## 2026-08-28 (43) — 레벨 전체 실주행 스트레스 테스트 (Claude, 추가 확인)

### 목표
- (42)의 개별 항목 시뮬레이션에 더해, 실제 한 판을 처음부터 끝까지 입력을 계속 넣으며 돌리는 스트레스 테스트로 새 콘텐츠(활/화살/Jumper/Turret/씨앗)가 기존 필수 동선을 막거나 예상 밖의 상호작용(자원 누수, NaN, 전역 상한 위반, 소프트락)을 만들지 않는지 한 번 더 확인했다.

### 방법
- 직접 작성한 임시 스크립트(검증 후 삭제, 커밋하지 않음)로 "항상 오른쪽 이동 + 18프레임마다 점프 + 활 소지 시 항상 공격"이라는 단순 휴리스틱 봇을 최대 6분(21600프레임) 동안 실제 `stepGame`에 통과시켰다. 매 프레임 `player`의 모든 숫자 필드가 유한한지, 화살/씨앗 개수가 비정상적으로 쌓이지 않는지, 전역 씨앗 상한(6)이 실제 플레이 중에도 지켜지는지 검사했다.

### 결과
- 835프레임(약 14초) 만에 목숨 3개를 모두 소진해 게임오버로 종료됐다. 사망 3건 전부 `x≈650~758` 구간, 즉 **기존에 이미 존재하던 `piston1`(x=650) 근처**에서 발생했고 Jumper/Turret/화살/씨앗과 관련된 사망은 0건이었다.
- 활은 예정대로 x=250 부근에서 정상 수령(hasBow=true), 이후 쿨다운을 지키며 화살 20발을 발사, 코인 11/47 획득, Jumper 2기·Turret 2기 모두 생존(봇이 그 구간까지 도달하지 못함).
- 전 구간에서 `anomalies` 0건: NaN/Infinity 없음, 화살/씨앗 누적 폭주 없음, 전역 씨앗 상한 위반 없음.
- 사망 원인을 추적해보니 봉이 무조건 오른쪽으로 걷고 정해진 프레임마다만 점프하는 단순 로직이라 `Pressure Piston`의 예고된 주기(충전→분사→휴식)를 피하지 못하고 반복해서 부딪히는 것으로 확인 — 이는 신규 콘텐츠와 무관하며, 원래 설계상 타이밍 실력을 요구하는 기존 위험요소가 의도대로 동작하고 있음을 보여준다(회피에 실패하는 것이 곧 로직 결함은 아님).

### 결론
- 새로 추가된 활/화살/Jumper/Turret/씨앗 시스템이 레벨의 필수 동선을 막거나, 실제 연속 플레이 중 자원 누수·수치 이상을 일으키지 않음을 추가로 확인했다. (42)의 개별 회귀 결과와 합쳐, 이번 세션에서 코드를 바꿀 근거는 여전히 발견되지 않았다.
- 봇을 "레벨을 완주시키는" 수준까지 정교화하는 것(피스톤 타이밍 회피, 적 조우 시 우회 등 실제 플레이 AI)은 이번 검증의 목적을 넘어서는 과잉 구현으로 판단해 진행하지 않았다 — 개별 위험요소별 반응형 봇 검증(보스전 등)은 이미 (35)/(38)에서 별도로 확인되어 있다.
## 2026-08-28 (44) — 기본 적 실루엣 및 적 반응 모션 보정 (Codex)

### 사용자 피드백
- 기계 곤충형 기본 적이 숲 원정의 분위기와 어울리지 않고, 귀엽지만 징그러운 벌레로 읽힌다.
- 그래플 오브젝트와 적의 반응이 목적에 비해 장식적으로 보여 한눈에 파악하기 어렵다.

### 변경
- 기본 순찰 적의 외형을 기계 딱정벌레에서 `Brambleling`으로 교체했다. 도토리 모자·나무 가면·잎 망토를 쓴 작은 숲 수호자라서, 벌레/거미형 다리 없이도 위험 대상을 명확히 구분한다.
- 기존 AABB, 패트롤/돌진, 피격과 점수 처리에는 손대지 않았다. 렌더러의 상하 보행 리듬·몸 기울임·방향 전환 반동은 그대로 적용된다.
- Jumper는 윈드업의 납작해짐에 더해 공중에서 세로로 늘어나는 실루엣을 갖게 했고, Root Turret은 충전 중 짧은 반동과 확대를 더해 발사 직전 상태를 읽기 쉽게 했다.
- Root-Hook 앵커는 복잡한 뿌리 그림 대신 잎이 달린 고목 고리로 단순화했다. 그래플 규칙/사거리/입력은 전혀 변경하지 않았다.

### 아트 출처
- `assets/sprites/brambleling_v1/brambleling_0.png`: Codex 내장 이미지 생성 도구로 만든 자체 제작 투명 PNG. 외부 에셋/제3자 라이선스 사용 없음.

### 검증
- `npx tsc --noEmit` 통과.
- Expo 웹에서 시작→실제 플레이 화면을 열어, 새 Brambleling·HUD·컨트롤·배경이 함께 렌더링되는 것을 확인했다.

### 후속
- 기본 적의 두 번째 전용 보행 포즈와 실제 휴대폰 터치의 활/Hook 체감은 다음 검수 대상으로 남긴다.

## 2026-08-28 (45) — Brambleling 2프레임 보행 연결 (Codex)

### 변경
- 같은 Brambleling의 반대 발·반대 팔 보행 포즈를 제작해, 기본 적이 더 이상 한 장의 일러스트만 흔들리지 않도록 두 프레임 보행으로 연결했다.
- 적이 바라보는 방향은 기존 렌더러의 반전 처리로 일관되게 유지하고, 이동/돌진/충돌 판단에는 전혀 손대지 않았다.

### 아트 출처
- `assets/sprites/brambleling_v1/brambleling_1.png`: Codex 내장 이미지 생성 도구로 기존 Brambleling을 참조해 제작한 자체 제작 투명 PNG. 외부 에셋/제3자 라이선스 사용 없음.

### 검증
- `npx tsc --noEmit` 통과.
- Expo 웹 실제 플레이 화면에서 새 적 스프라이트가 기존 배경·HUD·조작부와 함께 로드되는 것을 확인했다.

## 2026-08-28 (46) — Chestnut Roller 적 디자인·구현 명세 준비 (Codex)

### 사용자 승인 범위
- 느리게 걷는 형태와 빠른 무적 구르기 형태를 오가는 새 적을 추가한다.
- 구르기 후에는 무방비 상태를 거치고, 쿨다운이 끝나기 전에는 다시 구르지 않는다.
- 슈퍼마리오식 껍질 차기/반사는 재미는 있지만 이번 단계에서는 제외한다. 먼저 하나의 명확한 대응 규칙을 완성한다.

### 디자인
- 적 이름: `Chestnut Roller`. 밤송이 갑옷과 잎 후드를 두른 작은 숲 수호자다. 기존에 피드백 받은 곤충·거미·버섯 괴물 인상을 피했다.
- 보행 상태는 몸통·발·눈이 보여 “지금 공격 가능”을 읽게 하고, 구르기 상태는 눈과 팔다리가 완전히 사라진 둥근 밤 껍질 공으로 만들어 “지금은 무적”을 즉시 읽게 한다.
- Codex는 구르는 동안 회전 애니메이션, 짧은 낙엽/먼지 흔적, 예고와 회복의 압축·늘어남을 구현한다. Claude는 승인된 AABB/타이머 상태 규칙과 배치를 맡는다.

### 아트 출처
- `assets/sprites/chestnut_roller_v1/chestnut_roller_walk.png`
- `assets/sprites/chestnut_roller_v1/chestnut_roller_roll.png`
- 두 파일 모두 Codex 내장 이미지 생성 도구로 만든 자체 제작 투명 PNG. 외부 에셋/제3자 라이선스 사용 없음.

### 협업 요청
- `AI_COLLAB.md` REVIEW REQUESTS에 Claude용 상태 전이·무적 조건·수치·검증 기준을 P1으로 기록했다. 렌더링은 `GameState.chestnutRollers` 인터페이스가 들어오는 즉시 연결한다.

## 2026-08-28 (47) — Chestnut Roller 상태·물리·레벨 배치 구현 (Claude)

### 목표
- (46)에서 Codex가 `AI_COLLAB.md` REVIEW REQUESTS에 남긴 사용자 승인 명세(걷기→예고→빠른 무적 구르기→무방비→2.6초 쿨다운)를 그대로 `src/game/*`에 최소 구현한다. 대규모 리팩터링 없이 기존 Jumper/Turret/Cogmite 패턴을 재사용한다.

### 변경
1. **`src/game/types.ts`**: `ChestnutRollerPhase`(`'walk'|'windup'|'rolling'|'recover'`)와 `ChestnutRoller`(`id,x,y,width,height,minX,maxX,vx,facing,phase,timer,cooldown,alive`) 추가. 명세에 적힌 필드 이름을 그대로 사용했다. `Level`/`GameState`에 `chestnutRollers` 필드 추가.
2. **`src/game/constants.ts`**: `CHESTNUT_ROLLER_WIDTH(32), HEIGHT(30), PATROL_SPEED(55), DETECT_RANGE(280), WINDUP_DURATION(0.45), ROLL_SPEED(250), ROLL_DURATION(1.15), RECOVER_DURATION(0.65), COOLDOWN(2.6)` — 요청받은 수치를 그대로 상수화했다(폭/높이만 명세에 없어 기존 `ENEMY_WIDTH/HEIGHT`와 비슷한 값으로 판단해 정했다).
3. **`src/game/level.ts`**: 배치 전에 기존 모든 지상 엔티티(적 9기의 순찰 구간, 피스톤 3기, Bio-Coil, Steam Blower, 스폰/활/코그 클러스터)의 x구간을 스캔하는 스크립트를 직접 작성해 돌려 확인했다 — 결과는 정확히 3곳(2000-2200, 2700-2900, 6220-6800)만 150px 이상 완전히 비어 있었다. 그중 가장 넓은 6220-6800(580px, 완전 공백)과 2700-2900(200px, e4~e5 사이 틈)을 사용해 `roller1: x=2720-2880`, `roller2: x=6260-6760`으로 `makeChestnutRollers()`를 작성했다(각 구간 안쪽으로 여유를 두어 인접 요소와 완전히 분리). 이전 세션((23) Route Gate, (38) 유물 활)에서 겪은 "눈대중 배치 후 겹침 발견" 실수를 반복하지 않기 위해 이번엔 처음부터 계산으로 확인했다.
4. **`src/game/physics.ts`**: `stepChestnutRollers(prev, player, dt)`를 Cogmite/Bio-Coil과 같은 "`prev.player`로 이번 프레임을 결정한다" 관례를 따라 작성했다.
   - `walk`: 순찰 이동(경계에서 반전)은 감지가 안 될 때만 적용하고, 감지 시엔 그 프레임에 정지·`windup` 진입(이동 안 함) — Bio-Coil의 "coiled에서 곧장 windup" 패턴과 동일하게 처리했다. 감지 조건은 "플레이어가 같은 높이에서 280px 이내"인데, "같은 높이"를 플레이어·롤러 사각형의 y축 겹침으로 구현해 플레이어가 머리 위로 점프해 지나가면 트리거되지 않게 했다(이 부분은 명세에 정확한 판정 방식이 없어 직접 내린 판단).
   - `windup`: 정지 상태로 0.45초 대기 후 `rolling` 진입, 이때 `vx = facing * ROLL_SPEED`로 고정.
   - `rolling`: 매 프레임 이동 후 자기 `minX`/`maxX` 경계에 닿으면 그 자리에서 즉시 `recover`로 전환(경계 도달이 곧 조기 종료), 아니면 `ROLL_DURATION` 경과 시 자연 종료 — 두 조건 중 먼저 오는 쪽으로 전환한다.
   - `recover`: 정지 0.65초 후 `walk`로 복귀하며 `cooldown = 2.6`을 새로 부여, 순찰 방향(`facing`)은 windup 진입 시 잠긴 방향을 그대로 유지해 재개한다.
   - 화살/스톰프 처치 판정은 `phase === 'rolling'`일 때만 완전히 건너뛰고, 접촉 피해(`applyHit`)는 phase와 무관하게 항상 적용해 명세의 "구르기 중엔 화살·스톰프 무효, 접촉은 일반 적과 동일 규칙"을 그대로 구현했다. Jumper/Turret과 동일하게 `consumedArrowIds`를 공유해 한 화살이 여러 대상을 중복 처치하지 않는다.
5. **`createInitialState`/`stepGame` 반환값**: 다른 신규 콘텐츠(Jumper/Turret/씨앗)와 동일한 패턴으로 `chestnutRollers` 필드를 초기화·반환에 추가했다.

### 검증 (pure-logic 시뮬레이션 45종 + tsc + 6분 실주행 스트레스 테스트 + Playwright)
- `npx tsc --noEmit` 통과.
- 배치 검증: 정확히 2기가 배치되고, 시작 위치가 자기 `[minX,maxX]` 안에 있으며, 기존 적 순찰 구간·피스톤·Bio-Coil·Steam Blower 어느 것과도 x구간이 겹치지 않음을 확인.
- 전이 검증: walk→windup(방향 잠금+정지)→rolling(ROLL_SPEED)→recover(정지)→walk(새 cooldown) 전체 사이클, cooldown이 남아있는 동안 재트리거 차단, cooldown 소진 후 재트리거 허용을 확인.
- 감지 조건 검증: x범위 안이어도 플레이어가 롤러보다 훨씬 높이(머리 위) 있으면 트리거 안 됨, 같은 높이여도 280px 밖이면 트리거 안 됨을 확인.
- 경계 조기종료 검증: 좁은 방향으로 구르게 유도했을 때 `ROLL_DURATION`을 다 채우지 못하고 자기 경계에서 즉시 `recover`로 전환되며 `x`가 경계를 넘지 않음을 확인.
- 무적/피해 검증: `rolling` 중엔 화살이 명중해도 소멸하지 않고(화살이 그냥 통과) 스톰프도 처치로 이어지지 않지만, 접촉 시 플레이어는 정상적으로 피해를 입음(무적시간 존중)을 확인. `windup`/`recover`/`walk`는 화살·스톰프 각각으로 정상 처치됨을 확인.
- 기존 회귀 검증: 이동/대시 속도, Cogmite 감지+돌진, Root-Hook 그래플 부착, 체크포인트 갱신, 보스전 무피해 클리어(반응형 봇) 전부 이상 없음.
- 6분(21600프레임) 레벨 실주행 스트레스 테스트: NaN/Infinity 없음, 두 롤러 모두 한 번도 자기 `[minX,maxX]`를 벗어나지 않음, 화살/씨앗 자원 폭주 없음.
- Expo 웹 + Playwright: 시작 화면 로드→Start 클릭까지 콘솔 에러 0(신규 롤러는 아직 전용 렌더링 컴포넌트가 없어 화면엔 보이지 않음 — 정상, 로직은 동작 중).
- 검증 중 표면화된 것은 전부 **테스트 스크립트 자체의 설정 실수**였다(실제 로직 결함 아님): (a) "자연 종료" 케이스를 좁은 160px 구간(roller1)으로 테스트했다가, 롤러가 자기 `minX`에서 스폰돼 플레이어 쪽(우측)으로 128px 만에 경계에 부딪혀 조기 종료된 것을 "버그"로 오인 → 넓은 500px 구간(roller2)으로 바꿔 재확인, (b) "쿨다운 소진 후 재트리거"를 실시간 프레임 진행으로 검사하다가, 정지해 있던 테스트용 플레이어가 롤러의 실제 구르기 경로 한가운데 서 있어서 롤러가 지나갈 때마다 접촉 피해를 입고(명세대로 정상 동작) 결국 목숨을 잃어 체크포인트로 멀리 리스폰되는 바람에 감지 범위 밖으로 벗어난 것을 "재트리거 안 됨"으로 오인 → 쿨다운과 플레이어 위치를 직접 지정하는 격리된 상태로 재검증.

### 영향
- 기존 이동/대시/그래플/체크포인트/Gear Socket/보스전/Jumper/Turret/화살 로직은 전혀 변경하지 않았다(회귀 테스트로 확인).
- Route Gate/Bloom Shift/처치 후 발판 등 폐기된 시스템은 재도입하지 않았다.

### 남은 문제 (Codex에게 인계)
- `ChestnutRollerView` 렌더링 컴포넌트 미구현 — `AI_COLLAB.md` REVIEW REQUESTS에 `phase`/`facing`만으로 그릴 수 있는 인터페이스를 정확히 남겨뒀다.
- 실제 터치 플레이에서 Chestnut Roller의 구르기 예고/무적 구간이 명확히 읽히는지는 Codex의 실플레이 검수가 필요하다(로직 수치는 검증했지만 체감은 별개).

## 2026-08-28 (48) — 탐험 보상 캐시 디자인·구현 명세 준비 (Codex)

### 목표
- 사용자가 지적한 “무엇이 나올까?” 기대를 단순 코인 줄이 아닌 짧은 탐험 보상 루프로 만든다.
- 마리오의 ? 블록을 복제하지 않고, 밝은 숲 원정의 고목 상자와 봉인 꼬투리로 재해석한다.

### 구성
- `Root Cache`: 금 간 고목 상자. 대시로만 부순다.
- `Relic Pod`: 화살로만 여는 봉인 씨앗 꼬투리. 활 획득 후의 선택 경로에 둔다.
- 열기 전에는 결과를 알 수 없지만 빈 보상은 없으며, 각 캐시는 레벨 데이터에서 미리 정한 Sunseed 묶음/생명 회복/FLOW 충전 중 하나를 준다.
- 보상은 인벤토리나 랜덤 상점으로 확장하지 않고 즉시 적용한다. Codex는 개봉 순간 파편·빛·보상 팝업을 담당한다.

### 아트 출처
- `assets/sprites/treasure_cache_v1/root_cache.png`
- `assets/sprites/treasure_cache_v1/relic_pod.png`
- 두 파일 모두 Codex 내장 이미지 생성 도구로 만든 자체 제작 투명 PNG. 외부 에셋/제3자 라이선스 사용 없음.

### 협업 요청
- `AI_COLLAB.md`에 Claude용 P1 인터페이스/개봉 조건/보상 수치/배치 제한/회귀 검증 기준을 기록했다. 상태가 전달되면 Codex가 바로 화면과 연출을 연결한다.

## 2026-08-28 (49) — 탐험 보상 캐시 상태·물리·레벨 배치 구현 (Claude)

### 목표
- (48)에서 Codex가 남긴 명세(`rootCache`=대시로만 개봉, `relicPod`=화살로만 개봉, 3종 고정 보상, 빈 보상 없음)를 `src/game/*`에 최소 구현한다. Chestnut Roller에 이어 같은 세션에서 Codex의 두 번째 REVIEW REQUEST를 처리했다.

### 변경
1. **`src/game/types.ts`**: `TreasureCache`(`id,x,y,width,height,kind,reward,opened`, `kind:'rootCache'|'relicPod'`, `reward:'sunseedBurst'|'lifeBloom'|'flowSpark'`)와 `LootReveal`(`id,reward,x,y,timeLeft`) 추가. 명세에 적힌 필드명을 그대로 사용했다. `Level.treasureCaches`, `GameState.treasureCaches`/`lootReveals`/`lootRevealSeq`(다른 시퀀스 카운터와 동일한 패턴) 추가.
2. **`src/game/constants.ts`**: `TREASURE_CACHE_WIDTH/HEIGHT=28`(명세에 크기가 없어 CogPickup(22)/BowPickup(24)과 비슷한 값으로 판단), `SUNSEED_BURST_SCORE=8`, `LIFE_BLOOM_LIVES=1`, `FLOW_SPARK_GAUGE=30`(요청 수치 그대로), `LOOT_REVEAL_DURATION=1`(팝업 표시 시간, 명세에 없어 직접 정함 — EFFECT_DURATION(0.5)보다 좀 더 길게 잡아 보상 텍스트를 읽을 시간을 줬다).
3. **`src/game/level.ts`**: 새 좌표를 계산하지 않고, 이미 존재하는 "보너스" 발판 5개(`bonusPlatforms` 배열: bonus-early/mid/late/final/deep)를 그대로 재사용해 `makeTreasureCaches()`를 작성했다. 이 발판들은 세션 (35)에서 이미 "실패해도 안전한 동선으로 이어지는 숙련 보상 경로"로 확립된, 필수 동선을 막지 않는 선택 경로라 명세의 배치 조건(선택 경로/높은 발판/되돌아볼 지점)과 정확히 일치했다 — 새로 스캔할 필요가 없었다. 각 캐시는 해당 발판 위에 중앙 정렬로 얹었다(`y = platform.y - CACHE_HEIGHT`). 배치: `cache-root1`(bonus-early, sunseedBurst), `cache-root2`(bonus-mid, flowSpark), `cache-root3`(bonus-late, sunseedBurst), `cache-pod1`(bonus-final, lifeBloom), `cache-pod2`(bonus-deep, sunseedBurst) — relicPod 2개 모두 유물 활(x=250)보다 한참 뒤(x=4450/5700)라 "활 획득 전 구간에 relicPod 금지" 조건을 만족한다. 보상 종류 배분(어느 캐시에 어떤 보상을 줄지)은 명세에 없어 직접 판단했다 — 상대적으로 귀한 lifeBloom을 유일한 relicPod에 배치해 활을 쓰는 보람을 주고, 나머지는 sunseedBurst/flowSpark로 채웠다.
4. **`src/game/physics.ts`**: `resolvedCogPickups` 블록 직후에 캐시 해석 로직을 추가했다.
   - `rootCache`: `player.dashTimer > 0 && rectIntersect(player, cache)` — Spore Sprite의 "대시 중 접촉만 처치" 패턴을 그대로 재사용.
   - `relicPod`: `findArrowHit(cache)` — 다른 화살 타겟(Jumper/Turret/씨앗 등)과 동일하게 `consumedArrowIds`로 한 화살이 여러 대상을 중복 처치하지 못하게 공유.
   - 이미 `opened`인 캐시는 두 조건 모두 건너뛰고 그대로 반환해 재개봉을 원천 차단.
   - 보상 적용: `sunseedBurst`는 `score += SUNSEED_BURST_SCORE`, `lifeBloom`은 `lives = Math.min(STARTING_LIVES, lives + LIFE_BLOOM_LIVES)`로 직접 클램프, `flowSpark`는 이미 함수 안에 있던 `gaugeGain`(대시/스톰프가 채우는 Overdrive 콤보 게이지 변수)에 `+= FLOW_SPARK_GAUGE`만 더해 기존 클램프·게이지 충전·Overdrive 발동 로직을 통째로 재사용했다 — 새 클램프 코드를 따로 안 만들어도 됐다.
   - 개봉 시 `pushEffect('pickup', ...)`(기존 코인 픽업과 같은 이펙트 종류 재사용, 새 `EffectKind` 추가 안 함)과 신규 `LootReveal` 1개를 함께 남긴다. `LootReveal`은 `effects`와 동일한 "매 프레임 `timeLeft -= dt` 감쇠 후 0 이하 제거" 패턴으로 수명 관리한다.
5. **`createInitialState`/`stepGame` 반환값**: 다른 신규 콘텐츠와 동일한 패턴으로 `treasureCaches`/`lootReveals`/`lootRevealSeq` 필드를 초기화·반환에 추가했다.

### 검증 (pure-logic 시뮬레이션 33종 + tsc + 6분 실주행 스트레스 테스트 + Playwright)
- `npx tsc --noEmit` 통과.
- 배치 검증: 정확히 5개(rootCache 3+relicPod 2)가 생성되고 전부 시작 상태 `opened=false`, relicPod 전부 유물 활 위치보다 뒤, 모든 캐시가 새 좌표가 아니라 기존 보너스 발판 위에 정확히 얹혀 있음을 확인.
- 개봉 조건 검증: rootCache는 대시 충돌에만 열리고(일반 접촉·화살 모두 무효), relicPod는 화살 충돌에만 열림(대시·일반 접촉 모두 무효)을 각각 교차로 확인. 이미 열린 캐시는 다시 열리지 않고 점수가 중복 지급되지 않음을 확인.
- 보상 검증: sunseedBurst가 정확히 +8점, lifeBloom이 부족한 생명을 +1(정확한 수치)하고 이미 최대(STARTING_LIVES)일 땐 넘치지 않음, flowSpark가 중간 게이지에 정확히 +30을 더하고 상한 근처에서는 OVERDRIVE_MAX를 넘지 않으며 오히려 가득 차면 정상적으로 Overdrive가 발동됨을 확인.
- LootReveal 검증: 캐시 하나를 열면 정확히 1개의 LootReveal이 생성되고, `LOOT_REVEAL_DURATION` 경과 후 자동 소멸함을 확인.
- 기존 회귀 검증: 이동 속도, Cogmite 감지, Root-Hook 그래플, 체크포인트, **Chestnut Roller 감지**(이전 세션에서 막 추가한 기능도 재확인), 보스전 무피해 클리어 전부 이상 없음.
- 6분(21600프레임) 레벨 실주행 스트레스 테스트: NaN 없음, `lootReveals` 배열 폭주 없음, 화살/씨앗 자원 폭주 없음.
- Expo 웹 + Playwright: 시작 화면 로드→Start 클릭까지 콘솔 에러 0(신규 캐시는 아직 전용 렌더링 컴포넌트가 없어 화면엔 보이지 않음 — 정상, 로직은 동작 중).
- 검증 중 나온 실패 2건은 전부 **테스트 스크립트 자체의 설정 실수**였다(실제 로직 결함 아님): (a) 대시 개봉 테스트에서 플레이어 y좌표를 캐시와 똑같이 맞췄더니, 캐시가 얹힌 발판(높이 16px) 안쪽으로 플레이어(높이 40px)가 파묻혀서 대시 이동이 그 발판에 "벽처럼" 충돌해 옆으로 튕겨나가는 바람에 캐시를 아예 못 건드린 것을 "안 열림 버그"로 오인 → 플레이어를 발판 위에 정확히 서 있는 자세(발판 상단에 발이 닿는 y)로 고쳐 재확인, (b) sunseedBurst 점수 검증에서 플레이어를 bonus-early 발판 근처(x≈496)에 뒀는데 그 자리에 원래 있던 기존 Sunseed 코인 2개(x=490/520, 개당 10점)를 캐시 개봉과 같은 프레임에 같이 주워버려 점수가 8이 아니라 28로 나온 것을 오인 → 해당 코인들을 테스트 시작 전 미리 수집 처리해 캐시 보상만 분리 측정.

### 영향
- 기존 이동/대시/그래플/체크포인트/Gear Socket/보스전/Jumper/Turret/화살/Chestnut Roller 로직은 전혀 변경하지 않았다(회귀 테스트로 확인).
- Route Gate/Bloom Shift/처치 후 발판 등 폐기된 시스템은 재도입하지 않았다.
- `CLAUDE.md` 5.1장의 "(다음 구현)" 표기를 제거했다(로직 구현 완료, 프레젠테이션은 대기 중).

### 남은 문제 (Codex에게 인계)
- `TreasureCacheView`/`LootReveal` 팝업 렌더링 컴포넌트 미구현 — `AI_COLLAB.md` REVIEW REQUESTS에 `kind`/`opened`/`reward`만으로 그릴 수 있는 인터페이스를 정확히 남겨뒀다.
- 어느 캐시에 어떤 보상을 배정할지(예: lifeBloom을 유일한 relicPod에 둔 것)는 로직상 임의 판단이라, Codex나 사용자가 밸런스상 다른 배분을 원하면 `level.ts`의 `makeTreasureCaches()` defs 배열만 바꾸면 된다.

## 2026-08-28 (50) — 유물 활 탄약 경제 추가 (Codex 설계)

### 사용자 피드백
- 무제한 화살은 원거리 공격이 다른 이동·전투 선택을 압도해 밸런스가 맞지 않는다.
- 탐험 보상에서 화살을 얻어, 상자를 열 때 더 큰 기대가 생기게 한다.

### 확정 규칙
- 유물 활은 획득 시 3발로 시작하며 최대 5발을 보관한다. 발사 성공마다 1발을 소비한다.
- `arrowBundle` 보상은 +1발(최대치 제한)이다. 보상 캐시에는 기존 점수/생명/FLOW와 함께 이 보상이 들어간다.
- 화살 0발이면 공격 버튼은 잠기고 발사/소비가 일어나지 않는다. 체크포인트 부활 시 현재 화살 수는 유지한다.
- 첫 번째 화살 전용 Relic Pod보다 앞선 선택 경로에 화살 묶음을 반드시 둬, 탄약 부족이 필수 진행을 막지 않게 한다.

### 협업 요청
- `AI_COLLAB.md` P1 요청과 `CLAUDE.md`에 타입·초기치·소비 조건·보상·배치·회귀 검증 기준을 갱신했다. Claude가 상태/물리를 구현하고, Codex는 탄약 HUD·빈 버튼·획득 연출을 구현한다.

## 2026-08-28 (51) — Chestnut Roller 상태별 프레젠테이션 연동 (Codex)

### 변경
- Claude가 추가한 `walk`/`windup`/`rolling`/`recover` 상태를 `ChestnutRollerView`로 실제 게임 화면에 연결했다.
- 걷기·예고·회복에는 팔다리가 보이는 밤송이 갑옷 수호자를, 구르기에는 눈·팔다리가 완전히 들어간 둥근 껍질 공을 사용했다.
- 구르는 동안에는 고속 회전, 짧은 먼지 꼬리, 금빛 보호 고리를 보이고, 예고 때는 바닥 고리와 압축 실루엣을 사용한다. 따라서 “이때는 공격이 안 통한다”와 “곧 구른다”를 화면만으로 구분할 수 있다.
- 물리 수치/무적 판단/충돌/배치는 변경하지 않았다.

### 검증
- `npx tsc --noEmit` 통과.

## 2026-08-28 (52) — 유물 활 탄약 경제 구현 (Claude)

### 목표
- (50)에서 Codex가 남긴 명세(활 픽업 시 3발/최대 5발, 발사마다 1발 소비, `arrowBundle` 보상으로 +1 회복, 첫 Relic Pod보다 앞에 배치)를 `src/game/*`에 최소 구현한다. 같은 세션에서 Chestnut Roller·탐험 보상 캐시에 이은 세 번째 REVIEW REQUEST.

### 변경
1. **`src/game/types.ts`**: `Player`에 `arrows: number`, `maxArrows: number` 추가. `TreasureReward`에 `'arrowBundle'` 추가.
2. **`src/game/constants.ts`**: `RELIC_BOW_STARTING_ARROWS=3`, `RELIC_BOW_MAX_ARROWS=5`, `ARROW_BUNDLE_ARROWS=1` — 요청 수치(이미 Codex가 6/12/4에서 3/5/1로 한 번 낮춘 최종값) 그대로 상수화.
3. **`src/game/physics.ts`**:
   - `createInitialState`: `arrows: 0, maxArrows: 0`(활 미획득 상태에서는 의미 없는 값이므로 0).
   - 활 픽업 블록(`bowPickup.collected` 전이 지점)에 `player.arrows = RELIC_BOW_STARTING_ARROWS`, `player.maxArrows = RELIC_BOW_MAX_ARROWS`를 추가 — `hasBow = true`와 같은 프레임, 같은 위치에서 설정해 "픽업 즉시 탄약 보유" 순간을 보장했다.
   - 발사 조건문에 `&& player.arrows > 0`을 추가하고, 발사 성공 분기 맨 앞에 `player.arrows -= 1`을 추가 — 기존 쿨다운 체크와 정확히 같은 위치라 "쿨다운 중엔 아예 조건문에 안 들어가서 소비도 안 됨"이라는 기존 보장이 탄약 0에도 그대로 적용된다.
   - 탐험 보상 캐시 해석 블록의 보상 분기에 `arrowBundle` 케이스를 추가: `player.arrows = Math.min(player.maxArrows, player.arrows + ARROW_BUNDLE_ARROWS)`.
   - 부활(`applyHit`) 로직은 건드리지 않았다 — 이 함수는 `lives`/`invulnerableFor`/`x`/`y`/`vx`/`vy`/`mirrorTrail`만 다루므로, `arrows`/`maxArrows`는 `hasBow`와 마찬가지로 체크포인트 부활에서 자동으로 보존된다(별도 코드 불필요, 기존 설계를 그대로 재사용).
4. **`src/game/level.ts`**: 새 캐시나 좌표를 추가하지 않고, 기존 `cache-root3`(bonus-late 발판, x=2650, 대시로만 열리는 rootCache)의 보상을 `sunseedBurst`에서 `arrowBundle`로 교체했다. 첫 Relic Pod인 `cache-pod1`(x=4450)보다 한참 앞이라 명세의 "탄약 부족이 필수 진행을 막지 않게" 조건을 만족한다. Codex의 요청 문구("기존 캐시 재활용 또는 6번째 캐시 추가 모두 가능, 배치는 Claude 판단")에 따라 새 좌표를 계산하지 않는 더 단순한 쪽을 택했다.

### 검증 (pure-logic 시뮬레이션 22종 + tsc + 6분 실주행 스트레스 테스트 + Playwright)
- `npx tsc --noEmit` 통과.
- 픽업 전/후 검증: 활 획득 전 `arrows=0`/`maxArrows=0`이고 공격 입력이 화살을 만들지 않음, 픽업 즉시 `arrows=3`/`maxArrows=5`가 정확히 부여됨을 확인.
- 소비 검증: 발사 1회마다 정확히 1발 소비, 탄약을 다 쓰면 발사가 완전히 막힘(화살 생성 안 됨, 쿨다운도 소모 안 됨, 음수로 내려가지 않음)을 확인.
- 회복 검증: `cache-root3`가 정확히 `arrowBundle` 보상을 가짐, 개봉 시 정확히 `ARROW_BUNDLE_ARROWS`(1)만큼 회복, 이미 최대치일 때 오버플로 없음, 이 캐시가 첫 Relic Pod보다 앞에 위치함을 확인.
- 보존 검증: `DEATH_Y` 낙사로 강제 사망시켜 체크포인트 부활을 유도한 뒤에도 탄약 수가 그대로 유지됨(목숨은 정상적으로 줄어듦으로 실제 부활 경로를 탔는지 별도 확인), 완전 재시작(새 `createInitialState`)에서만 0으로 리셋됨을 확인.
- 기존 회귀 검증: 이동 속도, 화살로 몬스터 처치(이제 유한 탄약이어도 명중 시 처치 규칙은 동일), 보스전 스톰프만으로 무피해 클리어 전부 이상 없음.
- 6분(21600프레임) 레벨 실주행 스트레스 테스트: 탄약이 한 번도 음수이거나 `maxArrows`를 초과하지 않음, 발사 중인 화살 개수 폭주 없음, NaN 없음.
- Expo 웹 + Playwright: 시작 화면 로드→Start 클릭까지 콘솔 에러 0, 스크린샷으로 기존 화면(주인공/Brambleling/코인/HUD) 정상 렌더링 확인.
- 검증 중 나온 실패 4건은 전부 **테스트 스크립트 자체의 실수**였다(실제 로직 결함 아님): (a)(b) 탄약 소진 루프의 `for` 조건에 매 반복 갱신되는 `player.arrows`를 직접 참조해서, 탄약이 줄어들수록 루프 상한도 함께 줄어들어 실제로는 다 소진되기 전에 루프가 멈춘 것을 "탄약이 0이 안 됨"으로 오인 → 루프 시작 전 값을 별도 변수에 고정해 수정, (c)(d) "최대치에서 arrowBundle이 넘치지 않는지" 검증에서 픽업 직후 상태(탄약 3/최대 5, 즉 아직 최대치가 아님)를 "최대치"라고 잘못 가정 → 테스트 안에서 탄약을 명시적으로 최대치까지 채운 뒤 검증하도록 수정.

### 영향
- 기존 이동/대시/그래플/체크포인트/Gear Socket/보스전/Jumper/Turret/Chestnut Roller/탐험 보상 캐시 로직은 전혀 변경하지 않았다(회귀 테스트로 확인).
- 화살이 이제 유한 자원이 되었으므로, 이후 세션에서 "화살을 무제한으로 연사할 수 있다"는 가정에 의존하는 밸런스/테스트 코드가 있다면 더 이상 유효하지 않다는 점을 남겨둔다(이번 검증 범위에서는 그런 기존 가정을 발견하지 못했다).

### 남은 문제 (Codex에게 인계)
- 활 탄약 HUD(현재/최대 표시, 0발 시 공격 버튼 잠금) 미구현 — `AI_COLLAB.md` REVIEW REQUESTS에 `player.arrows`/`player.maxArrows`만으로 표시 가능한 인터페이스를 정확히 남겨뒀다.
- 실제 터치 플레이에서 3발/5발이라는 수치가 체감상 너무 인색하거나 너무 넉넉한지는 사람의 실플레이 검수가 필요하다(로직·클램프는 검증했지만 밸런스 체감은 별개).

## 2026-08-28 (53) — 16:9 게임 프레임 및 비주얼 리빌드 착수 (Codex)

### 사용자 확정 기준
- 게임은 별도 HUD/컨트롤 띠로 화면을 자르지 않고, 사용자가 승인한 고급 숲 액션 게임 시안과 같은 **16:9 전체 프레임**으로 구성한다.
- 월드가 프레임 전체를 사용하고, HUD·터치 컨트롤은 월드 위에만 겹친다.
- 기존의 평면 도형·서로 다른 화풍·과대 버튼을 유지한 채 덧칠하지 않는다. 배경·지형·캐릭터·적·수집품·HUD를 공통 아트 디렉션으로 재구성한다.

### 이번 변경
- `GameScreen`을 중앙 고정 16:9 게임 프레임으로 전환했다. 기기 화면비가 더 넓어도 게임 내부 구성은 늘어나거나 잘리지 않고, 여백은 프레임 밖에서 처리된다.
- 기존 HUD/조작부 예약 높이를 제거해 스테이지가 화면 세로를 온전히 사용하도록 했다.
- 새 고해상도 페인터리 알파인 숲 원화 `assets/backgrounds/scene_alpine_valley_v4.png`를 추가하고 첫 숲 장면에 적용했다.
- 향후 다층 패럴랙스·스프라이트 애니메이션·파티클을 위한 `@shopify/react-native-skia` 렌더링 기반을 추가했다. 게임 규칙·AABB 물리·레벨 데이터는 변경하지 않았다.

### 검증
- `npx tsc --noEmit` 통과.
- 첫 Android preview APK는 이 변경 직전에 이미 서버에 제출되어 완성됐다. 새 16:9 리빌드 묶음은 시각 QA 완료 후 별도 APK로 다시 빌드한다.

