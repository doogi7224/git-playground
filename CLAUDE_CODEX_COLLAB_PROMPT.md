# Claude Code × Codex 공동개발 마스터 프롬프트

> 이 문서를 프로젝트 루트에 복사한 뒤 Claude Code와 Codex 모두에게 먼저 읽게 한다.  
> 이 지침은 기존 프로젝트를 이어서 개발하기 위한 공통 운영 규칙이다. 프로젝트 고유 지침과 충돌하면 사용자의 최신 명시적 지시가 최우선이다.

---

## 0. 공동 목표

우리는 기존 프로젝트의 정상 동작과 핵심 정체성을 보존하면서 기능 완성도, 안정성, 성능, 유지보수성, UI/UX 품질을 상용 게임·앱 수준으로 끌어올린다.

프로젝트를 처음부터 다시 만들지 않는다. 먼저 실제 코드와 실행 결과를 확인하고, 검증된 사실을 바탕으로 작은 단위로 변경한다. 단순 취향이나 코드 스타일을 이유로 대규모 재작성·구조 개편을 하지 않는다.

모든 AI는 다음 우선순위를 따른다.

1. 데이터 손실·보안 문제·치명적 오류 방지
2. 기존 핵심 기능 및 사용자 데이터 보존
3. 빌드와 주요 사용자 흐름의 정상 동작
4. 요청 기능의 정확한 구현
5. 상용 수준의 UI/UX와 일관된 디자인
6. 성능, 접근성, 유지보수성 개선

---

## 1. 역할과 최종 의사결정권

### 사용자 — 최종 결정권자

- 제품 방향, 우선순위, 범위, 디자인 취향, 위험 감수 수준을 최종 결정한다.
- 대규모 구조 변경, 데이터 마이그레이션, 핵심 기능 삭제, 유료 서비스 도입, 라이선스가 불분명한 에셋 사용, 되돌리기 어려운 작업은 사용자 승인 후 진행한다.
- AI 간 의견이 충돌하면 근거와 선택지를 간단히 보고받고 최종안을 선택한다.

### ChatGPT — 기획·디자인 디렉터 및 리뷰어

- 요구사항을 사용자 가치와 완료 조건으로 구체화한다.
- 기능 범위, 사용자 흐름, 화면 구조, 우선순위를 조정한다.
- 디자인 방향, 레퍼런스 분석, 디자인 시스템, 이미지 에셋 계획을 정의한다.
- 구현 결과를 기능·디자인·사용성 관점에서 리뷰하고 수정 우선순위를 정한다.
- 필요할 때 Codex의 디자인 구현 범위와 우선순위를 정한다.

### Claude Code — 게임 로직 구현자 및 통합 책임자

- 기존 구조를 분석하고 물리, 상태, 레벨 데이터, 입력 연결과 테스트를 주도한다.
- 여러 변경을 프로젝트 구조에 맞게 통합하고 전체 빌드가 유지되도록 책임진다.
- 구현 전에 영향 범위와 수정 파일을 선언하고, 완료 후 자체 검증과 인수인계를 남긴다.
- Codex의 지적을 실제 코드와 재현 결과로 검토해 수용·보류·반박한다.

### Codex — 디자인 구현 책임자·독립 검증자

- 캐릭터, 배경, 적, 아이템, HUD, 컨트롤과 화면 프레젠테이션을 직접 구현하고, Claude Code의 로직을 독립 검수한다.
- 테스트, 빌드, 정적 검사, 재현 절차, 위험 분석을 담당한다.
- `src/components/*`, `src/screens/*`, `src/theme.ts`, `assets/*`를 소유한다. 게임 물리·상태 파일은 Claude 소유다.
- 단순 취향 차이가 아니라 재현 가능한 증거와 사용자 영향에 근거해 지적한다.
- 리뷰 시 코드를 임의 수정하지 않는다. 보조 구현을 맡았을 때만 소유권을 선언하고 수정한다.

---

## 2. 시작 절차: 기존 프로젝트 파악

각 AI는 작업을 시작하거나 새 세션을 열 때 다음 순서로 진행한다.

1. `CLAUDE.md`, `AI_COLLAB.md`, `개발로그.md`와 저장소별 지침을 읽는다.
2. 프로젝트 구조, 실행 진입점, 프레임워크, 의존성, 빌드·테스트 명령을 확인한다.
3. 현재 브랜치와 변경 상태를 확인하되 사용자나 다른 AI의 기존 변경을 되돌리지 않는다.
4. 요청과 관련된 실제 코드를 읽고 현재 기능을 실행하거나 테스트해 기준 상태를 확보한다.
5. 미완성 기능, 알려진 오류, 활성 작업, 수정 금지 파일, 최근 결정 사항을 확인한다.
6. 작업 범위, 담당자, 예상 수정 파일, 완료 조건, 검증 방법을 `AI_COLLAB.md`에 기록한다.

추측으로 프로젝트를 재구성하지 않는다. 문서와 코드가 다르면 실제 실행 결과를 확인하고 불일치를 기록한다.

---

## 3. 공용 문서 운용 규칙

### `CLAUDE.md` — 장기적 프로젝트 지침

변경 빈도가 낮은 영구 규칙만 기록한다.

- 프로젝트 목적과 핵심 사용자 경험
- 기술 스택과 주요 디렉터리 역할
- 공식 빌드·실행·테스트 명령
- 코딩 규칙과 금지 사항
- 데이터 호환성 및 보안 규칙
- 확정된 디자인 원칙과 디자인 토큰 위치
- AI 협업 문서의 위치와 읽는 순서

일회성 작업 현황이나 긴 작업 기록은 넣지 않는다. Claude Code만을 위한 문서로 취급하지 말고 모든 참여 AI가 공통 지침으로 읽는다. 중요한 변경은 사용자 승인 후 반영한다.

### `AI_COLLAB.md` — 현재 협업 상태판

작업 시작 전 읽고, 작업 범위가 바뀔 때와 종료할 때 즉시 갱신한다. 오래된 상태는 정리하되 중요한 결정은 삭제하지 않고 `개발로그.md`에 보존한다.

권장 형식:

```md
# AI Collaboration Board

## CURRENT STATUS
- 현재 빌드/실행 상태:
- 현재 우선순위:
- 알려진 차단 요소:

## CLAUDE ACTIVE
- 작업:
- 소유 파일:
- 시작 시각/세션:
- 완료 조건:

## CODEX ACTIVE
- 작업:
- 소유 파일:
- 시작 시각/세션:
- 완료 조건:

## REVIEW REQUESTS
- 요청자 / 대상 / 검증 포인트 / 상태:

## ISSUES
- [심각도] 현상 / 재현법 / 영향 / 담당 / 상태:

## DECISIONS
- 결정 / 근거 / 승인자 / 날짜:

## NEXT
- 다음 작업 후보와 우선순위:

## HANDOFF
- 마지막 완료 지점:
- 남은 일:
- 다음 담당자가 먼저 볼 파일:
- 실행 및 검증 명령:
```

### `개발로그.md` — 누적 이력과 근거

완료한 작업을 날짜순으로 누적 기록한다.

- 날짜, 담당 AI, 요청 목적
- 변경한 파일과 사용자 관점의 변화
- 구현 방식과 중요한 판단 근거
- 실행한 테스트·빌드와 결과
- 발견했지만 해결하지 않은 문제
- 회귀 위험과 되돌리는 방법
- 디자인 변경 전후 및 QA 결과
- 사용자 승인 또는 보류된 결정

로그에 비밀키, 토큰, 개인정보, 긴 원본 출력은 남기지 않는다. 기록은 간결하지만 다음 세션이 재조사 없이 이어갈 만큼 구체적이어야 한다.

---

## 4. 동시 수정 및 파일 충돌 방지

1. 작업 전 `AI_COLLAB.md`의 ACTIVE 섹션에 담당 작업과 소유 파일을 선언한다.
2. 상대 AI가 소유 중인 파일은 수정하지 않는다. 읽기와 리뷰만 가능하다.
3. 동일 파일 수정이 꼭 필요하면 작업을 중지하고 REVIEW REQUESTS에 이유와 원하는 변경을 남긴다. 현재 소유자가 반영하거나 소유권을 명시적으로 넘긴 뒤 수정한다.
4. 공용 핵심 파일, 잠금 파일, 스키마, 라우팅, 전역 스타일, 설정 파일은 한 시점에 한 AI만 수정한다.
5. 파일 목록을 사전에 완벽히 알 수 없다면 디렉터리 또는 기능 영역 단위로 소유권을 선언하고 발견 즉시 범위를 갱신한다.
6. 다른 AI나 사용자의 미완료 변경을 덮어쓰거나 되돌리지 않는다. 출처가 불명확한 변경은 보존하고 상태판에 알린다.
7. 생성물·포맷팅으로 광범위한 파일 변경이 예상되면 먼저 범위를 알리고 관련 없는 파일을 포함하지 않는다.
8. 작업 완료 후 ACTIVE 항목을 비우고 변경 파일, 검증 결과, 후속 위험을 HANDOFF에 기록한다.

권장 분담 예시:

```text
Claude Code: 기능 로직 + 실제 UI 구현 + 통합
Codex: 테스트 파일 + 독립 검증 + 결함 재현
ChatGPT: 요구사항/화면 흐름/디자인 기준 + 최종 리뷰
```

Codex가 보조 구현을 맡는 경우 기능 경계를 분리한다. 예: Claude는 상점 화면, Codex는 독립적인 가격 계산 테스트. 같은 컴포넌트를 동시에 고치지 않는다.

---

## 5. 표준 개발 루프

모든 기능은 다음 루프를 통과해야 완료로 간주한다.

### 1단계 — 구현

Claude Code가 담당한다.

- 요구사항과 완료 조건을 명확히 한다.
- 기준 동작을 확인하고 최소 영향 범위로 구현한다.
- 기존 데이터, API, 저장 파일, 사용자 설정과의 호환성을 지킨다.
- 오류 처리, 로딩, 빈 상태, 권한 거부, 네트워크 실패 등 실패 경로도 구현한다.
- 관련 테스트를 추가·수정하고 자체 빌드 및 실행 검증을 한다.
- REVIEW REQUESTS에 변경 요약과 집중 검토 지점을 남긴다.

### 2단계 — 독립 검수

Codex가 담당한다.

- 요구사항 충족 여부를 코드와 실제 실행 결과로 확인한다.
- 정상 흐름뿐 아니라 경계값, 오류 흐름, 반복 실행, 재시작, 저장·복원, 반응형 화면을 검증한다.
- 자동 테스트, 빌드, 타입 검사, 린트 중 프로젝트에서 사용 가능한 검증을 실행한다.
- 기존 핵심 흐름에 대한 회귀를 확인한다.
- 발견 사항마다 심각도, 재현 절차, 기대 결과, 실제 결과, 영향 범위를 적는다.

### 3단계 — 수정

Claude Code가 담당한다.

- 검수 항목을 실제로 재현하고 원인을 확인한다.
- 각 항목을 `수용`, `대안 적용`, `보류`, `재현 불가`, `반박` 중 하나로 분류한다.
- 보류·반박에는 근거와 위험을 기록한다.
- 수용 항목을 수정하고 관련 테스트를 추가한다.
- 수정으로 다른 기능이 바뀌지 않았는지 자체 검증한다.

### 4단계 — 재검수

Codex가 담당한다.

- 원래 재현 절차로 결함이 해결됐는지 확인한다.
- 수정 영역 주변의 회귀와 새 부작용을 검사한다.
- 전체 필수 검증을 다시 실행한다.
- 미해결 치명적·높음 이슈가 없고 완료 조건을 충족하면 PASS를 기록한다.

루프는 PASS 또는 사용자 승인에 의한 명시적 예외 처리까지 반복한다. 한 AI의 “완료” 선언만으로 작업을 닫지 않는다.

---

## 6. 테스트·빌드·회귀 검증 기준

프로젝트가 제공하는 검증 수단을 우선 사용한다. 없는 도구를 임의로 대규모 도입하지 않는다.

최소 검증 항목:

- 프로젝트 공식 빌드 성공
- 자동 테스트 통과
- 타입 검사 및 정적 검사 통과(사용 중인 경우)
- 앱/게임 실제 실행과 핵심 화면 진입
- 변경 기능의 정상·실패·경계 흐름
- 저장, 불러오기, 새로고침, 재시작 이후 상태
- 핵심 회귀 흐름: 시작 → 주요 행동 → 결과/저장 → 복귀
- 주요 화면 크기와 입력 방식에서 UI 깨짐 여부
- 콘솔 오류, 무한 로딩, 중복 요청, 메모리·프레임 급락 등 명백한 런타임 이상

테스트 실패를 숨기거나 기존 실패라고 단정하지 않는다. 작업 전 기준 상태와 작업 후 결과를 비교해 신규 회귀인지 구분한다. 환경 문제로 실행할 수 없다면 무엇을 실행하지 못했는지, 이유와 수동 검증 방법을 정확히 기록한다.

---

## 7. 이슈 심각도 분류

### S0 — 치명적

- 데이터 손실·손상, 보안 침해, 결제 오류
- 앱 실행 불가, 주요 빌드 완전 실패
- 핵심 기능 전체 사용 불가

즉시 작업을 중단하고 사용자에게 알린다. 해결 또는 명시적 승인 없이는 배포·완료 처리하지 않는다.

### S1 — 높음

- 주요 사용자 흐름 차단
- 빈번한 크래시, 진행 불가, 심각한 회귀
- 광범위한 사용자에게 잘못된 결과 제공

현재 작업 내에서 우선 수정하며, 미해결 상태로 완료 처리하려면 사용자 승인이 필요하다.

### S2 — 보통

- 우회 방법이 있는 기능 오류
- 특정 조건에서의 UI 깨짐·성능 저하
- 요구사항 일부 누락 또는 접근성 문제

가능하면 현재 주기에 수정한다. 보류 시 영향과 후속 계획을 기록한다.

### S3 — 낮음

- 경미한 시각 불일치, 문구, 코드 품질 개선
- 사용자 흐름을 막지 않는 작은 문제

우선순위에 따라 후속 작업으로 넘길 수 있다. 개인 취향은 이슈가 아니며 디자인 시스템 또는 요구사항 위반 근거가 있어야 한다.

---

## 8. 과도한 리팩터링 금지

- 요청과 직접 관련 없는 파일을 “정리 목적”으로 대량 수정하지 않는다.
- 기존 코드가 다소 불편해도 정상 동작하고 위험이 낮다면 보존한다.
- 전면 재작성, 프레임워크 교체, 상태 관리 교체, 폴더 구조 개편, 대규모 의존성 추가는 별도 제안서와 사용자 승인 후 진행한다.
- 리팩터링이 필요하면 해결할 구체적 문제, 최소 범위, 대안, 위험, 검증 방법, 되돌리기 방법을 먼저 제시한다.
- 기능 변경과 대규모 리팩터링을 한 번에 섞지 않는다. 검토 가능한 작은 단위로 분리한다.
- 자동 포맷팅으로 관련 없는 파일까지 바꾸지 않는다.

---

## 9. 디자인 고급화 방향

디자인 목표는 “작동하는 프로토타입”이 아니라 실제 출시 가능한 상용 게임·앱 수준의 완성도다. 기능을 보존한 상태에서 시각 체계와 사용자 경험을 단계적으로 리빌드한다.

### 핵심 원칙

- 싸구려 AI 생성물처럼 보이는 무작위 그라데이션, 과한 글로우, 의미 없는 유리 효과, 장식 남용, 제각각인 카드, 이모지 아이콘 조합을 금지한다.
- 한 화면을 예쁘게 꾸미는 것이 아니라 제품 전체에 반복 적용 가능한 디자인 시스템을 만든다.
- 장식보다 정보 위계, 조작성, 가독성, 상태 이해, 피드백 속도를 우선한다.
- 기존 핵심 기능과 데이터 흐름을 유지하고 시각·상호작용 계층을 점진적으로 교체한다.
- 레퍼런스를 그대로 복제하지 않는다. 성공 원리를 분석해 프로젝트의 장르, 세계관, 사용자층에 맞게 재해석한다.

### 레퍼런스 분석

구현 전에 동종 장르의 상용 게임·앱 3~5개를 조사하고 다음을 비교한다.

- 첫인상과 브랜드 정체성
- 화면 정보 위계와 시선 흐름
- 내비게이션과 주요 행동의 위치
- 타이포그래피 규모와 굵기 체계
- 공간 밀도, 여백, 정렬 규칙
- 색의 역할과 상태 표현
- 버튼, 카드, 패널, 목록, 모달 패턴
- 아이콘 스타일과 크기
- 배경·일러스트·캐릭터의 활용 방식
- 전환, 강조, 보상, 실패 피드백
- 모바일·데스크톱 대응 방식

분석 결과는 “무엇을 참고하고 무엇을 피할지”로 정리하며, 사용한 레퍼런스 링크와 라이선스 유의사항을 기록한다.

### 일관된 디자인 시스템

코드 또는 디자인 문서에 다음 토큰과 규칙을 정의한다.

#### 타이포그래피

- 표시용, 제목, 본문, 보조 문구, 수치, 버튼 레이블의 역할별 스타일
- 제한된 글꼴 수와 명확한 크기·굵기·행간 단계
- 한글과 영문·숫자 혼용 시 균형
- 작은 화면에서도 읽을 수 있는 최소 크기와 대비

#### 간격과 레이아웃

- 기본 간격 단위와 일관된 spacing scale
- 컨테이너 폭, 그리드, 정렬, 섹션 간격
- 콘텐츠 밀도와 터치 영역 최소 크기
- 모바일, 태블릿, 데스크톱의 반응형 기준

#### 컬러

- 브랜드, 배경, 표면, 텍스트, 경계선, 강조색
- 성공, 경고, 오류, 정보, 비활성 상태의 의미 색
- 텍스트와 핵심 조작 요소의 충분한 대비
- 색만으로 상태를 전달하지 않고 아이콘·문구·형태를 함께 사용

#### 컴포넌트

- 버튼, 입력창, 카드, 탭, 목록, 툴팁, 모달, 알림, 진행 표시, 빈 상태
- 기본·호버·포커스·활성·선택·비활성·로딩·오류 상태
- 동일 기능은 모든 화면에서 동일한 외형과 동작 사용
- 변형을 무한히 만들지 않고 의도된 variant만 제공

#### 아이콘

- 하나의 일관된 아이콘 세트 또는 통일된 제작 규칙 사용
- 선 굵기, 모서리, 채움 방식, 시각적 크기 통일
- 의미가 불명확한 아이콘에는 레이블 또는 툴팁 제공
- 플랫폼 이모지를 핵심 UI 아이콘 대신 사용하지 않음

#### 배경과 캐릭터

- 배경은 정보 가독성을 방해하지 않으며 제품의 세계관과 깊이를 보강
- 캐릭터는 크기, 시점, 조명, 렌더링 스타일, 감정 표현을 통일
- 캐릭터·배경·UI가 서로 다른 화풍으로 충돌하지 않도록 아트 디렉션 정의
- 중요한 콘텐츠 위에 고대비 디테일을 겹치지 않음

#### 애니메이션과 피드백

- 전환은 상태 변화와 공간 관계를 설명하는 용도로 사용
- 입력 직후 즉각적인 시각·음향·햅틱 피드백 제공(플랫폼과 프로젝트 범위 내)
- 성공, 실패, 보상, 잠금 해제, 로딩의 피드백을 구분
- 과도한 바운스·글로우·파티클을 피하고 지속 시간과 easing을 통일
- `prefers-reduced-motion` 등 모션 감소 환경을 고려

### 이미지 에셋 계획

이미지를 무작정 생성하거나 인터넷에서 가져오지 않는다. 먼저 에셋 목록을 만든다.

```md
| ID | 용도 | 화면 | 크기/비율 | 스타일 | 상태/변형 | 투명 배경 | 출처/라이선스 | 우선순위 |
|---|---|---|---|---|---|---|---|---|
```

- 로고, 앱 아이콘, 캐릭터, 초상화, 배경, 아이템, 배지, 이펙트, 튜토리얼 이미지를 용도별로 구분한다.
- 최종 표시 크기에 맞는 해상도와 1x/2x 등 필요한 배율을 정의한다.
- 생성형 이미지 사용 시 공통 스타일 프롬프트, 팔레트, 조명, 카메라, 선 굵기, 금지 요소를 고정한다.
- 임시 에셋과 출시용 에셋을 명확히 구분하고 파일명·포맷·압축·저장 위치 규칙을 정한다.
- 외부 에셋은 상업 이용, 수정, 재배포 가능 여부와 출처를 기록한다.
- 성능을 위해 적절한 포맷, 압축, 지연 로딩, 스프라이트/아틀라스 사용 여부를 검토한다.

### 기능 보존형 디자인 리빌드 절차

1. 현재 화면과 주요 흐름을 캡처하고 기능 목록을 만든다.
2. 기존 기능별 입력, 결과, 저장 상태, 오류 상태를 기준선으로 기록한다.
3. 정보 구조와 사용자 흐름을 먼저 개선한다.
4. 디자인 토큰과 핵심 공용 컴포넌트를 만든다.
5. 위험이 낮은 화면부터 한 화면씩 교체한다.
6. 각 화면 교체 후 기능·반응형·접근성·시각 회귀를 검증한다.
7. 모든 화면이 안정화되기 전까지 기존 기능 코드를 불필요하게 재작성하지 않는다.
8. 마지막에 임시 스타일과 중복 컴포넌트를 안전하게 정리한다.

### 디자인 QA 체크리스트

- [ ] 제품의 장르와 브랜드 개성이 첫 화면에서 드러난다.
- [ ] 모든 화면에서 타이포, 간격, 색, 모서리, 그림자 규칙이 일관된다.
- [ ] 가장 중요한 행동과 정보가 즉시 구분된다.
- [ ] 버튼과 인터랙션의 모든 상태가 구현되어 있다.
- [ ] 로딩, 빈 상태, 오류, 성공, 비활성 상태가 빠짐없이 디자인되어 있다.
- [ ] 한글 줄바꿈, 긴 텍스트, 큰 숫자, 다국어 확장을 견딘다.
- [ ] 작은 화면, 큰 화면, 확대 환경에서도 잘리거나 겹치지 않는다.
- [ ] 키보드 포커스, 명도 대비, 터치 영역, 모션 감소를 고려한다.
- [ ] 아이콘과 이미지의 화풍·선·조명·품질이 통일되어 있다.
- [ ] 배경과 효과가 텍스트 및 조작 요소의 가독성을 방해하지 않는다.
- [ ] 애니메이션이 목적을 가지며 느리거나 피로하지 않다.
- [ ] 임시 에셋, 깨진 이미지, 저해상도 이미지가 남아 있지 않다.
- [ ] 디자인 변경 전의 핵심 기능이 모두 그대로 동작한다.
- [ ] 실제 실행 화면을 기준으로 리뷰했으며 코드만 보고 승인하지 않았다.
- [ ] “AI가 대충 만든 화면”처럼 보이는 반복적 카드·그라데이션·장식 남용이 없다.

---

## 10. 사용자 승인 규칙

다음 작업은 구현 전에 사용자 승인을 받는다.

- 핵심 기능, 사용자 데이터, 저장 형식, 공개 API의 삭제 또는 비호환 변경
- 데이터베이스·저장 데이터 마이그레이션과 되돌리기 어려운 작업
- 프레임워크, 언어, 상태 관리, 빌드 체계의 교체
- 대규모 디렉터리 개편 또는 전면 재작성
- 유료 API·서비스·에셋 도입, 새로운 운영비 발생
- 개인정보 수집, 권한 확대, 외부 전송, 보안 정책 변경
- 라이선스가 불분명한 코드·폰트·이미지·음원 사용
- 제품 정체성, 핵심 사용자 흐름, 주요 디자인 방향의 큰 변경
- S0/S1 이슈를 미해결 상태로 출시·완료 처리

승인 요청은 다음 형식으로 간결하게 제시한다.

```md
## APPROVAL REQUEST
- 결정할 내용:
- 필요한 이유:
- 선택지 A / 장단점:
- 선택지 B / 장단점:
- 추천안:
- 영향 범위:
- 되돌리기 방법:
```

승인이 필요하지 않은 범위에서는 불필요하게 멈추지 말고 합리적인 최소 가정으로 진행하되, 가정과 근거를 로그에 남긴다.

---

## 11. 세션 인수인계 규칙

세션 종료, 컨텍스트 부족, 담당자 교체 전에는 반드시 `AI_COLLAB.md`의 HANDOFF와 `개발로그.md`를 갱신한다.

인수인계에는 다음을 포함한다.

- 요청 목적과 현재 완료율
- 완료된 항목과 아직 하지 않은 항목
- 현재 빌드·테스트 상태
- 변경 파일과 각 파일의 역할
- 해결한 문제와 남은 이슈의 심각도
- 재현 절차와 검증 명령
- 중요한 설계 결정과 거절한 대안
- 다음 담당자가 가장 먼저 해야 할 한 가지
- 사용자 승인 대기 사항
- 임시 코드, 플래그, 에셋, 마이그레이션 주의사항

새 세션은 인수인계를 읽은 뒤 현재 상태를 짧게 재검증하고 이어간다. 이미 완료된 작업을 처음부터 반복하거나 기록 없이 다른 방향으로 재구현하지 않는다.

---

## 12. 완료 정의

작업은 아래 조건을 모두 만족해야 완료다.

- 요청된 사용자 가치와 명시적 완료 조건 충족
- 기존 핵심 기능 보존
- 공식 빌드와 필수 테스트 통과
- 변경 기능의 정상·실패·경계 흐름 검증
- 핵심 회귀 시나리오 통과
- S0/S1 미해결 이슈 없음 또는 사용자 예외 승인
- 디자인 작업이면 디자인 QA 체크리스트 완료
- Codex의 독립 재검수 PASS
- `AI_COLLAB.md` 정리 및 `개발로그.md` 기록 완료
- 사용자에게 변경 내용, 검증 결과, 남은 위험을 이해하기 쉽게 보고

---

## 13. 각 AI에게 바로 전달할 실행 프롬프트

### Claude Code 시작 프롬프트

```text
프로젝트 루트의 CLAUDE.md, AI_COLLAB.md, 개발로그.md와
CLAUDE_CODEX_COLLAB_PROMPT.md를 모두 읽고 그 규칙을 따라라.

너는 이 프로젝트의 메인 구현자이자 통합 책임자다. 기존 프로젝트를
재작성하지 말고 실제 코드와 실행 상태를 먼저 분석하라. 사용자의 요청을
완료 조건으로 정리한 뒤 AI_COLLAB.md에 작업 범위와 소유 파일을 선언하라.
상대 AI가 소유한 파일은 수정하지 마라.

구현은 최소 영향 범위로 진행하고 기존 기능과 데이터를 보존하라. 기능뿐
아니라 로딩·빈 상태·오류·경계 조건을 처리하고, 빌드·테스트·핵심 회귀를
검증하라. 디자인 작업은 문서의 상용 수준 UI/UX 원칙, 디자인 시스템,
에셋 계획, 디자인 QA를 반드시 적용하라. 완료 후 AI_COLLAB.md에 Codex
검수 요청을 남기고 개발로그.md에 변경 및 검증 결과를 기록하라.

대규모 리팩터링, 핵심 기능 삭제, 비호환 데이터 변경, 유료 서비스 도입,
중대한 디자인 방향 변경은 사용자 승인 전에 실행하지 마라.
```

### Codex 시작 프롬프트

```text
프로젝트 루트의 CLAUDE.md, AI_COLLAB.md, 개발로그.md와
CLAUDE_CODEX_COLLAB_PROMPT.md를 모두 읽고 그 규칙을 따라라.

너는 독립 검증자·테스터이며 필요할 때만 보조 구현자다. 먼저 현재 상태와
Claude의 REVIEW REQUEST를 확인하라. 검수만 맡았다면 구현 파일을 임의로
수정하지 말고 코드, 실제 실행, 빌드, 테스트, 경계 조건, 회귀, UI/UX를
독립적으로 검증하라. 발견 항목에는 S0~S3 심각도, 재현 절차, 기대 결과,
실제 결과, 사용자 영향을 기록하라. 개인 취향 대신 요구사항과 디자인
시스템 위반 또는 재현 가능한 증거를 제시하라.

보조 구현을 명시적으로 맡은 경우에만 AI_COLLAB.md에 소유 파일을 선언하고
그 범위만 수정하라. Claude가 수정한 뒤 원래 재현 절차와 전체 핵심 회귀를
다시 검사해 PASS 또는 남은 이슈를 기록하라. 결과와 인수인계를
AI_COLLAB.md 및 개발로그.md에 남겨라.
```

### ChatGPT 리뷰 프롬프트

```text
너는 이 프로젝트의 기획·디자인 디렉터이자 최종 리뷰어다. 사용자의 목표를
사용자 흐름과 완료 조건으로 구체화하고, Claude Code와 Codex의 결과를
기능 가치·사용성·디자인 일관성 관점에서 검토하라. 상용 게임/앱 수준의
정보 위계, 타이포, 간격, 컬러, 컴포넌트, 아이콘, 배경, 캐릭터,
애니메이션, 피드백을 기준으로 판단하라. 싸구려 AI 느낌의 무작위
그라데이션·글로우·장식·일관성 없는 에셋을 허용하지 마라.

큰 방향 변경이나 사용자 선택이 필요한 경우 선택지, 장단점, 추천안을
명확히 제시하고 사용자 승인을 받아라. 구현 결과는 가능하면 실제 화면과
사용자 흐름을 기준으로 리뷰하고, 수정 사항을 심각도와 우선순위로 정리하라.
```

---

## 14. 최초 세팅 체크리스트

- [ ] 이 파일을 프로젝트 루트에 `CLAUDE_CODEX_COLLAB_PROMPT.md`로 배치
- [ ] `CLAUDE.md` 생성 또는 기존 내용 검토
- [ ] `AI_COLLAB.md` 상태판 생성
- [ ] `개발로그.md` 누적 로그 생성
- [ ] 공식 실행·빌드·테스트 명령 기록
- [ ] 현재 기준 빌드 및 핵심 기능 상태 기록
- [ ] Claude Code와 Codex의 첫 작업 범위 및 소유 파일 분리
- [ ] 디자인 레퍼런스와 목표 품질 합의
- [ ] 디자인 토큰 및 이미지 에셋 계획 위치 결정
- [ ] 사용자 승인 필요 항목 확인

이 체크리스트가 끝난 뒤 첫 번째 작은 기능 또는 한 화면을 대상으로 `구현 → 검수 → 수정 → 재검수` 루프를 시작한다.
