# 《전역까지 D-100》 — 개발 핸드오프 문서

> 장르: 뱀파이어 서바이벌형 (Bullet Heaven) · 엔진: Godot 4.7 · 플랫폼: PC(Steam) 우선 → 모바일 이식 고려
> 이 문서는 **클로드코드에게 그대로 넘기는 사양서**입니다. `CLAUDE.md`로 프로젝트 루트에 두세요.

---

## 0. 먼저, 솔직한 규모 이야기

"대작 수준"은 한 번의 명령으로 나오지 않습니다. 이 사양대로 가면 **혼자 + AI 보조로 대략 6~9개월**, 데모까지는 2~3개월입니다.
대신 이 문서는 그 6개월을 **매주 뭘 시킬지 정해진 순서**로 쪼개놨습니다. M0부터 순서대로 시키세요. 한 번에 다 시키면 클로드코드가 뒤엉킵니다.

---

## 1. 컨셉

**한 판 = 전역까지 남은 100일 = 실시간 20분.**

말년 병장이 전역일까지 밀려드는 군생활(작업·훈련·검열)을 뚫고 살아남는다.
화면 상단에 `D-100`이 실시간으로 줄어들고, `D-DAY`가 되면 위병소 문이 열린다.

- **승리 조건**: 20분 생존 → 위병소 통과 컷신
- **패배**: 체력 0 → "전역 연기" 화면
- **톤**: 예비군 감성의 블랙코미디. 훈련·작업·행정의 부조리를 웃음으로. **구타·가혹행위·인권침해 소재는 넣지 않습니다** (재미도 없고 심의·여론 리스크만 큼). 적은 "사람"이 아니라 **삽, 낙엽, 모포, 제설, 검열 서류** 같은 사물·상황으로 의인화합니다.
- **실명 회피**: 실제 부대명·인물명·상표명(예: 특정 제과 브랜드) 대신 패러디 명칭 사용. `초코파이` → `찰떡파이`, 부대명은 `○○사단`.

### 핵심 루프
```
이동(자동공격) → 적 처치 → '짬'(경험치) 획득 → 진급(레벨업)
→ 명령서 3장 중 1장 선택(무기/패시브) → 무기 진화 → 보스
→ 20분 생존 → 전역 → 월급으로 영구 강화 → 다음 판
```

### 진급 = 레벨
| 레벨 | 계급 | 연출 |
|---|---|---|
| 1–5 | 이등병 | 계급장 UI 1줄 |
| 6–15 | 일병 | 2줄, 이동속도 +5% |
| 16–30 | 상병 | 3줄, 공격속도 +5% |
| 31–50 | 병장 | 4줄, 화면 테두리 금색 글로우 |
| 50+ | 말년 | 화면 채도 상승 + BGM 변주 |

---

## 2. 왜 Godot 4.7인가 (엔진 결정)

클로드코드로 개발한다는 전제에서 Godot이 압도적입니다.

| 항목 | Godot 4.7 | Unity |
|---|---|---|
| 씬/리소스 파일 | `.tscn`/`.tres` = **사람이 읽는 텍스트** → AI가 직접 작성·수정 가능 | YAML + GUID 참조 → AI가 눈으로 못 봄, 깨지기 쉬움 |
| 에디터 없이 작업 | 가능 (CLI 빌드, 텍스트 편집) | 사실상 불가 |
| 2D 라이팅/셰이더 | Normal map, PointLight2D, WorldEnvironment glow 전부 내장 | URP 2D 세팅 필요 |
| 빌드 | `godot --headless --export-release` 한 줄 | 무거움 |
| 라이선스/수수료 | MIT, 무료 | 조건부 |

**버전 고정**: 최신 stable은 4.7.2 (2026년 8월 기준). `4.7.x`로 고정하고 프로젝트에 명시. 렌더러는 **Forward+** (2D 라이팅·글로우 품질 때문). 저사양 옵션으로 Mobile 렌더러 토글 제공.

> 참고: 4.6에서 SSR 재작성·Jolt 물리 기본화 등 워크플로 정비가 있었고, 4.7이 그 위 최신 stable입니다. 3.x는 쓰지 마세요.

---

## 3. 아트 디렉션 ★ 최우선

그래픽이 제일 중요하다고 하셨으니 여기가 이 문서의 심장입니다.

### 3.1 스타일 정의
- **고해상도 2D 카툰** (픽셀아트 아님). 한국 웹툰/애니 느낌의 셀셰이딩 + 두꺼운 잉크 외곽선.
- **픽셀아트를 버리는 이유**: AI 이미지 생성은 픽셀아트를 제대로 못 뽑습니다(가짜 픽셀, 팔레트 붕괴). 반대로 고해상도 카툰 일러스트는 AI가 가장 잘하는 영역입니다. 그래픽이 최우선이면 AI의 강점 쪽으로 가야 합니다.
- **시점**: 3/4 톱다운 (45° 부감). 캐릭터는 정면 기울임.
- **해상도 기준**: 1920×1080. 캐릭터 원본 512px, 인게임 표시 96~128px. 보스 1024px.

### 3.2 컬러 규칙 (가독성 = 생존)
화면에 적이 500마리 나오는 장르입니다. **예쁨보다 가독성이 먼저.**

| 역할 | 색 | 규칙 |
|---|---|---|
| 배경/지형 | 국방색 `#3E4A32`, 카키 `#7A6E4E` | **채도 40% 이하로 눌러버림** |
| 적 | 갈색·회색 계열 | 외곽선 어둡게, 실루엣 단순화 |
| 플레이어 | 밝은 올리브 + 흰 하이라이트 | 항상 화면에서 가장 밝음 |
| 플레이어 공격 이펙트 | 시안 `#3FE0D0`, 금색 `#FFC94A` | **고채도 독점** — 적은 절대 이 색 안 씀 |
| 위험(적 공격/장판) | 진홍 `#C8102E` | 이 색 = 무조건 피해야 함 |
| 회복/획득 | 연녹 `#8FE388` | |

**철칙**: 적은 절대 시안/금색을 쓰지 않는다. 플레이어 이펙트는 절대 붉은색을 쓰지 않는다.

### 3.3 라이팅 & 포스트프로세싱
이게 "인디 티" 벗기는 핵심입니다.

- `CanvasModulate`로 맵별 전역 톤 (야간 위병소는 짙은 남색)
- `PointLight2D`: 총구 화염, 조명탄, 연막 발광, 플레이어 발밑 약한 림라이트
- **노멀맵**: 주요 캐릭터·오브젝트에 노멀맵 텍스처 부여 → 조명 받으면 입체감 (AI 이미지에서 자동 생성 가능, 파이프라인 참조)
- `WorldEnvironment`: Glow(bloom) intensity 0.6, Adjustments로 대비 +10%, 채도 +5%
- 비네트 + 아주 약한 색수차 (강하면 촌스러움)
- **히트 필 3종 세트** (필수):
  1. `hit_flash.gdshader` — 피격 시 2프레임 흰색 플래시
  2. **히트스톱** — 강한 타격 시 `Engine.time_scale = 0.05` 후 0.04초 복구
  3. **스쿼시&스트레치** — 이동/피격 시 스프라이트 정점 셰이더로 눌림 (Sprite2D 스케일 애니 아님, 셰이더로)
- 화면 흔들림: Perlin 노이즈 기반, 강도 슬라이더로 조절 가능(접근성)

### 3.4 UI 아트
**컨셉: 군대 행정 서류.**
- 갱지/누런 종이 텍스처, 빨간 인장 도장, 타자기 느낌
- 레벨업 선택창 = **「병력 운용 명령서」** 3장이 책상에 툭 놓이는 연출
- 결과 화면 = **전역증** 발급 (도장 쾅)
- 폰트: `Pretendard`(SIL OFL, 상업 이용 자유) 본문 + 제목용 굵은 고딕. **폰트 라이선스는 반드시 확인** — 무료 배포 폰트도 게임 내장은 조건이 다른 경우 있음.

### 3.5 사운드 (그래픽만큼 체감 큼)
- BGM: 군가 리듬 × 신스웨이브/락 리믹스. 보스전엔 나팔 모티프.
- SFX: 삽 긁는 소리, 호루라기, 기상나팔, 무전기 노이즈, "일병 김○○!" 짧은 보이스
- **오디오 덕킹**: 보스 등장 시 SFX 살짝 낮추고 BGM 강조

---

## 4. AI 아트 파이프라인 (그록 활용법) ★

여기가 가장 실무적인 부분입니다.

### 4.1 대원칙
> **AI에게 스프라이트 시트를 시키지 마세요. 반드시 실패합니다.**
> AI는 프레임 간 일관성을 못 지킵니다. 걷기 8프레임을 뽑으면 프레임마다 얼굴이 다릅니다.

**정답 파이프라인:**
```
그록으로 캐릭터 1장(정지) 고해상도 생성
  → 배경 제거 (rembg)
  → 파츠 분리 (머리/몸통/양팔/양다리/무기) — 포토샵 or GIMP or 스크립트
  → Godot Skeleton2D + Polygon2D 컷아웃 리깅
  → 걷기/공격/피격/사망 애니메이션은 리깅으로 재사용
```
이 방식이면 **캐릭터 1장으로 모든 애니메이션이 나오고**, 새 캐릭터도 뼈대를 그대로 복사해서 붙입니다. 8종 캐릭터 = 이미지 8장 + 리깅 재사용.

### 4.2 그록에게 시킬 것 / 시키면 안 되는 것

| ✅ 그록에게 시킬 것 | ❌ 시키면 안 되는 것 |
|---|---|
| 컨셉아트, 무드보드 | 애니메이션 프레임 |
| 캐릭터 키비주얼 (정지 1장) | 이어지는 타일셋 (심 안 맞음) |
| 보스 일러스트 | UI 최종 레이아웃 |
| 배경 텍스처, 지형 소재 | 아이콘 세트의 일관된 스타일 (개별로 뽑으면 다 다름) |
| 무기/아이템 아이콘 원안 | 최종 게임 애셋 그대로 사용 |
| 스팀 상점 아트, 트레일러 배경 | |

**아이콘 일관성 트릭**: 아이콘은 한 장에 **4×4 그리드로 16개를 한 번에** 생성 요청 → 잘라 쓰기. 개별 생성보다 스타일이 훨씬 균일합니다.

### 4.3 후처리 자동화 스크립트 (클로드코드에게 시킬 것)
`tools/art_pipeline.py` 를 만들게 하세요. 기능:
1. `rembg`로 배경 제거 + 알파 엣지 정리
2. **팔레트 강제 매핑** — 3.2의 컬러 규칙 팔레트로 색 스냅 (스타일 통일의 핵심)
3. 외곽선 강화 (Sobel 엣지 → 어두운 라인 합성)
4. **노멀맵 자동 생성** (그레이스케일 높이맵 → 노멀 변환)
5. 텍스처 아틀라스 패킹 (`.png` + `.tres` AtlasTexture 자동 생성)
6. 원본은 `art/raw/`, 결과는 `art/processed/`, 아틀라스는 `art/atlas/`

> 이 스크립트가 있으면 그록이 뭘 뱉든 **자동으로 우리 게임 색으로 통일**됩니다. 스타일 붕괴 방지 장치입니다.

### 4.4 그록 프롬프트 예시 (영어로 넣으세요)

**플레이어 캐릭터**
```
Full body character illustration, top-down 3/4 view, Korean army conscript soldier
in olive drab fatigues and combat boots, holding a small field shovel,
tired but determined expression, Korean webtoon art style, thick black ink outlines,
cel shaded flat colors, muted olive and khaki palette with bright cyan rim light,
transparent background, centered, no text, sharp clean lineart, 512x512
```

**보스 (검열관)**
```
Top-down 3/4 view boss character, imposing military inspector figure made of stacked
paperwork and clipboards, red official stamps floating around, glowing crimson eyes,
Korean webtoon style, thick ink outlines, cel shading, dark khaki and crimson palette,
dramatic rim lighting, transparent background, no text, 1024x1024
```

**배경 텍스처**
```
Seamless tileable texture, packed dirt military parade ground with faint white line
markings, top-down view, muted desaturated brown and olive, subtle gravel detail,
flat cel shaded style, no shadows, no characters, no text, 1024x1024
```

**아이콘 16종 배치**
```
4x4 grid of 16 military item icons on transparent background: field shovel, canteen,
combat boots, helmet, smoke grenade, bugle, ration can, wristwatch, rank insignia,
ammo belt, radio, flashlight, entrenching tool, poncho, whistle, leave pass.
Consistent Korean webtoon icon style, thick black outlines, cel shaded,
olive and khaki palette with gold accents, each icon centered in its cell, no text
```

---

## 5. 게임 디자인 데이터

### 5.1 무기 10종 (+ 진화)

| # | 무기 | 동작 | 진화 조건 | 진화형 |
|---|---|---|---|---|
| 1 | **야전삽** | 전방 회전 베기 | +탄띠 | **굴삭기** — 전방위 광역 |
| 2 | **K-2식 소총** | 최근접 적 자동사격 | +계급장 | **분대지원화기** — 3점사 관통 |
| 3 | **수통** | 물 장판, 지속 피해 | +군화 | **급수차** — 대형 이동 장판 |
| 4 | **예초기** | 주변 상시 회전 | +전투화 | **트랙터** — 범위 2배 + 넉백 |
| 5 | **연막탄** | 포물선 투척 폭발 | +시계 | **다연장 발연통** — 5발 동시 |
| 6 | **행군화** | 지나온 자리 발자국 장판 | +무전기 | **완전군장 행군** — 장판 확대·지속 |
| 7 | **짬통** | 뒤에 흘려 적 유인+피해 | +건빵 | **잔반차** — 적 흡인 |
| 8 | **위생병** | 주기적 회복 오브 생성 | +찰떡파이 | **의무후송 헬기** — 전체 회복 |
| 9 | **방탄모** | 방어 오라, 근접 반사 | +방탄복 | **완전무장** — 무적 프레임 |
| 10 | **기상나팔** | 화면 전체 스턴+넉백 | +휴가증 | **국기하강식** — 전 적 5초 정지 |

### 5.2 패시브 10종
전투화(이속) · 탄띠(투사체+) · 계급장(공격력) · 시계(쿨감) · 건빵(체력회복) · 찰떡파이(부활 1회) · 사이버지식정보방(경험치+) · 무전기(획득범위) · 방탄복(방어) · 휴가증(행운/희귀드랍)

### 5.3 적 설계
- **잡몹(15종)**: 삽, 낙엽, 모포, 관물대, 모기, 눈덩이(제설), 잡초, 불침번 유령, 신병, 각목, 페인트통, 자재, 창고박스, 화생방 마스크, 짬통
- **중형(5종)**: 당직사관, 행보관, 유격조교, CS가스 구름(장판), 눈보라
- **보스(4종)**:
  - 5분 — **대대장 순시** (돌진 + 소환)
  - 10분 — **사단 검열관** (서류 탄막, 3.4의 종이 UI와 연결)
  - 15분 — **유격 3주차** (플레이어 이속 감소 필드 + PT 카운트 패턴)
  - 20분 — **전역 연기 통보서** (페이크 승리 → 진짜 최종전)

### 5.4 웨이브 디렉터
시간 기반 테이블(`.tres` 리소스)로 정의. 1분 단위 구간마다 `{적 종류, 밀도, 스폰 패턴(원형/벽/기둥), 엘리트 확률}`.
**밀도 곡선**: 0~5분 완만 → 5분 보스 → 6~10분 급증 → 15분 이후 화면 절반이 적. 20분 직전 1분은 **의도적 소강** (긴장 완화 후 최종전).

### 5.5 플레이어 캐릭터 8종
김이병(밸런스) · 최상병(공속+) · 박병장(말년: 체력↓ 공격력↑) · 하사(직업군인: K-2 시작) · 의무병(회복) · 취사병(짬통 시작) · 운전병(이속+) · 통신병(범위+)

### 5.6 맵 4종
연병장 / 유격장(산악) / 혹한기 설원 / **야간 위병소**(라이팅 쇼케이스, 트레일러용)

### 5.7 메타 진행
판 종료 시 획득한 **월급**으로 PX 상점에서 영구 강화. 도전과제 = **표창장** 수집. 캐릭터·맵 해금.

---

## 6. 기술 아키텍처

### 6.1 폴더 구조
```
res://
├── autoload/       GameState, EventBus, AudioManager, SaveSystem, ObjectPool
├── core/
│   ├── combat/     damage_system.gd, stat_block.gd, status_effect.gd
│   ├── spawn/      spawn_director.gd, spatial_hash.gd
│   └── upgrade/    upgrade_pool.gd, evolution_rules.gd
├── entities/
│   ├── player/     player.gd, player.tscn
│   ├── enemies/    enemy_manager.gd  ← ★ 개별 노드 아님, 데이터 배열
│   └── pickups/
├── weapons/        base_weapon.gd + 10종
├── data/           *.tres (WeaponData, EnemyData, WaveData, UpgradeData, CharacterData)
├── ui/             hud/, level_up/, results/, meta/
├── vfx/            shaders/, particles/
├── maps/
├── art/            raw/ processed/ atlas/
└── tools/          art_pipeline.py, build.sh
```

### 6.2 성능 설계 ★ (이거 안 지키면 200마리에서 죽습니다)

**목표: 3,000마리 동시 @ 60fps @ 1080p**

1. **적을 Node로 만들지 않는다.**
   `EnemyManager`가 `PackedFloat32Array`로 위치·속도·체력·타입을 배열로 관리.
2. **렌더 = `MultiMeshInstance2D`**
   타입별로 MultiMesh 1개. 애니메이션은 셰이더에서 UV 오프셋 + 시간 기반 bob/squash로 GPU 처리.
3. **충돌 = 자체 Spatial Hash Grid**
   Godot 물리 엔진 쓰지 마세요. 셀 크기 64px 해시 그리드로 O(1) 근접 질의.
4. **투사체·데미지 넘버·파티클 전부 오브젝트 풀링.** 런타임 `instantiate()` 금지.
5. **데미지 넘버**는 개별 Label 금지 → 전용 MultiMesh + 폰트 아틀라스, 동시 최대 120개 캡.
6. **저사양 옵션**: 데미지 넘버 off, 파티클 밀도, 글로우 off, Mobile 렌더러 토글.

### 6.3 데이터 주도 설계
무기·적·업그레이드·웨이브는 **전부 `Resource` (.tres)**. GDScript 하드코딩 금지.
→ 밸런싱을 코드 수정 없이 데이터만 고쳐서 하고, 클로드코드에게 "적 20종 추가" 같은 걸 안전하게 시킬 수 있습니다.

### 6.4 EventBus
`signal enemy_died(pos, xp)`, `signal player_leveled(level)`, `signal boss_spawned(id)` 등 전역 시그널로 결합도 낮추기. UI·오디오·VFX는 전부 EventBus 구독.

---

## 7. 마일스톤 (이 순서대로 시키세요)

| M | 기간 | 산출물 | 완료 기준 |
|---|---|---|---|
| **M0** | 1주 | 화이트박스 프로토타입 (전부 도형) | 이동+자동공격 1종+적 1종+XP+레벨업+20분 타이머가 돈다 |
| **M1** | 2주 | 코어 루프 | 무기 5·패시브 5·진화·웨이브 디렉터·보스 1·승패 화면 |
| **M2** | 2주 | **아트 1차 패스** | 아트 파이프라인 구축, 플레이어+적 5종 실아트, 라이팅·글로우·히트필 3종 |
| **M3** | 3주 | 콘텐츠 확장 | 무기 10·패시브 10·적 20·보스 4·캐릭터 8·맵 3 |
| **M4** | 1주 | 메타 진행 | 저장, PX 상점, 해금, 표창장 |
| **M5** | 2주 | 폴리시 | 사운드 전체, 튜토리얼, 옵션, 한/영 로컬라이즈, 성능 최적화 |
| **M6** | 2주 | 출시 준비 | Steam 페이지, 데모 빌드, 트레일러 |

**성능 검증은 M1에서 합니다.** 아트 넣기 전에 3,000마리 스트레스 테스트를 통과해야 M2로 갑니다. 아트 넣고 나서 성능 문제 발견하면 재작성입니다.

---

## 8. 클로드코드 핸드오프 프롬프트 팩

아래를 **순서대로, 하나씩** 넣으세요. 한 번에 두 개 이상 주지 마세요.

### 프롬프트 0 — 프로젝트 초기화
```
이 문서(기획서.md)를 읽고 CLAUDE.md로 정리해서 프로젝트 루트에 저장해줘.
그다음 Godot 4.7 프로젝트를 초기화해:
- 렌더러 Forward+, 해상도 1920x1080, 스트레치 모드 canvas_items / expand
- 6.1의 폴더 구조 전부 생성
- autoload 5개(GameState, EventBus, AudioManager, SaveSystem, ObjectPool) 스텁 작성
- .gitignore, README.md
- tools/build.sh (godot --headless --export-release 래퍼)
아직 게임 로직은 만들지 마. 뼈대만.
```

### 프롬프트 1 — M0 프로토타입
```
M0을 구현해줘. 그래픽은 전부 단색 도형(ColorRect/원)으로.
1. 플레이어: WASD 8방향 이동, 카메라 추적
2. 무기 '야전삽': 0.8초마다 전방 부채꼴 베기, 데미지 10
3. 적 1종: 플레이어를 향해 직선 추격, 체력 20, 접촉 시 초당 5 피해
4. 처치 시 '짬' 드랍 → 자석 범위 안에서 플레이어에게 흡수 → XP
5. 레벨업 시 게임 일시정지 + 카드 3장 선택 UI (더미 업그레이드 3개)
6. 상단에 D-100 → D-DAY 카운트다운 (실시간 20분)
7. 20분 생존 시 승리 화면, 체력 0 시 패배 화면

핵심: 적은 개별 Node로 만들지 말고 EnemyManager가 배열로 관리하고
MultiMeshInstance2D로 렌더해줘. 충돌은 spatial hash grid 직접 구현.
이 구조는 나중에 절대 안 바꿀 거니까 처음부터 제대로 잡아줘.
```

### 프롬프트 2 — 성능 검증
```
적 3000마리를 동시에 스폰하는 스트레스 테스트 씬을 만들고,
1080p에서 60fps가 나오는지 프로파일링해줘.
FPS/적 수/드로우콜을 화면에 표시하는 디버그 오버레이도 추가.
60fps 미달이면 병목을 찾아서 고치고, 뭘 고쳤는지 보고해줘.
```

### 프롬프트 3 — 데이터 주도 전환
```
무기/적/업그레이드/웨이브를 전부 Resource(.tres) 기반으로 리팩터링해줘.
WeaponData, EnemyData, UpgradeData, WaveData 클래스를 정의하고
기존 하드코딩된 값을 data/ 폴더의 .tres 파일로 이관.
앞으로 밸런싱은 코드 수정 없이 .tres만 고쳐서 되게 해줘.
```

### 프롬프트 4 — M1 코어 루프
```
기획서 5.1~5.4를 참고해서:
- 무기 5종(야전삽, K-2식 소총, 수통, 예초기, 연막탄) 구현
- 패시브 5종(전투화, 탄띠, 계급장, 시계, 건빵)
- 무기 진화 시스템 (무기 Lv8 + 지정 패시브 보유 시 보물상자에서 진화)
- SpawnDirector: 1분 구간별 웨이브 테이블(.tres) 기반
- 5분 보스 '대대장 순시' 1종
전부 데이터 주도로. 새 무기 추가가 .tres 하나 만드는 걸로 끝나야 해.
```

### 프롬프트 5 — 아트 파이프라인
```
tools/art_pipeline.py 를 만들어줘. 기획서 4.3 사양대로:
1. rembg 배경 제거 + 알파 엣지 정리
2. 기획서 3.2 팔레트로 색상 강제 스냅
3. Sobel 엣지 기반 외곽선 강화
4. 그레이스케일 → 노멀맵 자동 생성
5. 텍스처 아틀라스 패킹 + AtlasTexture .tres 자동 생성
입력 art/raw/ → 출력 art/processed/, art/atlas/
CLI: python tools/art_pipeline.py --input art/raw --palette core --normal
requirements.txt도 같이.
```

### 프롬프트 6 — 그래픽 폴리시 (가장 중요)
```
기획서 3.3의 비주얼 품질을 구현해줘:
1. hit_flash.gdshader — 피격 시 2프레임 흰 플래시
2. squash_stretch.gdshader — 이동/피격 시 정점 셰이더 눌림
3. 히트스톱 시스템 (강타 시 Engine.time_scale 0.05 → 0.04초 복구)
4. WorldEnvironment: glow 0.6, 대비 +10%, 채도 +5%, 비네트
5. 노멀맵 대응 2D 라이팅 (CanvasModulate + PointLight2D)
6. Perlin 노이즈 화면 흔들림 (강도 슬라이더 연동)
7. 데미지 넘버 — 개별 Label 금지, MultiMesh+폰트아틀라스, 동시 120개 캡
   크리티컬은 크기 1.5배 + 금색
전부 설정에서 on/off 가능하게. 저사양 프리셋도.
```

### 프롬프트 7 — 컷아웃 리깅 템플릿
```
캐릭터 컷아웃 리깅 템플릿 씬을 만들어줘.
Skeleton2D + Polygon2D 기반으로 머리/몸통/좌우팔/좌우다리/무기 파츠 슬롯을 두고,
걷기/공격/피격/사망 4개 AnimationPlayer 애니메이션을 뼈대에 정의.
새 캐릭터는 파츠 텍스처만 교체하면 애니메이션이 그대로 재사용되게 해줘.
사용법을 docs/rigging.md에 정리해줘.
```

**이후 M3~M6는 위와 같은 방식으로 기획서 섹션을 지정해서 순차 진행.**

### CLAUDE.md에 반드시 넣을 규칙
```
- 적/투사체는 절대 개별 Node로 만들지 않는다. EnemyManager 배열 + MultiMesh.
- 물리 엔진 대신 spatial hash grid를 쓴다.
- 런타임 instantiate() 금지. ObjectPool 사용.
- 게임 수치는 GDScript에 하드코딩하지 않는다. 전부 .tres.
- 새 기능 추가 후 반드시 3000마리 스트레스 테스트 통과 확인.
- 적은 시안/금색을 쓰지 않고, 플레이어 이펙트는 붉은색을 쓰지 않는다.
- 실제 부대명·인물명·상표명 사용 금지.
```

---

## 9. 리스크 체크리스트

- [ ] **성능**: M1에서 3,000마리 검증 완료했는가 (아트 넣기 전에)
- [ ] **아트 일관성**: 팔레트 스냅 파이프라인이 돌고 있는가
- [ ] **폰트 라이선스**: 게임 내장 배포 조건 확인했는가
- [ ] **상표·실명**: 패러디 명칭으로 치환했는가
- [ ] **소재 톤**: 가혹행위·인권침해 소재가 들어가지 않았는가
- [ ] **AI 생성물**: Steam은 AI 생성 콘텐츠 사용 시 **신고 의무**가 있습니다. 출시 전 최신 정책 확인 필수
- [ ] **스코프**: M3에서 콘텐츠 욕심으로 무한정 늘어나지 않게 컷라인 정하기

---

## 10. 다음 액션

1. Godot 4.7.x 설치
2. 빈 폴더 만들고 이 문서를 `기획서.md`로 저장
3. 클로드코드 실행 → **프롬프트 0** 붙여넣기
4. 그록에는 병행해서 **4.4의 프롬프트**로 컨셉아트부터 뽑기 시작 (M2 전에 소재 쌓아두기)
