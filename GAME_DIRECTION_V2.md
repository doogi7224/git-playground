# Gearwood: The Broken Spring — 게임 방향 V2

> 최신 기준: 사용자가 구간 분할·Route Gate·전역 자연/기계 전환의 쓰임과 재미를 모두 불분명하다고 판단했다. 이 문서가 기존 관련 기획보다 우선한다.

## 한 문장

**달리고, 뛰고, 대시하고, 그래플을 끊기지 않게 이어서 한 방향으로 질주하는 액션 플랫포머.**

## 핵심 원칙

- Route Gate, 분기, 시간제 문, 모드 선택을 사용하지 않는다.
- 길은 하나다. 코인·적·발판은 자연스러운 한 동선 위에서 이동 액션을 이어 주는 역할만 한다.
- 잘 플레이하면 높은 코인 줄과 더 빠른 착지로 이어진다. 놓치면 즉시 아래 안전한 길로 이어진다.
- 자연과 기계는 선택형 시스템이 아니라 세계의 아트·발판·적 개성으로만 남긴다.
- 재미의 중심은 "무슨 상태를 골라야 하지?"가 아니라 "이 흐름을 계속 이어 갈 수 있을까?"다.

## 핵심 루프: Momentum Run

```text
이동 액션 연결 → FLOW/Overdrive 축적 → 다음 이동 도전 돌파 → 코인·스톰프 보상 → 더 큰 흐름
```

FLOW는 대시, 스톰프, 벽점프, 그래플, 공중 코인 연속 수집으로 쌓인다. 기존 Overdrive 게이지와 콤보를 기반으로 하되, 수치 변경은 플레이테스트 후에만 한다.

## Area 1의 첫 90초

1. **맛보기**: 넓은 평지에서 대시와 낮은 코인 줄을 한 번에 이해한다.
2. **연결**: 낮은 발판 → 공중 코인 → 첫 Cogmite 스톰프 → 안전한 착지로 입력을 이어 본다.
3. **숙련**: 대시 또는 벽점프를 잘 쓰면 높은 보너스 코인을 먹고, 놓치면 바로 아래 길로 이어진다.
4. **결산**: 연속 액션으로 찬 FLOW/Overdrive를 첫 강한 적과 보너스 보상에 쓴다.

## 역할

### Codex — 게임 디렉터·디자인 구현

- 재미의 기준, 구간 흐름, 캐릭터·환경·UI·이펙트와 실제 화면 구현을 맡는다.
- 실제 플레이 화면에서 동선이 자연스러운지 검수하고 Claude에게 다음 작은 구현 단위를 지시한다.

### Claude Code — 시스템 구현·테스트

- 게임 로직, 레벨 데이터, 충돌, 상태, 회귀 테스트를 맡는다.
- Codex가 정한 동선을 시스템으로 구현하고, 모호하거나 기존 규칙과 충돌하면 `AI_COLLAB.md` REVIEW REQUESTS에 남긴다.

## Claude 즉시 작업: Route Gate 완전 제거

1. `RouteGate`, `RouteGateKind`, `Platform.activeGate`, `routeGates`, `stepRouteGates`, `ROUTE_GATE_*`, `RouteGateView` 및 `GameScreen`의 연결 코드를 제거한다.
2. Route Gate용 발판·코인은 조건 없는 연속 동선으로 재배치하거나, 시작 구간을 어지럽히면 제거한다.
3. 기존 Bloom Shift는 확장하지 않고 격리 유지한다. 삭제 여부는 다음 별도 기획 결정으로 미룬다.
4. 점프·대시·그래플·스톰프·체크포인트·보스·Gear Socket 회귀를 검증한다.

### 완료 조건

- 시작 구간에 Route Gate/분기/시간제 길이가 전혀 없다.
- 첫 90초 동안 연속 이동·코인·스톰프·대시/벽점프 흐름이 자연스럽게 이어진다.
- `npx tsc --noEmit`과 기존 핵심 로직 회귀 검증을 통과하고, 기록을 `AI_COLLAB.md`/`개발로그.md`에 남긴다.

## Codex 후속

Route Gate 제거 후 Area 1의 코인 줄·발판·카메라 시선·적 배치와 시각 피드백을 직접 검수하고 다듬는다. 연속 동선이 재밌다고 확인된 뒤에만 나머지 몬스터·플랫폼·Area 2~3를 확장한다.
